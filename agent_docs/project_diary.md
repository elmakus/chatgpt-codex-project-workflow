# Project diary

## 2026-09-23 — V2 continuation authority

Use current `elmakus/project_workflow_v2@main` as operational authority, as specified by `PROJECT.md` and `workflow/PWV2_ADOPTION.md`. A migration package pin records that migration event; legacy in-repository V1 contracts do not override V2 routing.

A fresh-session locator identifies the first obligation to recover; current Git and exact subjects govern continuation. See the 2026-09-24 correction below for the distinction between deliberate optional-handoff choice and arbitrary locator text.

## 2026-09-23 — Planning verification lessons

Distinguish a safe serial fallback from contradictory durable state requiring Recovery. Review sequencing must have no circular gate: the final workstream review follows completed Milestone reviews. PW specifies semantic eligibility and evidence; runtime realization remains outside the kernel.

Helper-less parity needs independent reasoning from canonical inputs with a committed answer before reference comparison; another code path alone cannot prove it. Verify exact Git blob pins mechanically before freezing the plan, then store its immutable subject in a later metadata commit to avoid self-reference. Detailed acceptance remains canonical in the P1 plan and its planner audit.

## 2026-09-24 — Optional handoff and material Planning correction

Do not infer a second user confirmation from a missing procedural verb when accepted authority already offers the action as a valid alternative. A deliberate offered A/C handoff choice and an arbitrary or stale locator are different events. Governed persistence of the exact gate and readback precede deterministic continuation; context replacement does not add another confirmation. The evidence-linked classification is canonical in the workstream's `PLANNING_CORRECTION_OPTIONAL_PREMIUM_HANDOFF_2026-09-24.md` record. It corrects the overbroad earlier locator-arrival lesson without making chat a source of product authority.

A material finding after freeze requires an explicit new Planning cycle and its own A/B/C subjects. Preserve the original frozen blob and prior gates through exact Git references; do not fabricate a RED review, invent an unsupported hold state or treat a draft scaffold as a completed correction. Premium B retains its distinct mandatory fresh-independent boundary.

## 2026-09-24 — Exact result handoff after runtime blocker

Recover a blocker from the selected consumer workstream and exact branch before assessing whether a changed runtime resolves it. An implementation QA pass and clean tests establish a reviewable result, not the required formal independent Card verdict. Persist the result at an immutable commit/blob and freeze a pending exact review attempt before a fresh-session handoff; keep historical blockers and RED attempts as provenance while current Board pointers advance.

## 2026-09-24 — R01 correction and completeness proof

A pre-Final reconciliation gate that checks only caller-supplied review attempts or a caller-supplied snapshot cannot prove completeness: an omitted earlier open observation remains invisible. The bounded BOOT-A gate must derive from durable Task Board review-attempt locators or another verifiable source; multi-layer M05 lifecycle aggregation may remain later. Three independent read-only lanes reproduced the gap, and a board-bound product correction plus independent provisional QA now covers it.

Preserve terminal RED attempts and their exact failed commit/blob. A repaired working-tree diff, even with passing tests, is not a new immutable review subject. Formal R02 requires a new committed product subject, updated consumer result pin, and a fresh independent Tester.

The installed PWV2 0.2.1 router currently returns Execution Prep from approved Planning before it reads an existing active Task Board. Its advertised RED-correction route therefore does not select this workstream's exact current obligation. Resolve the selector defect before depending on it for automatic continuation.

The current codex_workflow Executor role TOMLs forbid Git-state edits, while the user forbids Main from material Git/GitHub operations. This is an operational authority conflict, not a product defect; preserve the uncommitted patch in persistent storage and obtain explicit direction before commit/push or formal review.

## 2026-09-24 — Router precedence diagnosis

Later user authorization permits Main to perform ordinary Git/state writes for already-authorized Cards, resolving the earlier Git-operator conflict. A separate fail-closed requirement now controls continuation: installed `pw` 0.2.1 and local `project_workflow_v2@origin/main:986afff` return approved-plan Execution Prep before reading a co-bound active Task Board, so terminal RED is masked. Consumer P6 and Board rev46 are valid together. Do not use a V1 router or a work-branch/local-cache hotfix as canonical authority. A two-file proposal aligned to existing ROUTER.md passed 165 tests and independent read-only consumer routing QA, but changing and installing the Git-sourced router is outside M02R-T03 and requires its own scope/deployment decision. Preserve the product R01 repair patch until routing authority is valid again.

## 2026-09-25 — Router recovery and Card-level M02R checkpoint

The separate router precedence hotfix was integrated and installed under its own authorization. The canonical selector then read the existing consumer Board and resumed exact current obligations; the T03 R01 RED and historical M02 terminal records were not rewritten. Earlier entries above describe the state before that hotfix, not a current blocker.

BOOT-D split REQ-131 falsification evidence and REQ-132 YAGNI scope into separately useful Cards. A newly absent module causes a loader failure, not a meaningful behavioral RED; T12 and T13 report their own execution as `legacy_compatible` rather than inventing pre-implementation falsification. Independent review found the prospective contracts GREEN. REQ-132 MUST wording was corrected before T13 commit. Deterministic validation cannot establish the truth of self-declared natural-language scope; review retains that semantic check, and T13 R01 records its advisory disposition.

All accepted M02R BOOT-A/B/C/D Card-level work is now terminal with required reviews GREEN. The user placed an explicit audit-reconciliation checkpoint before the M02R Milestone Review and any M03 transition. Preserve exact result/review Git bindings and request the accumulated out-of-band audit findings at that checkpoint.
