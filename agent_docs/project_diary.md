# Project diary

## 2026-09-23 — V2 continuation authority

Use current `elmakus/project_workflow_v2@main` as operational authority, as specified by `PROJECT.md` and `workflow/PWV2_ADOPTION.md`. A migration package pin records that migration event; legacy in-repository V1 contracts do not override V2 routing.

A fresh-session locator identifies the first obligation to recover. Its arrival alone does not satisfy a premium gate. Premium A permits keeping the current planning context and requires an optional locator-only handoff; the gate does not store model or session identity.
