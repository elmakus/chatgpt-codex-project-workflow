# M01-T01 Implementation Evidence — Fork release versioning

## Authority and baseline

- Card: `implementation/workstreams/issue-fork-release-versioning/cards/M01-T01.md`
- Approved plan: `planning/FORK_RELEASE_VERSIONING_MASTER_PLAN.md` / `FRV-P2`
- Requirements: `requirements/FORK_RELEASE_VERSIONING.md` R1 / FRV-REQ-001..012
- Decision: `decisions/ADR_FORK_RELEASE_VERSION_LINEAGE.md` / ADR-FRV-001
- Plan-review evidence: `planning/reviews/FRV-P2.md` GREEN
- Execution branch: `fix/fork-release-versioning`
- Integration target at Refresh Gate: `main@f3cdb60367da3e978397409b51c37faa181d613f`
- Refresh result: branch ahead of target and `behind_by: 0`; no target drift required reconciliation before implementation.

## JIT OpenSpec

The required OpenSpec was materialized before behavior-changing edits:

- `openspec/changes/fork-release-versioning/proposal.md`
- `openspec/changes/fork-release-versioning/specs/fork-release-versioning.md`
- `openspec/changes/fork-release-versioning/design.md`
- `openspec/changes/fork-release-versioning/tasks.md`

It formalizes applicability/baseline provenance, canonical private-lane construction, numeric per-baseline revision selection, migration/legacy behavior, mixed-tag resolver semantics, prerelease-quality independence, publication-route integration, and authorization/non-goal boundaries inside FRV R1 / ADR-FRV-001.

## Implemented subject

- Added canonical policy-neutral semantic source: `workflow/common/FORK_RELEASE_VERSIONING.md`.
- ChatGPT-only publication flow defers downstream fork release selection/validation to the common contract: `workflow/chatgpt_only/CLOSE.md`.
- Codex-only publication flow defers to the same common contract: `workflow/codex_only/CLOSE.md`.
- Legacy/shared publication flow defers to the same common contract: `workflow/REVIEW_AND_HANDOFF.md`.
- Added deterministic regression source: `tests/test_fork_release_versioning.py`.
- Added concise discoverability in `README.md`.
- `.github/workflows/auto-patch-tag.yml` remains outside this downstream-fork contract and is byte-for-byte the same branch blob as `main` (`360f2259a42ffccb6ad3cd1fd069a62cb44139b3`).

## Verification

Final verification is GREEN.

1. Executed the exact regression-test source in an isolated Python standard-library fixture run: **8 tests passed**.
2. Independently read the exact branch blobs through the repository connector and evaluated the regression assertions against those persisted artifacts: **25/25 checks passed**.
3. Covered behavior includes:
   - first canonical release `v5.0.8-private.1`;
   - numeric gap/multi-digit increment `private.2 + private.10 -> private.11`;
   - baseline reset to `v5.0.9-private.1`;
   - baseline isolation when another baseline has a higher private revision;
   - legacy/malformed tag exclusion;
   - exact upstream repo/tag/SHA provenance clauses;
   - explicit rejection of generic highest-SemVer;
   - legacy immutability;
   - GitHub prerelease-quality independence;
   - all three supported publication surfaces referencing the common contract without copying `max(N)+1`;
   - README canonical-contract pointer;
   - unchanged repo-local auto-patch tag workflow.
4. One pre-freeze check initially reported 24/25 because the common contract said “exact accepted upstream version/tag” instead of the R1 wording “exact upstream version/tag”. The contract was corrected before freeze and the full verification rerun GREEN.
5. Card-specific compare from approved-plan commit `969ac27aa3722492f8dd9ea62322a3a478c05a91` showed only Execution Prep / OpenSpec / common-contract / three publication integrations / regression / README / workstream-state files. No unrelated auto-patch workflow change is present.

## OpenSpec / implementation coherence

The OpenSpec and implemented common contract agree on:

- downstream-fork-only applicability;
- exact upstream repository/version-or-tag/SHA baseline provenance;
- `v<upstream>-private.<N>`;
- first `private.1`;
- numeric per-baseline `max(N)+1`;
- reset/isolation across accepted upstream baselines;
- preservation/exclusion of legacy tags;
- explicit non-max-SemVer mixed-lineage selection;
- independence of `private` lineage from GitHub prerelease quality;
- one common semantic source used by all three supported publication surfaces;
- no historical release mutation, upstream-sync policy, or new external-write authorization.

## External effects

No downstream tag/release/deployment mutation was performed. No historical release was rewritten or deleted. This Card only changes Project Workflow repository authority/tests/documentation on its workstream branch.

## Result

Implementation acceptance checks are GREEN. The Card is ready to freeze as an immutable implementation subject for its `RECOMMENDED` independent review gate.
