# M01-T01 — Initialize clean V2 target and construction custody baseline

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound control workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/PROJECT_WORKFLOW_V2_MASTER_PLAN.md` — §4 Construction control, custody and adoption; §5 M01, especially M01.P1
- Requirements: `requirements/PROJECT_WORKFLOW_V2.md` — PWV2-REQ-001, PWV2-REQ-017, PWV2-REQ-018, PWV2-REQ-051
- Accepted decisions: ADR-PWV2-001, ADR-PWV2-002, ADR-PWV2-003, ADR-PWV2-006
- Relevant OpenSpec: none
- Accepted dependency results: none
- Execution Prep guidance: `handoffs/EXECUTION_PREP_ENTRY_2026-09-22.md` — S2 items 12–14 only where applicable

### Must preserve

- `elmakus/chatgpt-codex-project-workflow` remains the sole live construction control/state owner until the later approved custody-transfer boundary.
- `elmakus/project_workflow_v2` is the clean V2 implementation destination, not a second live construction controller.
- Re-read target state before every initialization write. If another actor has populated the target, do not overwrite or guess ownership.
- The empty target receives only the narrowly required no-content initialization commit on intended default branch `main` before product/change-specific writes.
- After initialization, create and use target implementation branch `feat/pwv2-m01-foundation`; do not perform product development directly on `main`.
- Do not copy V1 history, policy trees, live Task Boards or mutable duplicate planning state into V2.
- Target repository remains private.
- Cross-repository operations are not atomic; persist exact refs/readback needed for recovery.

### Must not / rationale that must travel

- Do not create a V2-side Task Board for this construction obligation.
- Do not treat a control-repository commit as proof that target implementation exists.
- Do not enable production adoption, plugin replacement, project migration or custody transfer.
- Do not force-push or rewrite target published history.

## Dependencies

- none

## Outcome

A recoverable clean target baseline exists: an empty initialization commit on `main`, a dedicated `feat/pwv2-m01-foundation` implementation branch, and only the minimal M01.P1 product skeleton/authority provenance needed to begin construction.

## Scope

### Included

- Fresh readback proving whether the target is still empty.
- If empty, create the no-content initialization commit on `main`.
- Create `feat/pwv2-m01-foundation` from that exact root commit.
- On the implementation branch, establish only the minimal P1 baseline needed now: README/layout placeholders only where they provide current value, a minimal test command/CI skeleton if concretely executable, and exact immutable provenance pointers to the frozen Definition/approved P1/control workstream.
- Persist exact target main/root/implementation-branch readback in control-workstream evidence.

### Excluded

- Common workflow semantics/state/router implementation beyond the minimal directories needed by later Cards.
- Full plugin package implementation.
- Live plugin installation.
- M02+ lifecycle modules.
- Migration/adoption/cutover.
- Any target-side live construction state owner.

## Acceptance

- Immediately-before-write readback proves the target was empty, or the Card fails closed with durable evidence if it was not.
- Target `main` has exactly the intended initialization root commit before product writes.
- Target `feat/pwv2-m01-foundation` resolves to the intended construction branch.
- Product-specific baseline writes exist only on the implementation branch.
- No V1 policy/state tree or competing Task Board was copied into target.
- Control evidence records exact target refs/SHAs and readback.
- Target remains private.

## Required tests / checks

- GitHub repository/branch/commit readback before and after initialization.
- Branch ancestry check: implementation branch descends from exact initialization root.
- Target tree inspection proving absence of copied V1 policy/state/control artifacts.
- Verify no target-side `implementation/.../TASK_BOARD.yaml` exists for the construction obligation.
- Verify private repository visibility remains unchanged.

## Optional execution hints

- Priority: HIGH
- Complexity: MEDIUM
- Phase: `M01.P1`
- Expected/relevant code locations:
  - target repo root
  - target `workflow/`, `templates/`, `schemas/`, `tests/`, `tools/` only as minimal empty/current-value skeleton requires

## External write/readback needs

Target: `elmakus/project_workflow_v2`. Every Git ref/content mutation requires exact GitHub readback. Control evidence must identify target repository + exact SHAs/paths.

## Independent review

`none`. This Card establishes the mechanical baseline. Integrated semantic M01 acceptance is independently reviewed under `M01-T05`.

## Contract overrides

None.
