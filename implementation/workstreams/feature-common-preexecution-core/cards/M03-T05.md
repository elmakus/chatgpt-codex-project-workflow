# M03-T05 — Cumulative M03 acceptance and immutable review freeze

Milestone: `M03 — JIT execution, delegated results and common independent review`
Target implementation: `elmakus/project_workflow_v2@feat/pwv2-m03-execution-review`
Depends on: M03-T04
Independent review: RECOMMENDED

## Authority

- `PWV2-P1` complete M03 contract, checkpoint and §6 validation mapping.
- `requirements/PROJECT_WORKFLOW_V2.md` R1 M03-owned/supporting requirements.
- ADR-PWV2-004, ADR-PWV2-005 and ADR-PWV2-006.
- accepted M03-T01..T04 results/evidence.
- M02 cumulative acceptance/integration handoff.
- `implementation/workstreams/feature-common-preexecution-core/evidence/M03-pi-preexecution-research-reconciliation-2026-09-22.md`.

## Must preserve

- exact runtime-neutral Main/worker/reviewer boundary;
- one Project Card per selected workstream;
- semantic-only reviewer independence and append-only exact-subject history;
- no runtime role/model/session/adapter/invocation schema;
- no scheduler, Context Health or V1 policy tree;
- Pi remains a compatibility candidate only and Codex delivery acceptance remains unchanged.

## Included scope

- run cumulative deterministic M01+M02+M03 validation against one exact target commit;
- verify A04/A05/A07/A11/A12 plus M03 route combinations, stale subject, duplicate result, invalid independence and no-capability fixtures;
- verify internal worker concurrency is represented only inside one active Card with Main sole state owner;
- verify production tests exercise production parser/router/helpers;
- freeze exact immutable M03 implementation subject + acceptance evidence;
- activate fresh independent review for that subject.

## Excluded scope

- L08/L09 live topology acceptance (reserved for later full-system/live checkpoint);
- M04 integration/Close semantics beyond normal milestone publication by the controlling workflow;
- Pi runtime live qualification/package implementation;
- migration/adoption/custody transfer.

## Acceptance

- complete cumulative deterministic suite GREEN;
- exact target head/tree and acceptance surface recorded;
- no hidden blocker or unauthorized runtime-specific schema;
- automated N semantic traces are GREEN but are not relabeled as L08/L09 live PASS;
- independent review subject is frozen from immutable Git/evidence identity;
- Card remains non-terminal until fresh independent review GREEN, then deterministic post-review finalization/Close may integrate M03.

## Required verification

- `sh scripts/test.sh`;
- full unittest discovery and compile/diff checks as applicable;
- repository-layout/DROP assertions;
- exact GitHub Actions/PR/readback;
- fresh independent review.
