# M01-T02 — Prove isolated pw / project_workflow_v2 delivery feasibility

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound control workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/PROJECT_WORKFLOW_V2_MASTER_PLAN.md` — §2.1; §5 M01.P3; §7 first-installed-V2-probe trigger
- Requirements: `requirements/PROJECT_WORKFLOW_V2.md` — PWV2-REQ-008..014 as feasibility constraints; this Card does not claim final M05/L04/L05 acceptance
- Accepted decisions: ADR-PWV2-002, ADR-PWV2-006
- Relevant OpenSpec: none
- Accepted dependency results: exact GREEN `M01-T01` target baseline result
- Execution Prep guidance: `handoffs/EXECUTION_PREP_ENTRY_2026-09-22.md` — S2 items 2, 5, 12–14

### Must preserve

- Plugin namespace remains exactly `pw`.
- Target V2 Skill remains exactly `project_workflow_v2`.
- Probe exact invocation is `$pw:project_workflow_v2`.
- Codex ordinary workflow authority must be satisfiable from the installed package's local canonical `workflow/` bytes; no normal remote-policy fetch design is introduced.
- Skill/hook/plugin metadata stay bootstrap-only and must not become a second semantic policy tree.
- Use an isolated disposable installation/test environment. Do not replace or mutate the user's live `pw` installation/config/trust state.
- Distinguish exact package source/root/hash enough to prevent a stale V1 or cached alternate source from producing a false PASS.
- A platform result contradicting the accepted namespace/Skill contract creates bounded Research/Definition evidence; never silently rename the accepted interface.

### Must not / rationale that must travel

- Do not treat earlier V1 plugin evidence as V2 PASS.
- Do not build the full M05 delivery package to satisfy this feasibility probe.
- Do not add an updater, provider registry, runtime selector or duplicate policy.
- Do not weaken fail-closed behavior merely to make invocation succeed.

## Dependencies

- `M01-T01`

## Outcome

Current real Codex/plugin-host behavior is proven sufficient—or durably proven insufficient—for the exact accepted V2 namespace, Skill invocation, bundled workflow root and SessionStart/trust discovery assumptions before deep dependent construction proceeds.

## Scope

### Included

- Create only the minimum target-branch package/Skill/hook/router stub required to perform a valid feasibility probe.
- Isolated install/source selection.
- Exact invocation and installed-root discovery.
- SessionStart/hook/trust discovery required to establish feasibility.
- Source/cache disambiguation.
- Missing/broken bundled-router fail-closed check where the current host permits a safe isolated test.
- Durable control-workstream evidence with current host/tool version observations where useful.

### Excluded

- Full V2 semantic router/state lifecycle implementation.
- Full M05 package acceptance/update propagation.
- User live plugin replacement.
- Production deployment/adoption.
- Any change to accepted names.

## Acceptance

- Exact `$pw:project_workflow_v2` resolves from the intended isolated `pw` source.
- Readback identifies the intended installed package/root and bundled canonical workflow location, not an older V1/stale source.
- Required SessionStart/hook/trust discovery is viable for the planned thin bootstrap.
- Probe evidence clearly separates feasibility from later L04/L05 production acceptance.
- If any accepted platform contract is impossible, the Card does not fabricate success and routes through the plan's bounded Research/Definition mechanism.

## Required tests / checks

- Exact Skill listing/discovery plus exact invocation.
- Installed package source/root readback.
- Relevant hook/SessionStart/trust discovery/readback.
- Cache/source collision check against any other available `pw` source.
- Safe negative/fail-closed missing-router observation where possible.
- Verify user's live plugin/config state is unchanged.

## Optional execution hints

- Priority: HIGH
- Complexity: MEDIUM
- Phase: `M01.P3 feasibility`
- Expected/relevant code locations:
  - target `.codex-plugin/plugin.json`
  - target `.agents/plugins/marketplace.json`
  - target `skills/project_workflow_v2/SKILL.md`
  - target `hooks/hooks.json`
  - target minimal `hooks/session-start.py`
  - target minimal `workflow/ROUTER.md` stub only as necessary for the probe

## External write/readback needs

Target branch `elmakus/project_workflow_v2:feat/pwv2-m01-foundation` plus an isolated Codex/plugin test environment. Persist target Git readback and isolated installed-source/root evidence. Do not mutate live user plugin state.

## Independent review

`none`. The probe is empirical gate evidence; integrated final M01 subject/acceptance is independently reviewed under `M01-T05`.

## Contract overrides

A failed exact accepted namespace/Skill/platform contract is not an ordinary implementation bug to work around by renaming; follow P1 Research/Definition escalation.
