# M07-T03 — Full ordinary model-backed Codex L04 qualification

Milestone: `M07 — System qualification and first production-ready adoption`
Depends on: `M07-T02`
Exact candidate: `elmakus/project_workflow_v2@15978113e46abc8498ceaef594461c8613fcadb8`
Review requirement: none (covered by M07 cumulative/final integration review)

## Authority
- `planning/PROJECT_WORKFLOW_V2_MASTER_PLAN.md` M07.P2 and §6.2 L04.
- M07-T01 exact candidate qualification.
- M05-T02/T03/T04 installed-package and delivery evidence.

## Outcome
Complete the still-missing ordinary model-backed L04 semantic continuation on the real Codex CLI/package surface, against the exact M07 candidate, without modifying the live/production Codex installation or any production consumer project.

## Included scope
- create/use a fresh disposable `CODEX_HOME` and disposable V2 consumer under `/tmp`;
- install or otherwise resolve the exact immutable M07 package subject;
- invoke exact `$pw:project_workflow_v2` with a normal model-backed `codex exec`;
- verify the Skill resolves to the exact installed candidate and the agent follows bundled local `workflow/ROUTER.md` progressively into the fixture's current durable obligation;
- verify no remote workflow-policy fetch, V1 semantic fallback or production-project mutation is used;
- record exact runtime/version, package source, prompt/result and any concrete runtime/access blocker.

## Excluded scope
- production plugin installation/update;
- L06/L07/L08/L09;
- migration/adoption/custody transfer;
- changing workflow semantics to make the live test pass.

## Acceptance
GREEN only if one ordinary model-backed invocation on the real Codex runtime completes far enough to demonstrate actual V2 semantic continuation from the disposable project's durable state using the exact candidate's bundled local authority. Existing M05 package-resolution/missing-router/no-V1 evidence may be reused only where unchanged-compatible.

If account/runtime/network/sandbox capability prevents that completion, persist the exact blocker and leave L04 non-GREEN; do not replace it with a simulator or older-package result.

## Required readback
- exact candidate/package identity;
- disposable consumer state before/after;
- ordinary model-backed command/result;
- local bundled router/Skill source;
- no live installation or production consumer mutation;
- compatibility with retained M05 delivery evidence.
