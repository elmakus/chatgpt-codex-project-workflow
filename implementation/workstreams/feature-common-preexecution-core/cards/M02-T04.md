# M02-T04 — GitHub Issue tracker recovery and correlation

Milestone: `M02 — Intake through Definition and premium planning`
Target implementation: `elmakus/project_workflow_v2@feat/pwv2-m02-intake-definition-planning`
Depends on: M02-T03
Review requirement: none (covered by cumulative M02 acceptance/review in M02-T05)

## Authority

- `PWV2-P1` M02.P4.
- `requirements/PROJECT_WORKFLOW_V2.md` R1: PWV2-REQ-032..033, 053..060.
- `decisions/ADR_PROJECT_WORKFLOW_V2_MANAGED_CHANGE_LIFECYCLE.md`.
- accepted M02-T01..T03 results.
- S2 guidance item 11: resolve current GitHub tracker mechanics JIT.

## Included scope

Implement `workflow/GITHUB_ISSUES.md` plus minimal correlation state/templates/tests:
- exact existing Issue/workstream discovery and dedup before create;
- durable repository + Issue identity correlated to the workstream;
- tracker remains bookkeeping, never requirements/authorization/execution authority;
- connector/capability absence is explicit and cannot silently authorize or duplicate work;
- write -> readback -> expected-state verification -> evidence contract for tracker create/update;
- interruption recovery reconnects existing tracker rather than creating a duplicate;
- ambiguous duplicate matches fail closed to bounded Recovery;
- preserve final-PR linkage fields needed later by M04 without implementing Close/closing behavior now.

No custom bot/server and no generic GitHub abstraction layer.

## Excluded scope

- actual final tracker closure semantics (M04);
- production adoption;
- unrelated GitHub automation.

## Acceptance

- deterministic fixtures for found-existing, create-needed, capability-absent, ambiguous duplicate and interrupted-after-create cases;
- tracker state cannot authorize repair;
- exact repo/Issue/workstream reference survives restart;
- M01/T01..T03 cumulative suite remains GREEN.

## Required verification

- production helper/fixture tests;
- current GitHub connector syntax/readback evidence where a harmless bounded test is possible without creating project-noise;
- `sh scripts/test.sh`;
- exact target commit readback.
