# Project Workflow V2 — Execution Prep Entry

Date: 2026-09-22
Workstream: `feature-common-preexecution-core`
Control branch: `feat/common-preexecution-core`
Approved plan: `PWV2-P1`
Reviewed plan blob: `0d55b03e2a4d1a2b1a4fd9972f8365f693262503`
Prior stop: `premium_stop_C`
Current obligation: `execution_prep:M01`
Execution policy: `chatgpt_only`
Implementation repository: `elmakus/project_workflow_v2`

## Planning-experiment reconciliation

The isolated meta-synthesis completed with outcome **KEEP_P1**.

Exact experiment result:
- path: `planning/experiments/synthesis/PWV2_META_SYNTHESIS_REPORT.md`
- experiment branch: `exp/v2-plan-meta-synthesis`
- report blob: `43716d66868200ae8883369a5092d7b39e3a88af`

No S1 strategic delta exists. `PWV2-P1` remains canonical. No `PWV2-P2` and no new premium Planning/Review cycle are authorized.

The experiment report is guidance evidence, not replacement plan authority. The frozen Definition plus approved `PWV2-P1` continue to outrank it.

## Execution Prep Guidance Pack — S2 only

Carry these refinements only inside already-approved P1 authority:

1. Decompose broad milestone packages by concern; do not create milestone-sized mega-Cards.
2. Make the early plugin probe evidentiary: isolated source, exact `$pw:project_workflow_v2`, package root, hook/trust discovery, cache/source disambiguation and fail-closed proof.
3. Before later custody transfer, materialize a finite takeover/recovery transition table; do not change the P1 one-live-owner/no-dual-write strategy.
4. Build milestone tests as one incrementally reusable cumulative conformance suite.
5. Bind deterministic tests to actual production parser/module/helper semantics, not a parallel test-only interpreter.
6. Choose minimal exact content+acceptance review-subject identity and target-race checks when the real Git/state representation exists.
7. Make terminal recovery refs demonstrably reachable for supported integration methods when M04 becomes current.
8. Make migration a finite, dry-run/readback/replay-safe transformation for real supported V1 families when M06 becomes current.
9. Use the smallest expected-state/revision guard needed to reject stale shared-state writes; do not add global locks/runtime IDs/scheduler state.
10. Encode premium/review recovery transitions explicitly in deterministic fixtures.
11. Resolve GitHub tracker query/correlation syntax JIT against the current API/connector.
12. Keep schemas minimal and owner-driven; add fields only for concrete current transitions/tests.
13. Maintain a small active JIT/deferred-decision checklist from P1 §7 rather than speculative placeholder artifacts.
14. Before freezing a major Card set, challenge whether any proposed decomposition changes strategy, gates or product intent; if not, keep it in Execution Prep/JIT.
15. Keep fork behavior trigger-only and do not instantiate it without a real downstream-fork release trigger.

## Current target readback

At Execution Prep entry, `elmakus/project_workflow_v2` is private, default branch name is `main`, and GitHub reports no branches and no commits. Re-read this immediately before initialization; this observation is not permission to overwrite a newly populated target.

## Construction authorization boundary

The current user continuation authorizes entering Execution Prep and executing the approved construction work in `elmakus/project_workflow_v2`.

It does **not** authorize:
- production adoption/cutover;
- migration of consumer projects;
- mass rollout;
- dual control state;
- changing accepted Definition/P1 strategy.

Until the P1 custody-transfer boundary, this control-repository workstream remains the sole live construction tracker. The target repository must not receive a competing Project Workflow Task Board for this construction obligation.

## Initial M01 decomposition

Materialize only currently knowable M01 work:

`M01-T01` target initialization + construction custody baseline  
→ `M01-T02` isolated plugin/Skill feasibility probe  
→ `M01-T03` minimal durable authority/state envelope  
→ `M01-T04` small router + progressive-disclosure boundary  
→ `M01-T05` cumulative deterministic M01 validation + integrated M01 acceptance

No M02+ implementation Cards are created yet. Their exact contracts remain JIT until M01 evidence exists.

This handoff is an Execution Prep recovery aid. Exact Definition, approved P1 and Card authority refs outrank this summary.
