# M01-T04 — small obligation router and progressive-disclosure evidence

Date: 2026-09-22
Card: `M01-T04`
Result: **GREEN**
Target: `elmakus/project_workflow_v2@feat/pwv2-m01-foundation`
Target result commit: `f1f4ed87875877529da6dc954e785d6373de7930`
Dependency result: `24bf91ce25f4a06a59b1cc674078094e69f57a35`

## Implemented bounded router

The target now contains the M01 common-router foundation:

- `workflow/ROUTER.md` is the runtime-neutral bootstrap/read-set contract and preserves the existing M01 package probe sentinel.
- `workflow/RECOVERY.md` is an identity/binding recovery boundary only, not a full Recovery lifecycle.
- `tools/router.py` is the production M01 obligation selector and imports the same `tools/state_contract.py` validators used by T03.
- disposable router fixtures prove exact project/workstream/Task Board/Card selection and progressive disclosure.
- `scripts/test-router.sh` is wired into cumulative `scripts/test.sh`.

The selector reads only router → PROJECT → exactly selected manifest → its exact Task Board → current Card contract when required to classify the current obligation. It does not preload templates, migration material, unrelated evidence or untrusted Issue text.

## Routing semantics proven

The production selector and tests prove:

- equivalent valid durable state selects the same semantic route under different runtime/model/session environment noise;
- new managed intent identifies Intake but returns explicit `unavailable` because Intake is not implemented until M02, without reading an unrelated active board;
- an identified active/ready execution obligation returns explicit `unavailable` because full Execution semantics are not implemented until M03;
- zero/multiple selected workstreams, invalid branch binding, cross-workstream locator and path escape route to the M01 Recovery boundary;
- legacy `execution_policy` is rejected through the production T03 validator rather than selecting policy;
- unrelated migration material, templates and untrusted Issue text are absent from the positive read-set;
- the approved precedence and real-stop sets are represented as runtime-neutral M01 foundations without implementing M02/M03 transition machinery;
- an unimplemented route is not misclassified as a real user/premium stop.

No generic DAG/orchestration engine, runtime-specific policy tree, Context Health/FRESH state, M02+ lifecycle implementation, production adoption or custody transfer was introduced.

## Tests

Executed on the exact target working tree before commit:

`sh scripts/test.sh`

Result:

- M01-T02 package probe checks: PASS;
- T03 state-envelope suite: 8/8 PASS;
- production state-envelope CLI validation: PASS;
- T04 router suite: 8/8 PASS;
- router CLI smoke: PASS;
- M01 baseline checks: PASS;
- Python compile check: PASS;
- `git diff --check`: PASS.

The first cumulative attempt correctly failed at the preserved T02 probe because the rewritten router no longer contained the literal fail-closed marker. The router wording was corrected without changing semantics, and the complete suite then passed.

## External readback

GitHub readback confirms target commit `f1f4ed87875877529da6dc954e785d6373de7930` is exactly one commit ahead of T03 and changes only the 14 bounded T04 files. Immediate combined-status readback returned no status records, so no CI PASS is claimed.
