# M05-T01 — Run final regression, architecture audit and integration-readiness verification

- Milestone: `M05`

> Stable Task Card contract. Mutable execution/review/result state lives only in the manifest-selected workstream Task Board.

## Authority slice

- Master Plan: `planning/CODEX_ONLY_MASTER_PLAN.md#M05--end-to-end-regression-architecture-audit-and-publication-readiness`
- Requirements: `requirements/CODEX_ONLY_POLICY.md` — CO-REQ-001..028.
- Accepted decisions:
  - `decisions/ADR_CODEX_ONLY_DEDICATED_NAMESPACE.md`
  - `decisions/ADR_CODEX_ONLY_RUNTIME_BOUNDARY.md`
  - `decisions/ADR_CODEX_ONLY_BOUNDED_PARALLEL_CARDS.md`
- Accepted dependency result: M04 GREEN checkpoint `48a56cbd5554b1378fa3727449bfce94e2f7f1df`, acceptance `implementation/workstreams/feature-codex-only-policy/evidence/M04-acceptance.md`.
- Current-target compatibility evidence: `implementation/workstreams/feature-codex-only-policy/evidence/M04-current-main-refresh.md`.
- Relevant OpenSpec: M02, M03 and M04 Codex-only changes as referenced by their accepted Cards/evidence.

### Must preserve

- This repository remains `execution_policy: chatgpt_only`.
- `chatgpt_only` remains isolated and behaviorally intact; non-migrated accepted policies remain on legacy routing.
- `codex_only` uses only genuinely common contracts plus `workflow/codex_only/*` for migrated semantics.
- Project Workflow owns durable project authority/state; `codex_workflow` owns concrete worker/session/model/profile/invocation/wait/resume/concurrency mechanics.
- Formal review remains immutable-subject and role/behavior based; Tester cannot repair production; no mandatory second normal-ChatGPT review follows a qualifying Codex-managed verdict.
- Serial execution remains valid by default; bounded concurrency requires current-state JIT proof; Codex Main remains sole shared Task Board/integration-state writer.
- Default and branch-isolated Task Board recovery, stacked workstreams, target refresh, terminal target-side package recovery and source-branch deletion safety remain valid.
- Historical M01–M04 review/evidence lineage must not be rewritten.

### Must not / rationale that must travel

- Do not use this verification Card to hide behavioral corrections. If a regression or architecture audit finds a material defect, persist the finding and create/reopen bounded corrective work through the router.
- Do not merge the workstream to `main` or issue the manifest final-integration verdict inside this Card. Final target refresh, exact integrated-subject freeze/coverage and integration belong to normal Close semantics.
- Do not claim `codex_workflow` runtime internals were exercised unless an actual runtime integration test is performed.

## Dependencies

- `M04-T01` done and M04 milestone GREEN.

## Outcome

The complete feature has reproducible end-to-end/static/semantic regression evidence, an architecture/coherence audit against CO-R1 + accepted ADRs, migration/publication notes, and exact current-target integration-readiness findings sufficient for normal Close to perform final refresh/review/integration.

## Scope

### Included

- cross-policy routing and namespace/static regression;
- end-to-end Codex-only lifecycle scenario matrix;
- M02 RED/repair/new-subject/GREEN review semantics, reviewer-reuse/replacement transparency and runtime-identity separation checks;
- M03 serial/parallel positive and negative safety/recovery scenarios;
- default-board + branch-isolated/stacked/terminal recovery checks;
- full CO-REQ-001..028 architecture/coherence audit;
- legacy-property disposition completeness;
- documentation/migration/publication-readiness notes;
- current `main` comparison, including semantic merged-result inspection and explicit root `PROJECT.md` reconciliation requirement for Close;
- conflict/whitespace/schema/import checks.

### Excluded

- final merge/integration to `main`;
- manifest final-integration review verdict;
- source-branch deletion;
- unrelated cleanup or migration of other policies;
- behavioral correction discovered by this verification Card (must become explicit corrective work).

## Acceptance

- all M05 Master Plan acceptance points have exact reproducible evidence;
- all CO-REQ-001..028 are mapped to verified behavior/contracts with no unexplained gap;
- serial lifecycle, formal review, bounded-parallel safety and recovery scenarios are coherent;
- `workflow/chatgpt_only/*` non-regression and legacy-route preservation are verified against current `main`;
- no forbidden Project Workflow runtime ownership schema/path is present;
- legacy behavior inventory has no unexplained loss;
- current-target semantic integration risks are explicitly classified and bounded for Close;
- root `PROJECT.md` still declares `execution_policy: chatgpt_only`;
- required docs/evidence are durable and conflict/whitespace checks are GREEN.

## Required tests / checks

- exact Git comparison of workstream vs current `main` and merge base;
- route/static import scans across root and `workflow/codex_only/*`;
- filename/lifecycle-owner parity against current-main `workflow/chatgpt_only/*`;
- scenario inspection for Intake → exploration/Research → promotion → Definition → Planning/plan review → Execution/Review/Recovery → Close;
- issue micro-fix and workstream final-review path;
- formal review RED/repair/recheck and runtime-decoupled recovery;
- M03 safe/unsafe/resource-conflicting concurrency and launched-batch recovery semantics;
- terminal namespaced package / branch-deletion recovery semantics;
- CO-REQ/ADR trace audit;
- runtime-only identifier/scheduler schema scan;
- conflict-marker and trailing-whitespace checks for the feature-owned final change range;
- semantic trial-integration inspection against current `main`, without performing final integration.

## External write/readback needs

GitHub repository evidence/documentation and Task Board state only. No deployment/live-system write. Final merge to `main` is excluded and remains Close-owned.

## Independent review

`none` for this verification/readiness Card. The intake-created feature workstream already has a distinct manifest-owned `RECOMMENDED` final-integration review gate; Close must refresh the current target and either freeze that exact final integrated subject for fresh independent review or prove exact stronger coverage. Any behavioral correction discovered here must be materialized separately and classified for review on its own subject.
