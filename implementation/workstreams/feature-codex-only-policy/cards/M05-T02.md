# M05-T02 — Remove contradictory intra-workstream concurrency prohibition

- Milestone: `M05`

> Stable corrective Task Card created from the workstream final-integration RED. Mutable execution/result state lives only in the selected workstream Task Board.

## Authority slice

- Final-integration RED: `implementation/workstreams/feature-codex-only-policy/evidence/M05-final-integration-review-01.md`
- Requirements: `requirements/CODEX_ONLY_POLICY.md` — CO-REQ-017..023, with CO-REQ-005/006/024/028 as preservation constraints
- Accepted decision: `decisions/ADR_CODEX_ONLY_BOUNDED_PARALLEL_CARDS.md`
- Accepted runtime boundary: `decisions/ADR_CODEX_ONLY_RUNTIME_BOUNDARY.md`
- Approved plan: `planning/CODEX_ONLY_MASTER_PLAN.md` — M03 bounded-parallel semantics, M04 lifecycle integration and M05 final regression/readiness
- Existing accepted M03/M04/M05 evidence remains predecessor evidence; this Card repairs only the contradictory Workstreams wording found by final-integration review.

## Must preserve

- serial execution remains the default inside one codex_only workstream;
- bounded intra-workstream concurrency is legal only through the explicit M03 current-state/JIT-proven batch contract;
- independent workstreams may execute concurrently when their own isolation/gates permit it;
- Codex Main remains sole shared Task Board/integration-state writer;
- no generic/global mutable scheduler is introduced;
- all M02 review/runtime-boundary, M03 batch/recovery, stacked-workstream, target-refresh and terminal-recovery semantics remain unchanged;
- `workflow/chatgpt_only/*` and this repository's own `execution_policy: chatgpt_only` remain unchanged.

## Outcome

`workflow/codex_only/WORKSTREAMS.md` no longer contains an absolute rule forbidding concurrency between Cards inside one workstream. Its Core model states serial-by-default execution plus the explicit bounded M03 exception coherently with CO-REQ-017 and ADR-CODEX-PAR-001.

## Included scope

- one normative sentence in `workflow/codex_only/WORKSTREAMS.md`;
- corrective verification/evidence and workstream-owned state needed to close this RED.

## Excluded scope

- no changes to M03 batch algorithms, schemas, ready-set selection or recovery mechanics;
- no changes to root routing, `workflow/chatgpt_only/*`, legacy routing, requirements, ADRs or Master Plan;
- no unrelated wording cleanup.

## Acceptance

1. The contradictory absolute sentence from final-integration RED is absent.
2. Workstreams Core model explicitly says intra-workstream execution is serial by default and concurrent Cards are legal only through the bounded M03 batch exception.
3. The corrected wording remains consistent with `STATE.md`, `EXECUTION_PREP.md`, `EXECUTION.md`, `RECOVERY.md`, CO-REQ-017..023 and ADR-CODEX-PAR-001.
4. No other production/workflow behavior is changed by the corrective implementation.
5. Current target compatibility remains GREEN and root `PROJECT.md` remains target-owned with `execution_policy: chatgpt_only`.
6. The old RED evidence remains durable and the corrected exact whole-workstream subject is frozen as a new manifest-owned `pending` final-integration review after Close refresh.

## Required checks

- exact readback of corrected `WORKSTREAMS.md` Core model;
- scan all 22 `workflow/codex_only/` owner files for the stale absolute prohibition and obvious ChatGPT-only execution wording used in the RED diagnosis;
- verify the correction diff touches only the intended production sentence plus durable workstream Card/state/evidence bookkeeping;
- verify current `main` still equals the target baseline or repeat the Close refresh if it moved;
- verify root `PROJECT.md` on the corrected feature subject equals current target and still declares `execution_policy: chatgpt_only`.

## Independent review

`none` for this corrective Card. The distinct manifest-owned `RECOMMENDED` final-integration gate remains mandatory and will independently review the corrected whole-workstream subject plus M01-M05 acceptance surface; this Card does not create a redundant narrower review gate.
