# M01-T02 — Establish codex_only namespace foundation and static boundary checks

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the manifest-selected workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/CODEX_ONLY_MASTER_PLAN.md#M01--dedicated-namespace-foundation-and-semantic-parity-inventory`
- Requirements: `requirements/CODEX_ONLY_POLICY.md` — `CO-REQ-001..006`, `CO-REQ-026..028`
- Accepted decisions:
  - `decisions/ADR_CODEX_ONLY_DEDICATED_NAMESPACE.md`
  - `decisions/ADR_CODEX_ONLY_RUNTIME_BOUNDARY.md`
  - `decisions/ADR_CODEX_ONLY_BOUNDED_PARALLEL_CARDS.md`
- Accepted predecessor evidence:
  - GREEN `M01-T01` subject `ad1cf4626816a83ca2d3ed47bbd0b3344154014b`
  - `docs/audits/CODEX_ONLY_M01_BASELINE_MATRIX.md`
  - `implementation/workstreams/feature-codex-only-policy/evidence/M01-T01-review-01.md`
- Relevant OpenSpec: `none` — this Card establishes a not-yet-routed policy namespace foundation; M02/M03 behavior/state contracts remain JIT work.

### Must preserve

- `workflow/chatgpt_only/` remains untouched and is the semantic lifecycle reference;
- migrated `codex_only` semantics are policy-local and do not import the legacy/shared execution core or another policy namespace;
- controlled duplication is allowed; do not extract a new shared fixed-policy execution core;
- root `workflow/CONTEXT_ROUTING.md` remains unchanged and MUST NOT route to the incomplete namespace in M01;
- Project Workflow owns durable project authority/state while `codex_workflow` owns worker/session/model/profile/invocation/wait/resume/concurrency mechanics;
- no required Project Workflow schema may contain runtime-only worker/session identifiers;
- serial behavior remains the safe foundation until M03 defines bounded-parallel state/JIT eligibility;
- exact formal-review provenance/state mechanics are deferred to M02 rather than invented here;
- this repository remains `execution_policy: chatgpt_only`.

## Dependencies

- `M01-T01` is terminal GREEN and its matrix/review evidence is the accepted migration baseline.

## Outcome

A complete but deliberately not-yet-routed `workflow/codex_only/` namespace ownership surface exists for all 22 lifecycle modules/templates mapped by M01-T01. Low-delta lifecycle ownership is established, Codex-specific modules record the accepted ownership/delta boundaries, M02/M03 deferred semantics are explicit, and static checks prove namespace completeness/isolation without activating root routing.

## Scope

### Included

- create exactly the 22 `workflow/codex_only/` files mapped from current `workflow/chatgpt_only/`;
- establish policy-local ownership for Intake, workstreams, Brainstorming, Research, Definition, Planning, plan review, Execution Prep/JIT, Task Cards, execution, review, state, recovery, repository, micro-fix, Close and policy templates;
- preserve already-known lifecycle invariants and adapt actor/boundary wording where deterministic;
- make M02 ownership explicit for formal review/state/recovery/runtime-boundary details;
- make M03 ownership explicit for bounded-parallel Card metadata, JIT eligibility, lane/worktree and Main-owned integration semantics;
- keep serial/default-safe behavior where the later parallel schema is intentionally not final;
- add static verification sufficient to prove 22/22 target ownership, no policy-specific cross-imports, no legacy shared execution dependency, no forbidden runtime identity fields as required schema, no root routing cutover, and unchanged root project execution policy.

### Excluded

- editing `workflow/CONTEXT_ROUTING.md` or activating `codex_only` routing;
- editing `workflow/chatgpt_only/*`;
- deleting or rewriting legacy/shared contracts;
- finalizing M02 formal-review provenance/state/recovery schema;
- finalizing M03 bounded-parallel Card/Task Board/lane schema or compatible-ready-set algorithm;
- implementing runtime worker/session/model/profile/invocation mechanics;
- changing this repository's `execution_policy`;
- M04 final lifecycle reconciliation/cutover and M05 end-to-end audit.

## Acceptance

- all 22 M01-T01 matrix target paths exist under `workflow/codex_only/`, with no missing or extra parity owner;
- every target file is policy-local and does not import `workflow/chatgpt_only/*`, `workflow/codex/*`, `workflow/legacy/*`, shared `workflow/EXECUTION.md`, or shared `workflow/contracts/*` as runtime authority;
- the namespace states the Project Workflow / `codex_workflow` ownership boundary consistently and contains no required project-state field for runtime-only session/invocation/model/profile/lease identity;
- review/state/recovery foundation preserves immutable exact subjects, reviewer non-repair and durable verdict/evidence while explicitly deferring exact M02 provenance mechanics;
- execution/prep/task-card/workstream/repository foundation preserves serial default and Main-owned shared state while explicitly deferring exact M03 bounded-parallel schema/JIT algorithm;
- root `workflow/CONTEXT_ROUTING.md` is byte-identical to current `main`;
- root `PROJECT.md` still declares `execution_policy: chatgpt_only`;
- no existing `workflow/chatgpt_only/*` file is modified by the Card;
- static checks and conflict-marker/whitespace checks are GREEN.

## Required tests / checks

- exact set comparison: M01-T01 matrix target paths vs `workflow/codex_only/` tree;
- scan `workflow/codex_only/` for forbidden active imports/references to another policy namespace or legacy/shared execution contracts;
- scan required state/template fields for forbidden runtime-only identity keys;
- compare root `workflow/CONTEXT_ROUTING.md` and `workflow/chatgpt_only/` against `main`;
- verify `PROJECT.md` execution policy remains `chatgpt_only`;
- verify M02/M03 deferred semantics are explicitly located rather than silently omitted;
- `git diff --check` plus conflict-marker scan.

## External write/readback needs

none beyond durable GitHub writes on `feat/codex-only-policy`.

## Independent review

`RECOMMENDED` — this Card creates the policy-local contract surface consumed by M02/M03/M04; an independent architecture/coherence check is required before M01 can close.

## Contract overrides

none
