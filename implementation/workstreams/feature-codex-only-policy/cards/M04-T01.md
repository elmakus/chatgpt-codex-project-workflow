# M04-T01 — Complete codex_only lifecycle integration and root routing cutover

- Milestone: `M04`

> Stable Task Card contract. Mutable execution/review/result state lives only in the manifest-selected workstream Task Board.

## Authority slice

- Master Plan: `planning/CODEX_ONLY_MASTER_PLAN.md#M04--full-lifecycle-integration-routing-cutover-and-compatibility`
- Requirements: `requirements/CODEX_ONLY_POLICY.md` — CO-REQ-001..006, CO-REQ-024, CO-REQ-026..028, plus lifecycle integration of CO-REQ-007..025.
- Accepted decisions:
  - `decisions/ADR_CODEX_ONLY_DEDICATED_NAMESPACE.md`
  - `decisions/ADR_CODEX_ONLY_RUNTIME_BOUNDARY.md`
  - `decisions/ADR_CODEX_ONLY_BOUNDED_PARALLEL_CARDS.md`
- Dependency: M03 GREEN checkpoint `1fbb601461604fe07151478e017cb94bf60de47b`.
- Lifecycle reference: current `main` `workflow/chatgpt_only/*`, used as semantic reference only.
- OpenSpec: `openspec/changes/codex-only-m04-lifecycle-cutover/`.

## Must preserve

- `chatgpt_only` routing and policy-local files remain unchanged.
- Non-migrated accepted policies continue through `workflow/legacy/CONTEXT_ROUTING.md`.
- This repository itself remains `execution_policy: chatgpt_only`.
- `codex_only` uses only genuinely common contracts plus `workflow/codex_only/*` for migrated semantics; it must not import another policy or the legacy shared execution core.
- Codex Main remains the sole Project Workflow shared-state/integration writer; `codex_workflow` owns concrete worker/session/model/profile/invocation/wait/resume/concurrency mechanics.
- M02 immutable review attempts, Tester non-repair and role/subject independence remain intact.
- M03 serial-default bounded parallelism, frozen-batch integration and recovery invariants remain intact.
- Default Task Board and branch-isolated manifest-selected Task Board modes remain valid.
- Intake, exploratory promotion, Research return, Definition, Planning/plan review, micro-fix, stacked workstreams, target refresh, final-integration review, Close and automatic continuation remain repository-recoverable.

## Outcome

A project with `execution_policy: codex_only` can enter through root routing and proceed through the complete project lifecycle using the dedicated policy namespace, while existing `chatgpt_only` and non-migrated-policy routes remain isolated.

## Scope

### Included

- reconcile policy-local lifecycle modules still carrying M01/M02/M03 deferred/foundation wording;
- complete Codex-only Intake, Brainstorming, Research, Definition, Planning, micro-fix, workstream/default-board selection, stacked-workstream/target-refresh/final-review and Close semantics;
- reconcile Router/Repository/Context Health with the complete lifecycle while preserving M02/M03 execution/review/batch semantics;
- activate explicit root `codex_only` routing;
- update manifest template and lifecycle/cutover audit;
- OpenSpec and static/semantic compatibility verification.

### Excluded

- changing this repository's own execution policy;
- migrating any other legacy policy;
- changing `workflow/chatgpt_only/*` behavior;
- moving runtime worker identity/lifecycle into Project Workflow;
- replacing bounded parallelism with a generic scheduler;
- final M05 end-to-end regression/publication readiness and final workstream integration to `main`.

## Acceptance

- root router has explicit, isolated `codex_only -> workflow/codex_only/ROUTER.md` selection;
- the 22-file dedicated namespace contains no deferred pre-M04 activation/foundation gaps needed by a live lifecycle;
- codex_only lifecycle covers Intake through Close, including explicit exploratory promotion, Research return ownership, plan review, micro-fix, default/workstream boards, stacking, target refresh, final-integration review and terminal package recovery;
- formal review remains role/subject based with Tester non-repair and no mandatory second normal-ChatGPT review;
- M03 batch/recovery rules remain coherent under Close/target refresh;
- no active codex_only execution path imports `workflow/chatgpt_only/*`, `workflow/codex/*`, `workflow/legacy/*` or `workflow/contracts/*`;
- `workflow/chatgpt_only/*` is unchanged against current `main`;
- non-migrated policies still route to legacy;
- root `PROJECT.md` remains `execution_policy: chatgpt_only`;
- OpenSpec/audit/static scans agree and conflict/whitespace checks are GREEN.

## Required checks

- route `chatgpt_only`, `codex_only`, and another accepted policy through the root dispatcher;
- trace feature Intake -> Brainstorming/Research -> explicit promotion -> Definition -> Planning -> plan review -> Execution Prep/Execution/Review -> Close;
- trace issue micro-fix -> Card review -> target refresh -> manifest final-integration gate -> integration finalization;
- trace independent and stacked workstream integration refresh, including target movement and exact review-subject preservation/invalidation;
- verify default-board and manifest-selected board recovery;
- verify existing M02/M03 review/batch invariants still hold;
- scan forbidden cross-policy/legacy execution dependencies and runtime-identity schema;
- compare protected `workflow/chatgpt_only/*` with current `main`;
- verify root project policy;
- conflict-marker/trailing-whitespace checks.

## Parallel safety

Serial only for this Card. Root cutover and lifecycle contracts form one coupled review subject.

## Independent review

`RECOMMENDED` — root routing and full lifecycle behavior change materially.

## External write/readback

GitHub repository writes only; no deployment/live-system write.
