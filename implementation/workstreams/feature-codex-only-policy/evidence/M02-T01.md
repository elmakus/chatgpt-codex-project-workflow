# M02-T01 Implementation Evidence

Card: `M02-T01`
Implementation subject: `b901d1cfa5ca26074b363b0c8980f7a7aaa223f1`
Prior reviewed subject: `af08ef755cb846118c9dbe1a6edf7f5f139cf0e8` — independent review Attempt 1 RED
Execution-start base: `10ae410efa05faa24b61417d59c399a2d5a3cb58`
JIT OpenSpec frozen through: `32c345c7eb8f44843a067b74003304e18df7a441`
Workflow-main baseline verified: `6b0445256b417f82431fb7b2704f56691eb4e7ae`

## Achieved state

- Finalized codex_only M02 project-level formal-review state around semantic `implementation_owner_role` plus append-only review attempts.
- Repaired the Attempt-1 RED gap for milestone-owned review: reviewable milestone/checkpoint subjects now carry aggregate `implementation_owner_role: executor`, Tester independence covers every concrete Executor realization contributing to that checkpoint, and milestone RED routes through Execution Prep to exact bounded corrective Card(s) rather than inferring a runtime worker.
- Each attempt binds one immutable exact subject to `pending | in_progress | green | red`, semantic `reviewer_role: tester`, and durable evidence.
- Prior RED/GREEN attempts remain directly addressable when a corrected subject becomes current.
- Codex Main is the sole shared Project Workflow state writer; Executor owns production implementation/repair and Tester owns independent judgment only.
- RED returns through Main to the owning Executor role. Corrected production is a new subject/attempt and receives a full independent recheck.
- Same logical Tester reuse and fail-closed replacement are runtime-owned and do not alter Project Workflow attempt identity for an unchanged subject.
- Recovery handles lost runtime state and partial Main writes from durable repository facts without requiring worker/session identity.
- Codex-managed formal review satisfies the project gate without a second mandatory normal-ChatGPT review.
- Plan-review independence was adapted to the same role/subject boundary without storing runtime identity.
- M03 parallel schema/algorithm remains explicitly deferred.

## JIT OpenSpec

Created and completed:

- `openspec/changes/codex-only-m02-formal-review-state/proposal.md`
- `openspec/changes/codex-only-m02-formal-review-state/specs/formal-review-state.md`
- `openspec/changes/codex-only-m02-formal-review-state/design.md`
- `openspec/changes/codex-only-m02-formal-review-state/tasks.md`

The technical contract fixes the review-attempt representation, RED/repair/recheck sequence, runtime replacement transparency and recovery invariants before root activation.

## Verification

Exact subject verification was run after the OpenSpec checklist was complete.

GREEN:

- executable state-machine trace: S1/R01 pending -> in_progress -> RED -> S2/R02 pending -> in_progress -> GREEN;
- R01 RED snapshot remains unchanged/directly addressable after R02 GREEN;
- runtime reviewer replacement against unchanged S1 keeps the same R01 project attempt;
- invariant checks reject terminal verdict without evidence and reject more than one non-terminal attempt;
- both codex_only Task Board templates expose Card-level semantic implementation owner plus milestone-level aggregate owner provenance when milestone review is active, together with review requirement/current-attempt/attempt-list/subject/state/reviewer-role/evidence;
- forbidden runtime identity keys (`session_id`, `invocation_id`, worker/model/profile/reasoning/lease/resume identity) are absent as required schema keys;
- M03-owned parallel keys/algorithm fields are absent from M02 schema;
- modified codex_only M02 contracts contain no active dependency on `workflow/chatgpt_only/*`, `workflow/codex/*`, `workflow/legacy/*` or `workflow/contracts/*`;
- root `workflow/CONTEXT_ROUTING.md` is identical to current workflow `main`;
- full `workflow/chatgpt_only/` tree is identical to current workflow `main`;
- root `PROJECT.md` still declares `execution_policy: chatgpt_only`;
- `git diff --check 10ae410e..b901d1cf` GREEN;
- conflict-marker scan GREEN.

YAML parser validation is not claimed: PyYAML is unavailable in the Tower runtime. The templates were structurally checked for the required M02 shape and forbidden fields.

## Scenario evidence

`docs/audits/CODEX_ONLY_M02_FORMAL_REVIEW_RECOVERY.md` durably traces:

- A1 implementation -> B1 RED;
- owning-Executor repair -> new S2/R02;
- same logical Tester full recheck -> GREEN;
- replacement Tester path with unchanged project attempt semantics;
- recovery after runtime loss and partial Main writes;
- no redundant normal-ChatGPT review.

## Independent review Attempt 1 and repair

Attempt 1 reviewed `af08ef755cb846118c9dbe1a6edf7f5f139cf0e8` and returned RED in `implementation/workstreams/feature-codex-only-policy/evidence/M02-T01-review-01.md`.

The bounded correction stayed inside accepted M02 authority:

- milestone review provenance is now durable at the milestone owner;
- Card-owned RED continues directly to that Card's semantic Executor owner;
- milestone-owned RED routes through Execution Prep to exact bounded corrective Card(s) with Card-level `implementation_owner_role: executor`;
- corrected milestone/checkpoint review freezes a new exact subject and preserves prior RED history;
- default/workstream template guidance, OpenSpec and the M02 audit were reconciled to the same rule.

Post-repair verification on exact corrected subject `b901d1cfa5ca26074b363b0c8980f7a7aaa223f1` is GREEN for diff-check, conflict scan, milestone owner/template shape, forbidden runtime schema keys, M03-field absence, policy cross-import absence, root `execution_policy: chatgpt_only`, and the previously verified unchanged root router/chatgpt_only baseline. YAML parser validation is still not claimed because no parser is available in the Tower runtime.

## Review boundary

`M02-T01` records `Independent review: RECOMMENDED`.

The correction-owning chat must freeze subject `b901d1cfa5ca26074b363b0c8980f7a7aaa223f1` as a new pending Card review and must not issue the verdict for that corrected subject.
