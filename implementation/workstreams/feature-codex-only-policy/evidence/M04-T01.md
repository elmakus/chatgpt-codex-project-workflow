# M04-T01 Implementation Evidence — lifecycle integration and routing cutover

Card: `M04-T01`
Implementation base: `1fbb601461604fe07151478e017cb94bf60de47b`
Final implementation subject: `48a56cbd5554b1378fa3727449bfce94e2f7f1df`
Current workflow main checked: `d64d8c1d7f05ce2a2584ffcb3ade4634263bbc95`
Merge base with current main: `6b0445256b417f82431fb7b2704f56691eb4e7ae`
OpenSpec: `openspec/changes/codex-only-m04-lifecycle-cutover/`
Detailed audit: `docs/audits/CODEX_ONLY_M04_LIFECYCLE_CUTOVER.md`
Current-main refresh: `implementation/workstreams/feature-codex-only-policy/evidence/M04-current-main-refresh.md`

## Implemented

- Root policy dispatch now has an explicit isolated `codex_only -> workflow/codex_only/ROUTER.md` route while preserving `chatgpt_only` and legacy fallback for other accepted policies.
- The 22-file `workflow/codex_only/` namespace is live across Intake, Brainstorming, Research, Definition, Planning, Execution Prep, Execution, Review, Recovery, Workstreams, Repository, micro-fix, Context Health and Close.
- M02 immutable review-attempt / Tester non-repair semantics and M03 serial-default bounded parallel / Main-owned integration semantics remain in force.
- Branch-isolated/default Task Board selection, stacked workstreams, target refresh, final-integration review, namespaced handoffs and terminal target-side durable-package recovery are represented.
- The repository itself remains `execution_policy: chatgpt_only`.

## Author verification

GREEN for exact subject `48a56cbd5554b1378fa3727449bfce94e2f7f1df`:

- feature-side diff against current-main merge base changes zero files under `workflow/chatgpt_only/*`;
- root dispatcher contains the dedicated `chatgpt_only`, `codex_only`, and legacy routes;
- `workflow/codex_only/` contains exactly 22 policy-owner files;
- no deferred M04 activation markers remain;
- no `workflow/chatgpt_only/*`, `workflow/codex/*`, `workflow/legacy/*`, or `workflow/contracts/*` path dependency exists inside `workflow/codex_only/*`;
- current-main terminal workstream semantics are represented in Codex-only Workstreams/Close/Recovery/Repository;
- Codex-only YAML templates contain no runtime worker/session/model/profile/invocation/resume/worktree identity and no scheduler/queue schema keys;
- selected manifest and Task Board binding identity matches `feature-codex-only-policy` / `feat/codex-only-policy`;
- M04 OpenSpec checklist is complete;
- conflict-marker scan is GREEN;
- `git diff --check 1fbb6014..48a56cbd` is GREEN after fixing two audit trailing-space defects and two EOF blank-line defects found during final verification;
- root `PROJECT.md` remains `execution_policy: chatgpt_only`;
- remote branch readback points exactly to `48a56cbd5554b1378fa3727449bfce94e2f7f1df`.

YAML files received structural sanity checks. Parser-based YAML validation is not claimed because the available verification environment does not provide a YAML parser.

## Result

Author verification GREEN for implementation subject `48a56cbd5554b1378fa3727449bfce94e2f7f1df`.

The stable Card contract marks independent review RECOMMENDED. The Card therefore remains non-terminal and must freeze this exact subject as `review_state: pending` before completion.
