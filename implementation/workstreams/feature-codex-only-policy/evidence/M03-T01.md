# M03-T01 Implementation Evidence — bounded parallel Card safety

Card: `M03-T01`
Implementation base: `43c234e59e6db081a0b3dbf7efd9ae16948e0553`
Prior implementation subject: `b03225ac0b92a87a938f9cd79c70ee1715eecd33`
Independent review 01: RED — `implementation/workstreams/feature-codex-only-policy/evidence/M03-T01-review-01.md`
Corrected implementation subject: `20dd6e7827ab249b3884ab7e10cb49ba61d40c60`
OpenSpec: `openspec/changes/codex-only-m03-bounded-parallel-safety/`

## Implemented

- Stable Card safety contract now defines explicit opt-in `parallel_safe`, bounded normalized `write_scope` and `exclusive_resources`, with absent metadata remaining serial-valid.
- Planning candidate parallelism is explicitly non-executable; current-state concurrency authority belongs to Execution Prep/JIT.
- Execution Prep deterministically scans READY Cards in canonical Task Board order, greedily builds one pairwise-compatible set, proves one exact recoverable integration base and isolated mutable workspaces, and freezes finite membership/order before launch.
- Selected Task Board M03 state now supports optional `parallel` batch history with stable project-local batch/lane labels, exact integration base, returned result refs, integrated refs and evidence. Concrete runtime identity is excluded.
- Execution keeps Codex Main as the sole shared-state/integration writer. Worker lanes receive one bounded Card/base and return exact result/evidence only.
- Returned lane diffs are checked against frozen base, write scope and reserved shared-state exclusions before Main integrates them in frozen member order.
- Reviewable Card subjects are frozen only after exact Main-integrated Card results are durable, preserving the M02 immutable-attempt/non-repair model.
- Recovery covers prepared, in-progress, returned, partially integrated, blocked and complete durable boundaries without replaying completed lane work after runtime loss.
- Workstream/Repository contracts apply isolated worktree/equivalent workspace requirements to intra-workstream concurrent local mutation.
- Default and branch-isolated codex_only Task Board templates expose optional batch state while `parallel: null` remains ordinary serial behavior.
- Positive/negative scenario audit covers compatible batches, serial fallbacks, reserved-state/scope escape, integration conflict and crash recovery.

## Independent-review RED-01 repair

The first independent review found that a prepared batch could be cleared after its Cards had already been moved to `in_progress`, with no deterministic reconciliation of those Card statuses.

The corrected contracts now require a pre-launch abandonment transition only while every member remains `prepared` and result-free. Main records the abandoned batch/member outcome as durable `blocked` history with evidence, restores each batch-owned Card to legal READY/serial state, and only then clears `current_batch`. The abandoned batch ID is never reused. Once any member becomes runtime-active or obtains a result, this unwind is forbidden and normal running/returned recovery applies.

The repair also distinguishes the immutable lane `integration_base` from expected Main-owned batch-control bookkeeping commits, so persisting the frozen batch does not invalidate its own implementation base.

## Verification

GREEN:

- Required M03 contract fields are present in `TASK_CARDS.md` and `TASK_CARD_TEMPLATE.md`.
- Both Task Board templates contain `parallel: null` plus the same commented finite-batch shape with `current_batch`, exact `integration_base`, semantic `lane`, `result_commit`, `integrated_commit` and evidence.
- Active YAML content of both Task Board templates parses successfully with PyYAML 6.0.3.
- Execution Prep contains deterministic canonical-Task-Board-order compatible-set selection and explicit no-refill semantics.
- Router prioritizes Review/Research/RED before current-batch continuation and routes READY state through JIT batch evaluation with serial fallback.
- Recovery explicitly covers all six durable batch boundaries required by the M03 design.
- Workstreams and Repository require separate mutable worktree/equivalent isolation for concurrent local lanes and persist no concrete worktree path.
- Required template schemas contain no `session_id`, `invocation_id`, worker/model/profile/reasoning IDs, lease/resume token, concrete worktree/workspace path, scheduler or queue key.
- Modified codex_only M03 contracts contain no active dependency on `workflow/chatgpt_only/*`, `workflow/codex/*`, `workflow/legacy/*` or `workflow/contracts/*`.
- Root `workflow/CONTEXT_ROUTING.md` and the complete `workflow/chatgpt_only/` file/blob set match current workflow `main` at verification.
- Root `PROJECT.md` remains `execution_policy: chatgpt_only`.
- M03 OpenSpec implementation checklist is complete.
- Original implementation compare `43c234e..b03225a` changes only M03 OpenSpec/audit and `workflow/codex_only/*` contract/template files.
- Repair compare `b03225a..20dd6e7` is clean under `git diff --check` and conflict-marker scan.
- Repair static scan adds no forbidden runtime identity/scheduler schema key; root routing/chatgpt_only remain identical to current main and root project policy remains `chatgpt_only`.
- The repaired stale-prepared scenario explicitly proves that batch-owned Card `in_progress` state is reconciled before `current_batch` is cleared and that the abandoned batch remains durable blocked history.

## Scenario result

The contract state machine satisfies:

- compatible two-or-more-Card batch: GREEN;
- missing/false opt-in: serial fallback GREEN;
- incomplete dependency: not batch-eligible GREEN;
- overlapping write scope: serial fallback GREEN;
- conflicting exclusive resource: serial fallback GREEN;
- unavailable isolation: serial fallback GREEN;
- unrecoverable/stale base before launch: rebuild/serial fallback GREEN;
- returned scope escape/reserved-state mutation: fail-closed non-integration GREEN;
- partial integration interruption: preserve integrated and returned results, resume next frozen member GREEN;
- runtime worker loss: project batch/member identity unchanged GREEN;
- exact integrated result feeds ordinary M02 review lifecycle GREEN.

## Boundary note

No actual `codex_only` runtime batch is launched by this milestone because root policy cutover is explicitly deferred to M04. M03 verifies the future policy contract and durable state semantics without changing this repository's active `chatgpt_only` execution route.

## Result

Author repair verification GREEN for corrected implementation subject `20dd6e7827ab249b3884ab7e10cb49ba61d40c60`. Independent re-review remains RECOMMENDED by the stable Card contract.
