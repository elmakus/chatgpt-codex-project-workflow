# M05-T01 — final regression and integration-readiness verification

Card: `implementation/workstreams/feature-codex-only-policy/cards/M05-T01.md`
Implementation subject: `3d72fdec89027b0b20fe54577e5a40cd162c14b1`
Current target checked: `main` at `d64d8c1d7f05ce2a2584ffcb3ade4634263bbc95`
Merge base: `6b0445256b417f82431fb7b2704f56691eb4e7ae`
Result: **GREEN**

## Verification

- exact root routing split present for `chatgpt_only`, `codex_only` and legacy fallback;
- Codex-only/current-main ChatGPT-only owner set: 22/22 same filenames;
- forbidden active cross-policy/legacy execution references in `workflow/codex_only/*`: none;
- deferred M04 activation markers: none;
- feature-side `workflow/chatgpt_only/*` changes relative to merge base: none;
- root repository policy remains `execution_policy: chatgpt_only`;
- runtime worker/session/model/profile/worktree/scheduler schema scan of active Codex-only YAML templates: GREEN;
- semantic lifecycle/review/recovery/workstream/parallel matrix: 29/29 GREEN;
- legacy-property inventory terms required by CO-REQ-026 remain represented;
- full feature-range `git diff --check`: GREEN after non-behavioral M01 Markdown whitespace normalization;
- added-line conflict-marker scan: GREEN;
- working tree after subject commit: clean.

PyYAML is not installed on the verification runner; parser-based YAML validation is therefore not claimed.
## Documentation/readiness outputs

- final architecture/readiness audit: `docs/audits/CODEX_ONLY_M05_FINAL_AUDIT.md`;
- migration notes: `docs/CODEX_ONLY_MIGRATION_NOTES.md`;
- README updated for both dedicated migrated namespaces;
- CHANGELOG updated for the Codex-only policy migration.

## Current-target trial integration

Updated subject trial merge tree against current `main`: `517e65b78f0888541fdad621daf2fa8b4d596295`.

Git merge-tree reports a textual merge result, but semantic readback confirms merged `PROJECT.md` does **not** equal current target `PROJECT.md`. The feature branch would otherwise carry stale branch-specific global status/authority pointers into the final target.

This is the expected bounded M05/Close refresh obligation. Final integration must preserve target-owned global project state, rerun affected compatibility checks, then freeze/reconcile the manifest final-integration review subject.

## Conclusion

M05-T01 acceptance is GREEN. No behavioral corrective Card is required.

The Card has `independent review: none`; the selected feature workstream's distinct manifest-owned `RECOMMENDED` final-integration review remains the next substantive review gate after milestone Close performs current-target refresh.
