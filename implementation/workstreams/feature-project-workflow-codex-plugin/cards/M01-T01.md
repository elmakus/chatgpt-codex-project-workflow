# M01-T01 — Verify current Codex plugin activation contract

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/PROJECT_WORKFLOW_CODEX_PLUGIN_MASTER_PLAN.md#milestone-m01--current-runtime-plugin-and-activation-contract`
- Requirements: `requirements/PROJECT_WORKFLOW_CODEX_PLUGIN.md` — PWCP-REQ-001, PWCP-REQ-003, PWCP-REQ-004, PWCP-REQ-005, PWCP-REQ-006, PWCP-REQ-007, PWCP-REQ-014, PWCP-REQ-015
- Accepted decisions: `decisions/ADR_PROJECT_WORKFLOW_CODEX_PLUGIN_PACKAGING.md`, `decisions/ADR_PROJECT_WORKFLOW_CODEX_PLUGIN_ACTIVATION.md`
- Relevant OpenSpec: none
- Accepted dependency results: qualified generic marketplace/plugin baseline from `elmakus/newproject-skill` current-runtime evidence. Existing M01-T01 runtime evidence at `implementation/workstreams/feature-project-workflow-codex-plugin/evidence/M01-T01-runtime-contract-2026-09-20.md` already proves the PWCP-specific packaging/activation/path/trust/isolation delta and the bundled-Skill identity shape `<plugin-name>:<skill-name>`. The resolved naming record at `implementation/workstreams/feature-project-workflow-codex-plugin/blockers/M01-T01-command-name-reopened-2026-09-20.md`, Definition R3 and approved plan PWCP-P3 change only the accepted explicit identity to plugin `pw` + Skill `project-workflow` → `$pw:project-workflow`; unchanged runtime evidence is reusable and only this affected naming delta requires bounded revalidation.

### Must preserve

- Same-repository packaging remains the accepted architecture unless exact current-runtime evidence proves a hard platform constraint.
- Canonical Project Workflow files remain the only workflow authority; Skill/hook/bootstrap content may only route to them.
- Exactly one explicit concise bundled Skill entrypoint is targeted as plugin `pw` + Skill `project-workflow`, exposed as `$pw:project-workflow`; do not create alias Skills.
- Enabled repositories must receive a small always-on invariant without eagerly loading the workflow tree.
- Activation must route through workspace `PROJECT.md` and canonical Project Workflow routing, preserving selected execution policy.
- A control repository without Project Workflow enabled must remain unaffected.
- Hook/equivalent trust behavior must be explicit and testable.
- Reuse generic marketplace/install evidence where unchanged; test only the PWCP-specific delta.
- Do not add plugin-local update scheduling.

### Must not / rationale that must travel

- Do not copy `workflow/codex_only/*` semantics into Skill/bootstrap files.
- Do not treat documentation of a hook capability as proof that the PWCP package actually resolves canonical files or injects the intended bounded context.
- Do not mutate live Codex plugin/marketplace state during verification when an isolated `CODEX_HOME` can prove the same behavior.
- Do not decide the final `#issue/#feature` versus `$pw:project-workflow issue/$pw:project-workflow feature` convention here; comparative E2E belongs to M03.

## Dependencies

- none

## Outcome

Reconcile the existing durable current-runtime contract under Definition R3 by reusing unchanged packaging/activation/path/trust/isolation evidence and performing only the bounded affected naming delta needed to prove plugin `pw` + Skill `project-workflow` is accepted/discoverable/invokable as `$pw:project-workflow`. End with a concrete M02 implementation contract under the approved identity. If current platform behavior contradicts an accepted architectural invariant, stop and route the contradiction back through Definition rather than inventing a workaround outside authority.

## Scope

### Included

- Exact current Codex runtime/version and native plugin/marketplace surfaces needed by this feature; unchanged prior findings may be reused by exact evidence reference.
- Reuse/audit of qualified generic Git-backed marketplace/plugin evidence.
- Isolated same-repository marketplace/plugin packaging probe whose payload includes sibling canonical workflow files.
- Installed plugin-root → canonical `workflow/CONTEXT_ROUTING.md` / Codex router path resolution.
- Minimal plugin-owned `SessionStart` (or smaller proven equivalent) activation candidate for startup/resume/compact lifecycle.
- Bounded additional-context payload and progressive-disclosure behavior.
- Hook trust/review behavior.
- Enabled-repository versus control-repository activation/isolation behavior at the current runtime.
- Bounded validation that the normal Skill name `project-workflow` is accepted by the current runtime and is exposed under plugin `pw` as `pw:project-workflow` through native Skill discovery plus real explicit invocation.
- Exact evidence needed to contract M02 implementation.

### Excluded

- Production plugin files in this repository.
- Final `#issue/#feature` versus `$pw:pw issue/$pw:pw feature` UX selection.
- Full M03 lifecycle acceptance matrix.
- Marketplace updater implementation.
- Final update-propagation acceptance.
- Live Workstation plugin installation or mutation of the user's live Codex configuration.

## Acceptance

1. Exact current Codex runtime/version is recorded.
2. Existing generic Git-backed marketplace/plugin evidence is referenced and its reusable boundary is explicit.
3. Existing isolated current-runtime evidence remains valid for same-repository packaging, canonical-path resolution, bounded activation, trust and enabled/control isolation where the mechanism is unchanged.
4. Installed activation code resolves canonical workflow paths from the installed plugin root without copied policy.
5. The chosen always-on candidate injects only a bounded routing reminder and does not preload canonical workflow contents.
6. Current-runtime evidence shows the candidate covers the accepted startup/resume/compact lifecycle contract sufficiently to proceed to M02, with full E2E lifecycle acceptance still owned by M03.
7. Trust behavior is explicit and reproducible.
8. Enabled/control behavior demonstrates that this feature does not silently govern a repository where it is not enabled.
9. No live Codex marketplace/plugin state is changed by the verification.
10. A bounded current-runtime naming-delta probe proves Skill name `project-workflow` is accepted/discovered for plugin `pw` and a real `$pw:project-workflow` invocation loads the expected Skill.
11. Evidence ends with a concrete M02 implementation contract or an exact hard-platform contradiction requiring Definition reconsideration.

## Required tests / checks

- Reuse the prior isolated `CODEX_HOME` marketplace/plugin/path/trust/isolation evidence by exact pointer where unchanged; do not rerun those checks solely because the accepted Skill name changed.
- Installed payload tree/path check for Skill, hook/bootstrap and canonical workflow files.
- Bounded isolated current-runtime Skill-name validation plus native Skill discovery for plugin `pw` / Skill `project-workflow`, followed by a real explicit `$pw:project-workflow` invocation with a unique sentinel.
- Reuse exact current-runtime trust-path evidence for the unchanged hook/equivalent.
- Reuse the existing enabled/control workspace comparison because the activation mechanism is unchanged.
- Live plugin/marketplace state pre/post comparison when the runtime surface is touched.
- Static size/content check proving the always-on payload is only routing context, not workflow policy.
- Repository/evidence diff check and conflict-marker scan before freezing the subject.

## External write/readback needs

Only disposable isolated runtime state under a temporary `CODEX_HOME` / test workspace. No live marketplace/plugin/config mutation is authorized.

## Independent review

`RECOMMENDED` — M01 selects the concrete activation/path contract that M02 will implement, so independent verification is useful before downstream source changes begin.

## Contract overrides

None.
