# M04-T01 — Reconcile release documentation and full feature acceptance

- Milestone: `M04`

> Stable Task Card contract. Mutable execution/review/result state lives only in the selected canonical Task Board.

## Authority slice

- Master Plan: `planning/PROJECT_WORKFLOW_CODEX_PLUGIN_MASTER_PLAN.md#milestone-m04--integrated-acceptance-and-release-readiness`
- Requirements: `requirements/PROJECT_WORKFLOW_CODEX_PLUGIN.md` — PWCP-REQ-001 through PWCP-REQ-015.
- Accepted decisions: `decisions/ADR_PROJECT_WORKFLOW_CODEX_PLUGIN_PACKAGING.md`, `decisions/ADR_PROJECT_WORKFLOW_CODEX_PLUGIN_ACTIVATION.md`.
- Dependency evidence:
  - `implementation/workstreams/feature-project-workflow-codex-plugin/evidence/M01-acceptance-2026-09-20.md`
  - `implementation/workstreams/feature-project-workflow-codex-plugin/evidence/M02-acceptance-2026-09-20.md`
  - `implementation/workstreams/feature-project-workflow-codex-plugin/evidence/M03-acceptance-2026-09-20.md`
  - `implementation/workstreams/feature-project-workflow-codex-plugin/handoffs/M03_HANDOFF.md`
- Relevant OpenSpec: none.

### Must preserve

- Canonical Project Workflow policy remains the normal repository `workflow/` tree; Skill/bootstrap stay thin and must not duplicate policy semantics.
- Production plugin identity remains plugin `pw` + one Skill `project-workflow` exposed as `$pw:project-workflow`.
- Repository opt-in remains explicit: installing the package must not implicitly activate Project Workflow for unrelated repositories.
- User-facing intake documentation must reflect M03 evidence: prefer `#feature <description>` / `#issue <description>`; keep `$pw:project-workflow` as general explicit entry/recovery; do not present Skill `feature` / `issue` arguments as equivalent intake syntax on the accepted runtime.
- Hook trust remains an explicit activation condition. Test-only trust bypass is not a normal user instruction.
- Marketplace/update ownership remains outside the plugin; no updater/timer/poller is introduced.
- Reuse qualified M01–M03 evidence rather than re-running unchanged platform certification.
- Final current-`main` refresh, manifest-owned final-integration review and final merge remain Close-owned workstream obligations after this Card becomes terminal.

## Outcome

Release-facing Project Workflow Codex plugin documentation and one complete feature acceptance record agree with the verified current-runtime behavior and accepted authority, leaving an internally consistent subject ready for final workstream refresh/review.

## Included scope

- Add/reconcile concise Codex plugin install/enable/trust/usage documentation.
- Link the plugin documentation from the main README.
- Record the supported `#feature` / `#issue` intake convention and `$pw:project-workflow` general entry/recovery behavior.
- Record per-repository enable/control semantics and the trust prerequisite without normalizing test-only bypass flags into user guidance.
- Audit the final package for copied/stale workflow semantics and duplicate Skills/updater surfaces.
- Run the repository regression suite and current-runtime package/readback checks needed for the M04 delta.
- Produce a durable PWCP-REQ-001…015 acceptance matrix using exact M01–M04 evidence pointers.

## Excluded scope

- New plugin behavior or new workflow policy semantics.
- New aliases, duplicate Skills, plugin-local updater/release automation or credentials.
- Re-certification of unchanged generic marketplace/update behavior.
- Final target reconciliation, PR/merge, target-side closure, source-branch cleanup and the manifest-owned final-integration verdict; those remain Close-owned.
- Live user marketplace/plugin/config/trust mutation.

## Acceptance

1. Release-facing documentation states the exact plugin/Skill identity and a current-runtime-compatible marketplace/install path.
2. Documentation explains per-repository opt-in/control isolation and the normal hook-trust prerequisite without recommending `--dangerously-bypass-hook-trust`.
3. Documentation states `#feature` / `#issue` as the supported preferred intake forms and `$pw:project-workflow` as the general explicit entry/recovery path; unsupported Skill-argument equivalence is not claimed.
4. Documentation states canonical-source/progressive-disclosure and updater ownership accurately.
5. A complete PWCP-REQ-001…015 matrix maps every requirement to exact accepted or freshly verified evidence with no unresolved gap.
6. Drift audit confirms one bundled Skill, bounded bootstrap, no copied policy tree in Skill/bootstrap, and no plugin-local updater/timer/poller.
7. Repository tests are GREEN and any current-runtime M04 package/readback delta check is GREEN.
8. No live Codex marketplace/plugin/config/trust state is changed.
9. The resulting workstream subject is ready for Close to run current-`main` integration refresh and the distinct manifest final-integration review gate.

## Required tests / checks

- `python3 -m unittest discover -s tests -p "test_*.py"`.
- Static package/drift audit against `.agents/plugins/marketplace.json`, `.codex-plugin/plugin.json`, `skills/project-workflow/SKILL.md`, `hooks/hooks.json`, `hooks/session-start.py`.
- Documentation consistency check against M01–M03 accepted evidence.
- Current Codex runtime identity plus isolated plugin install/readback when needed to validate documented CLI/package syntax.
- Complete requirement-to-evidence matrix audit.

## External write/readback needs

None. Any runtime verification must use disposable isolated state.

## Independent review

`none` at Card level. The selected feature workstream already carries a distinct `RECOMMENDED` manifest-owned final-integration review gate. M04 Close must run current-target refresh first and then freeze/reconcile that exact whole-workstream subject for fresh independent review; this Card must not create a duplicate earlier review gate over a narrower pre-refresh subject.

## Contract overrides

None.
