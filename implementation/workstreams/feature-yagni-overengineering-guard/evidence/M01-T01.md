# M01-T01 implementation evidence — YAGNI / proportional design

Date: `2026-09-21`
Card: `M01-T01`
Tested implementation head: `0eaa3955b68f831ea48c7024ffcf68155f319dc0`
Pull request: `#51`

## Implemented outcome

- Added one canonical policy-neutral YAGNI/proportional-design invariant to `workflow/common/AUTHORITY.md`.
- The invariant requires concrete current justification for material extra complexity, rejects hypothetical future-proofing alone, defines “simplest” as least unnecessary complexity, and preserves all current correctness/security/testing/maintainability/compatibility/observability/migration obligations.
- Added only minimal operational consumption to ChatGPT-only and Codex-only Planning: their existing overengineering audit now applies the common invariant and concrete-current-justification question.
- Added only minimal operational consumption to ChatGPT-only and Codex-only independent Review: unjustified speculative complexity is a review defect, while currently required complexity and quality obligations remain protected.
- Added focused contract regression coverage in `tests/test_yagni_proportional_design_contract.py`.
- Added one concise README discoverability note that points conceptually to common authority rather than becoming a second contract.
- Kept OpenSpec selective: no YAGNI-specific OpenSpec was created because the approved R1 Definition, ADR-YAGNI-001 and reviewed YAGNI-P2 already freeze the behavior and ownership boundary without additional implementation ambiguity.
- Added no YAGNI lifecycle/state owner/gate/score/budget/registry/numeric threshold and changed no existing policy-specific OpenSpec ownership.

## Verification

- Focused contract test: `python3 -m unittest tests.test_yagni_proportional_design_contract -v` → `6/6 GREEN`.
- Full repository suite: `python3 -m unittest discover -s tests -p 'test_*.py'` → `94/94 GREEN` on the tested implementation head.
- `git diff --check` for the implementation change → GREEN.
- Conflict-marker scan across changed workflow/test/docs files → GREEN.
- Contract test verifies both migrated fixed-policy routers still bootstrap `workflow/common/AUTHORITY.md`, policy-local consumers do not define a second `## YAGNI / proportional design` section, and quality/no-new-machinery guardrails remain explicit.
- Current `main` has moved since the workstream base. The M01 behavior files were checked before implementation and had no target-side divergence; target integration/mergeability reconciliation remains correctly owned by the later Close integration-refresh gate rather than this Card implementation.

## Result

Author-side Card acceptance is GREEN. The Card remains non-terminal because its `RECOMMENDED` independent review must judge the exact frozen subject before Definition of Done.
