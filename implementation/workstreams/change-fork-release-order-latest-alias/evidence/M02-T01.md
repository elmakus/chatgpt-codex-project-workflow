# M02-T01 Implementation Evidence — fork-channel ordering and moving aliases

## Authority and baseline

- Card: `implementation/workstreams/change-fork-release-order-latest-alias/cards/M02-T01.md`
- Approved plan: `planning/FORK_RELEASE_VERSIONING_MASTER_PLAN.md` / `FRV-P3` / M02
- Requirements: `requirements/FORK_RELEASE_VERSIONING.md` R2 / FRV-REQ-007, FRV-REQ-009, FRV-REQ-013..017
- Decision: `decisions/ADR_FORK_RELEASE_VERSION_LINEAGE.md` / `ADR-FRV-001`
- Independent plan review: `planning/reviews/FRV-P3.md` GREEN
- Completed baseline: prior `issue-fork-release-versioning` M01
- Execution branch: `work/fork-release-order-latest-alias`
- Refresh Gate target: `main@fd2dc95f539d982e1009d71bbf1301f3098900f6`
- Refresh result before implementation: branch `behind_by: 0`; no target reconciliation required.

## JIT OpenSpec

The existing `openspec/changes/fork-release-versioning/` change was reconciled for M02 before behavior-changing edits.

It now formalizes:

- baseline-local next-release selection versus cross-baseline current/update selection;
- exact canonical private candidate filtering;
- numeric `(X, Y, Z, N)` fork-channel order;
- exclusion of upstream-only, legacy and malformed tags;
- optional native moving-`latest` semantics;
- stable eligibility via project release-quality policy;
- immutable versioned-reference and artifact/content identity;
- no synthetic canonical `vlatest` Git release/tag;
- no runtime resolver service or cross-repository deployment machinery.

## Implemented subject

Exact immutable implementation subject:

`822c0ee434e5dde5904a3709af5022bc0905bb5b`

Implemented behavior:

- extended `workflow/common/FORK_RELEASE_VERSIONING.md` with canonical cross-baseline fork-channel ordering;
- explicitly rejected generic SemVer, including ad-hoc wrapping, as the canonical fork-channel resolver;
- added optional native moving-`latest` semantics while preserving immutable/versioned identity;
- extended `tests/test_fork_release_versioning.py` with canonical-private parsing/order regression coverage;
- updated README discoverability without duplicating the detailed algorithm;
- reconciled the existing fork-release OpenSpec for M02;
- left ChatGPT-only, Codex-only and legacy/shared publication modules as references to the one common contract;
- did not modify `.github/workflows/auto-patch-tag.yml`;
- did not mutate prior M01 execution history or any downstream repository.

## Verification

Verification on a fresh checkout of exact subject `822c0ee434e5dde5904a3709af5022bc0905bb5b`:

- `python3 -m unittest tests.test_fork_release_versioning` → **12/12 GREEN**;
- `python3 -m unittest discover -s tests -p 'test_*.py'` → **112/112 GREEN**;
- `git diff --check` → **GREEN**.

Focused coverage proves:

- numeric `private.10 > private.4`;
- cross-baseline tuple ordering;
- upstream-only `v1.1.18` is not a canonical channel candidate and cannot outrank `v1.1.18-private.4`;
- malformed/non-canonical tags are excluded;
- the common contract carries moving-alias, stable-eligibility, immutable-reference, artifact-identity and no-`vlatest` semantics;
- all three supported publication surfaces still reference `workflow/common/FORK_RELEASE_VERSIONING.md`;
- the repository-local auto-patch tagger remains outside this contract.

GitHub compare for `main...822c0ee` reports `behind_by: 0`; the changed-file set is confined to FRV authority/planning/workstream state, M02 OpenSpec, common contract, FRV tests and README.

## External effects

No tag, release, registry alias, deployment or downstream-repository mutation was performed. This Card changes only Project Workflow repository authority/contracts/tests/documentation on its workstream branch.

## Result

Implementation acceptance checks are GREEN. The Card remains non-terminal and is ready to freeze for its `RECOMMENDED` independent review gate.


## Independent review verdict

Verdict: **GREEN**

Reviewed immutable subject:

`822c0ee434e5dde5904a3709af5022bc0905bb5b`

Independent checks:

- reconstructed the exact M02-T01 authority slice from FRV R2, ADR-FRV-001, approved FRV-P3, the Task Card and reconciled OpenSpec;
- audited the exact subject tree and `main...822c0ee` changed-file boundary;
- verified the common contract separates baseline-local next-release selection from cross-baseline fork-channel selection and uses exact canonical private candidates with numeric `(X, Y, Z, N)` ordering;
- verified upstream-only/legacy/malformed/non-canonical candidates are excluded and generic SemVer is explicitly rejected as the canonical fork-channel resolver;
- verified optional native `latest` semantics preserve stable-eligibility policy, immutable/versioned references, exact artifact/content identity and the no-`vlatest` boundary;
- verified README remains concise, all three publication surfaces still reference the one common contract, and `.github/workflows/auto-patch-tag.yml` is outside the changed subject;
- independently reran the required verification on an isolated checkout of exact subject: `python3 -m unittest tests.test_fork_release_versioning` → **12/12 GREEN**; `python3 -m unittest discover -s tests -p 'test_*.py'` → **112/112 GREEN**; `git diff --check` → **GREEN**.

No blocking authority mismatch, acceptance failure, unjustified complexity, historical-state mutation, downstream write, or scope expansion was found.
