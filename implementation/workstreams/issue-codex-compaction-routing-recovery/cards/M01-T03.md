# M01-T03 — Add orchestration recovery regressions and documentation

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/CODEX_ORCHESTRATION_CONTEXT_RECOVERY_MASTER_PLAN.md#M01 — Token-light orchestration recovery boundary`
- Requirements: `requirements/CODEX_ORCHESTRATION_CONTEXT_RECOVERY.md` CCOR-R10 plus verification obligations from CCOR-R1…R9
- Accepted decisions: `decisions/ADR_CODEX_ORCHESTRATION_POLICY_BINDING.md`
- Relevant OpenSpec: `openspec/changes/codex-orchestration-context-recovery/`
- Accepted dependency results: M01-T01 and M01-T02 exact GREEN subjects

### Must preserve

- Exact reviewed behavior from M01-T01/T02.
- Same-version reconstruction is the primary regression case; fingerprint drift is additive.
- Negative tests must protect no durable latch, no concrete runtime identity, no silent fallback and no duplicated role map.
- `chatgpt_only` routing/Context Health behavior remains unchanged.

## Dependencies

- `M01-T01` GREEN and terminal
- `M01-T02` GREEN and terminal

## Outcome

The new recovery boundary is locked by focused contract tests and concise discoverability documentation, with repository-wide verification proving no regression to existing policy behavior.

## Scope

### Included

- Add focused `tests/test_codex_only_orchestration_recovery_contract.py` coverage for all CCOR-R10 cases.
- Preserve `tests/test_codex_only_continuous_orchestration_contract.py` GREEN.
- Update README/CHANGELOG only where needed to describe the recovery boundary and Project Workflow ↔ runtime responsibility split.

### Excluded

- New lifecycle semantics not already accepted and implemented by M01-T01/T02.
- Runtime-side `codex_workflow` changes.
- Unrelated documentation cleanup.

## Acceptance

- Tests prove same-version reconstruction cannot bypass the durable binding/re-bind gate.
- Tests prove durable binding/fingerprint alone cannot satisfy the current-context latch.
- Tests prove absent/invalid/unresolvable binding fails closed and fingerprint drift requires re-resolution.
- Tests prove no concrete worker/session/model-instance identity is permitted in project state.
- Tests prove canonical review/ownership/execution-policy/real-stop owners remain sufficient without whole-tree reread.
- Tests prove `chatgpt_only` behavior remains unchanged and concrete role/harness mechanics remain runtime-owned.
- Targeted tests and full repository Python unittest suite are GREEN.
- README/CHANGELOG are concise and do not encode runtime-specific mappings.

## Required tests / checks

- `python -m unittest tests.test_codex_only_orchestration_recovery_contract`
- `python -m unittest tests.test_codex_only_continuous_orchestration_contract`
- `python -m unittest discover -s tests -p 'test_*.py'`
- `git diff --check` equivalent.

## External write/readback needs

Repository branch writes only; verify persisted GitHub branch content. No deployment/live-system mutation.

## Independent review

`none` — this Card adds regression/docs for already independently reviewed behavior and must not change its semantics.
