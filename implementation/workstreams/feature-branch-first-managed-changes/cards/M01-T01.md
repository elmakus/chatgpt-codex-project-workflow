# M01-T01 — Define branch-first workstream routing state model

- Milestone: `M01`

## Authority slice

- Master Plan: `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` M01
- Requirements: `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` REQ-BF-001..009, REQ-BF-015..017
- Decisions: ADR-BF-001, ADR-BF-002, ADR-BF-003
- OpenSpec: `openspec/changes/branch-first-m01-state-entry/`
- JIT evidence: `implementation/workstreams/feature-branch-first-managed-changes/evidence/M01_JIT_INVENTORY_2026-09-20.md`

### Must preserve

Integration target stays integrated truth; read-only exploration remains branch-free; manifest routing fields are locators only; Task Board remains Card/milestone state owner; manifest `review` remains final-integration-review-only; no mutable global registry; policy-local lifecycle mechanics stay separate; historical/default evidence is preserved.

## Dependencies

- none

## Outcome

Both fixed-policy workstream contracts represent a neutral managed change and exact workstream-local pre-execution routing without duplicating lifecycle state.

## Scope

### Included
- `workflow/chatgpt_only/WORKSTREAM_TEMPLATE.yaml`
- `workflow/chatgpt_only/WORKSTREAMS.md`
- `workflow/codex_only/WORKSTREAM_TEMPLATE.yaml`
- `workflow/codex_only/WORKSTREAMS.md`
- M01 OpenSpec/schema coherence and focused evidence

### Excluded
- generic natural-language trigger implementation (M01-T02)
- root/bootstrap/default boundary cutover (M01-T03)
- full lifecycle pointer migration (M02/M03)
- README/dogfood cleanup (M04)

## Acceptance

1. Both manifest templates accept `kind: change`.
2. Both contracts provide exact nullable locators for exploratory scope, pre-execution Research and plan review.
3. Routing fields are locator-only; pointed artifacts retain lifecycle ownership.
4. Task Board and final-integration review ownership remain unchanged.
5. Recovery fails closed on malformed/mismatched routing locators without a global registry.
6. Equivalent schema exists in both fixed policies while mechanics remain policy-local.
7. Template/WORKSTREAMS/OpenSpec coherence checks are GREEN.

## Required tests / checks

Inspect both templates and WORKSTREAMS ownership/recovery sections; scan for the new kind/routing fields; verify no common lifecycle module/cross-policy import; read back changed branch files.

## External write/readback needs

Repository branch writes only; exact branch readback required.

## Independent review

REQUIRED — durable workstream schema/recovery semantics change in both fixed policies.
