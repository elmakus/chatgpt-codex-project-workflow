# M03-T01 — Migrate Codex-only pre-execution routing to workstream-local state

- Milestone: `M03`

## Authority slice

- Master Plan / milestone contract: `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md#M03--Codex-only-lifecycle-migration`
- Requirements: `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-002..009, REQ-BF-011, REQ-BF-016..017 and Codex-only part of REQ-BF-015
- Accepted decisions: ADR-BF-001, ADR-BF-002, ADR-BF-003
- Relevant OpenSpec: JIT `openspec/changes/branch-first-m03-codex-lifecycle/`
- Accepted dependency results: M01 GREEN checkpoint; M02 GREEN checkpoint + `implementation/workstreams/feature-branch-first-managed-changes/handoffs/M02_HANDOFF.md`

### Must preserve

- Root `PROJECT.md` remains integrated-project navigation only and must not regain active workstream-local mutable routing pointers.
- The selected Codex-only workstream manifest `routing.exploratory_scope`, `routing.research_obligation`, and `routing.plan_review` are locator-only; pointed artifacts retain lifecycle/status/subject/result ownership.
- Brainstorming → Definition promotion remains explicitly user-owned and bound to the exact pointed exploratory record/revision.
- Pre-execution Research remains crash-safe through one exact workstream locator plus record-owned Origin/Return/reconciliation state.
- Plan review remains an independent exact-plan-subject lifecycle realized by a qualifying Tester; Codex Main remains the sole durable shared-state writer.
- Existing Codex Main / Executor / Tester ownership, runtime-identity-free durable state, and policy-local semantics remain intact.
- Bounded batch execution semantics are not modified by this pre-execution routing Card.
- Codex-only lifecycle mechanics must not import ChatGPT-only modules or semantics.

### Must not / rationale that must travel

Do not satisfy this Card by renaming root PROJECT fields or mirroring workstream locators back into root state. Do not change runtime worker identity into durable project state. Do not perform the execution/default-board migration owned by M03-T02 except for the minimum routing boundary required for coherent recovery.

## Dependencies

- M01 checkpoint GREEN.
- M02 checkpoint GREEN is available as implementation-pattern evidence; it does not authorize importing ChatGPT-only modules.

## Outcome

Codex-only Brainstorming, pre-execution Research, Project Definition, Planning and Plan Review recover and mutate active routing only through the exact selected workstream manifest/pointed records, while preserving Codex Main + independent Tester semantics.

## Scope

### Included

- `workflow/codex_only/ROUTER.md` pre-execution route selection, promotion gate and route read sets.
- `BRAINSTORMING.md`, `RESEARCH.md`, `DEFINITION.md`, `PLANNING.md`, `PLAN_REVIEW.md`.
- Exact workstream locator validation/clearing semantics in `WORKSTREAMS.md` only where required by these routes.
- Fresh-session/plan-review locator wording required for deterministic recovery.
- JIT OpenSpec for Codex-only branch-first lifecycle state/transition contracts.
- Focused policy-local regression checks.

### Excluded

- Execution Prep/State/default-board migration and historical-live-state migration mechanics owned by M03-T02.
- Changes to bounded batch launch/integration semantics except assertions proving they remain untouched.
- Changes to independent Card/final-integration Tester mechanics except assertions proving ownership remains intact.
- ChatGPT-only lifecycle changes.
- Repository dogfood/README/general documentation closure (M04).
- Final workstream integration.

## Acceptance

1. No active Codex-only Brainstorming/Definition/Planning route reads or writes `PROJECT.md → Active exploratory scope`.
2. No pre-execution Codex-only Research handoff/return reads or writes `PROJECT.md → Active research obligation`; selected manifest `routing.research_obligation` owns the locator.
3. The Definition promotion gate resolves the exact exploratory record through selected manifest `routing.exploratory_scope` and remains user-owned.
4. Plan-review activation/recovery uses selected manifest `routing.plan_review` plus the exact review record; completed/stale pointers reconcile deterministically.
5. Pre-execution Research remains crash-safe and record-owned for Status/Origin/Return/reconciliation.
6. Missing/malformed/wrong-subject locators fail closed to Recovery rather than falling back to root/global inference.
7. Codex Main remains sole shared-state writer and plan review remains realized by an independent Tester without durable runtime identity.
8. Bounded batch/Card review ownership contracts remain unchanged.
9. Codex-only files do not import ChatGPT-only lifecycle modules.
10. OpenSpec and focused scenario/static checks are GREEN.

## Required tests / checks

Exact branch readback plus focused scenario/static checks for promotion, pre-execution Research handoff/return, plan-review lifecycle, stale/malformed locator recovery, root-PROJECT absence, Codex Main/Tester ownership, bounded-batch non-regression and cross-policy separation.

## External write/readback needs

Repository branch writes only.

## Independent review

RECOMMENDED — this changes durable Codex-only lifecycle routing/recovery and independent plan-review recovery semantics.
