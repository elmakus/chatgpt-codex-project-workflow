# M04-T01 — Reconcile repository dogfood, documentation and integrated branch-first regressions

- Milestone: `M04`

## Authority slice

- Master Plan / milestone contract: `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md#M04--migration-documentation-dogfood-and-integrated-regression-closure`
- Requirements: `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-001..017
- Accepted decisions: ADR-BF-001, ADR-BF-002, ADR-BF-003
- Accepted dependency results: M01 GREEN, M02 GREEN, M03 GREEN
- JIT inventory: `implementation/workstreams/feature-branch-first-managed-changes/evidence/M04_JIT_INVENTORY_2026-09-20.md`

### Must preserve

- integration target is integrated project truth; every managed fixed-policy change is branch-isolated before the first durable change-specific write and integrates through branch → PR → merge;
- read-only exploration may precede workstream creation;
- natural-language authorization is sufficient; `#issue` / `#feature` remain optional shortcuts;
- historical root/default state remains readable recovery/provenance only and must never be presented as a normal new/continued-work destination under `chatgpt_only` or `codex_only`;
- active exploratory, pre-execution Research and plan-review routing is workstream-local under the selected manifest/records, not root `PROJECT.md`;
- root canonical requirements/decisions/planning remain integrated project truth on the target and proposed future truth on the workstream branch;
- mixed/non-migrated policy legacy semantics remain outside this fixed-policy migration and must not be accidentally rewritten;
- M02/M03 policy-local review, Research, target-refresh, terminal-package and branch-cleanup behavior remains unchanged;
- this workstream's terminal namespaced package must remain durable through final integration.

### Must not / rationale that must travel

Do not delete historical root/default artifacts merely for cleanliness. Do not turn root `PROJECT.md` into an active-workstream registry. Do not broaden M04 into redesign of M02/M03 lifecycle mechanics. Do not perform final target refresh, final-integration review verdict, PR merge or target-side closure inside this Card; those remain Close-owned.

## Dependencies

- M02 GREEN.
- M03 GREEN.

## Outcome

Repository-facing documentation, root dogfood state and focused integrated regression checks consistently express the branch-first-only fixed-policy model, leaving only Close-owned refresh/review/PR integration.

## Scope

### Included

- reconcile `README.md`, root `PROJECT.md` and `CHANGELOG.md` where the JIT inventory found stale fixed-policy/default-state claims;
- add focused integrated regression checks for README/root PROJECT/template/bootstrap/router branch-first invariants;
- validate already-migrated prompt/template/router surfaces and policy separation;
- record exact implementation/verification evidence.

### Excluded

- deleting/reformatting historical default artifacts;
- changing accepted M01–M03 lifecycle behavior;
- final target refresh/reconciliation against current `main`;
- manifest-owned final-integration review verdict;
- PR creation/merge and target-side terminal reconciliation.

## Acceptance

1. README does not present root/default Task Board state as a normal active/new-work path for either migrated fixed policy.
2. README describes workstream-local pre-execution routing and generic natural-language branch-first entry, while retaining `#issue` / `#feature` shortcuts and a proportional trivial-change path.
3. Root `PROJECT.md` is an integrated-project index with no active exploratory/Research routing fields and no active root/default Task Board claim; historical root Task Board/handoff pointers are explicitly recovery/history only.
4. CHANGELOG records the branch-first fixed-policy migration without falsifying older historical entries.
5. Focused regression checks assert the fixed-policy branch-first/new-work absence invariants across README, root PROJECT, template, bootstrap and policy router surfaces.
6. Mixed/non-migrated legacy semantics remain scoped to their policy and are not incorrectly removed.
7. Existing migrated ChatGPT-only/Codex-only lifecycle contracts remain coherent and cross-policy imports are not introduced.
8. Exact branch readback/available validation is GREEN and final Close-owned integration work remains explicitly deferred.

## Required tests / checks

- focused integrated static regression module;
- exact branch readback of README/root PROJECT/CHANGELOG/templates/prompts/router/common authority;
- fixed-policy absence scan for stale active root/default/new-work ownership claims on the covered closure surfaces;
- policy-separation check that migrated fixed-policy lifecycle modules do not import the other fixed policy.

## External write/readback needs

Repository branch writes only.

## Independent review

`none` at Card level — M04 is bounded documentation/dogfood/regression closure and the selected workstream already has a distinct `RECOMMENDED` final-integration review gate that will be frozen only after Close performs current-target refresh. This does not waive that workstream-level review.
