# M01-T02 — Add generic natural-language managed-change entry

- Milestone: `M01`

## Authority slice

- Master Plan: `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` M01
- Requirements: REQ-BF-001..007, REQ-BF-011, REQ-BF-015..017
- Decisions: ADR-BF-001, ADR-BF-003
- OpenSpec: `openspec/changes/branch-first-m01-state-entry/`
- Dependency result: M01-T01 GREEN

### Must preserve

Read-only exploration remains branch-free; branch/workstream exists before first durable change-specific write; `#issue`/`#feature` remain shortcuts; generic work does not guess classification; exact existing identity is recovered; partial creation fails closed; mechanics stay policy-local.

## Dependencies

- M01-T01 GREEN

## Outcome

Both fixed-policy routers/intake contracts recognize clear natural-language managed-change authorization and create/recover a neutral workstream before durable change state.

## Scope

### Included
ChatGPT-only and Codex-only Router/Intake/Workstreams generic-entry, naming/collision/recovery surfaces plus focused transition evidence.

### Excluded
Full downstream lifecycle-pointer migration (M02/M03) and docs/dogfood closure (M04).

## Acceptance

1. Natural-language implementation/adoption requests enter managed-change intake without markers.
2. Read-only inspect/compare does not create a branch.
3. Generic identity is `change-<slug>` + `work/<slug>` with deterministic shared suffixing.
4. Exact valid workstream identity is recovered rather than duplicated.
5. Existing deterministic branch with missing/inconsistent manifest routes to Recovery.
6. Branch creation precedes change-specific durable Project Workflow writes.
7. `#issue`/`#feature` retain existing shortcut semantics.
8. Equivalent behavior exists in both fixed policies without cross-importing lifecycle modules.

## Required tests / checks

Scenario checks for read-only → authorization, marker regressions, naming/collision/recovery scans in both namespaces, and branch-before-write ordering.

## External write/readback needs

Repository branch writes only; exact branch readback required.

## Independent review

RECOMMENDED — routing/entry semantics affect all new managed changes.
