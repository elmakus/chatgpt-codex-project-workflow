# M02-T02 implementation evidence

Date: 2026-09-20
Card: `M02-T02 — Migrate ChatGPT-only execution state and historical-default recovery`
Milestone: `M02`
Candidate implementation content verified through: `12fa5f9913c7b8eb568d5c59a540d30f7910f340`

## Authority

- `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` — M02
- `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-002..009, REQ-BF-011..014, REQ-BF-016..017 and ChatGPT-only part of REQ-BF-015
- ADR-BF-001, ADR-BF-002, ADR-BF-003
- `openspec/changes/branch-first-m02-chatgpt-lifecycle/`
- M01 GREEN checkpoint
- M02-T01 GREEN result/review

## Implemented contract

- `EXECUTION_PREP.md` now requires an exact validated branch-isolated workstream before active Task Board creation and scaffolds only `WORKSTREAM_TASK_BOARD_TEMPLATE.yaml`. Root `implementation/TASK_BOARD.yaml` and the root Task Board template are historical recovery/migration surfaces only.
- `STATE.md` no longer defines legacy/default as a normal active ChatGPT-only state context. Active mutable execution is manifest-bound workstream state only.
- `RECOVERY.md` now owns deterministic historical root/default migration-before-mutation. The source root board is read-only during migration; one exact workstream identity is recovered/created; live review/Research/result/evidence/dependency truth is preserved; partial migration is idempotently recovered; exact manifest ↔ Task Board readback is required before active ownership switches; ambiguity fails closed.
- `CLOSE.md` no longer finalizes an active root/default Task Board and no longer creates root/default cumulative handoffs for active managed work. Historical root handoffs remain readable evidence.
- `TASK_CARDS.md`, `TASK_CARD_TEMPLATE.md` and `TASK_BOARD_TEMPLATE.yaml` no longer offer root/default as an active scaffolding/state destination.
- `REPOSITORY.md` recovery now resolves pre-execution locators through the selected manifest and carries any missing historical contract pointer into the workstream board instead of normalizing the root board in place.
- Existing `MICRO_FIX.md`, Card/milestone review ownership, implementation/recovery Research ownership, target-refresh, stacked-workstream, terminal target-side package and source-branch-deletion semantics remain intact.
- M02 OpenSpec design/spec now records the migration-before-mutation and crash/idempotency invariants; all M02-T02 tasks are checked complete.
- Added `tests/test_chatgpt_only_branch_first_execution_state_contract.py` to encode the migration/default-state regression contract for a normal repository checkout.

## Focused exact-branch checks

Exact branch readback was evaluated against the changed ChatGPT-only lifecycle surfaces.

1. GREEN — Execution Prep contains no legal root/default Task Board scaffolding path and requires the manifest-bound workstream template.
2. GREEN — State contract rejects root/default as active mutable state and requires exact branch-isolated manifest binding.
3. GREEN — historical root/default migration is higher priority than review/Research/execution on that board and is explicitly read-only until namespaced readback succeeds.
4. GREEN — partial migration recovers the same deterministic identity rather than creating a duplicate lane; ambiguous identity/mapping fails closed.
5. GREEN — Router/Workstreams still route historical state or binding mismatch to Recovery without default fallback.
6. GREEN — Card/milestone review remains Task-Board-owned while manifest review remains final-integration-review-only.
7. GREEN — implementation/recovery Research remains selected-Task-Board-owned; pre-execution Research remains manifest-located from M02-T01.
8. GREEN — selected-workstream micro-fix semantics remain unchanged.
9. GREEN — branch-isolated target-refresh gate remains present in Close.
10. GREEN — post-merge/terminal recovery forbids fallback to root `implementation/TASK_BOARD.yaml`.
11. GREEN — historical root/default evidence/handoffs remain readable and are not rewritten solely for topology normalization; task/template surfaces mark root state historical-only.
12. GREEN — inspected ChatGPT-only lifecycle files contain no `workflow/codex_only/` lifecycle import.

The same assertions are stored in `tests/test_chatgpt_only_branch_first_execution_state_contract.py`. This environment had no repository CI/status run for the candidate head, and its local runtime could not network-clone GitHub, so no Python test-runner execution is claimed. The assertions above were executed directly against exact GitHub branch readback and all passed.

## Scope audit

The M02-T02 implementation stays within ChatGPT-only execution/default-state migration plus required task/template/repository/OpenSpec/regression surfaces.

Not changed by this Card:
- Codex-only lifecycle migration (M03);
- completed historical root/default artifacts;
- repository dogfood/README/general documentation closure (M04);
- final workstream integration/merge to `main`.

The workstream still requires current-target refresh and its later final-integration lifecycle before merge; that is not an M02-T02 Card-completion blocker.

## Implementation verdict

GREEN for implementation verification. Freeze the exact evidence-bearing commit as the M02-T02 independent-review subject; Card remains non-terminal until RECOMMENDED independent review is GREEN.
