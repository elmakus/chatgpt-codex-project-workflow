# M02-T01 implementation evidence

Date: 2026-09-20
Card: `M02-T01 — Migrate ChatGPT-only pre-execution routing to workstream-local state`
Branch: `feat/branch-first-managed-changes`

## Authority applied

- `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` — M02
- `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-002..009, REQ-BF-011, REQ-BF-016..017 and ChatGPT-only REQ-BF-015
- ADR-BF-001, ADR-BF-002, ADR-BF-003
- M01 GREEN checkpoint/handoff
- `openspec/changes/branch-first-m02-chatgpt-lifecycle/`

## Implemented lifecycle boundary

ChatGPT-only pre-execution routing is now selected-workstream-local:

- durable Brainstorming records are located by manifest `routing.exploratory_scope`;
- pre-execution Brainstorming/Definition/Planning/plan-review Research is located by manifest `routing.research_obligation`;
- independent plan-review records remain under `planning/reviews/` but active recovery is located by manifest `routing.plan_review`;
- root `PROJECT.md` remains integrated-project navigation and is not a live locator owner.

The pointed artifacts remain the sole lifecycle/status/subject/result owners. Manifest locators are validated fail-closed through `WORKSTREAMS.md`.

## Promotion and recovery

- Brainstorming → Project Definition remains explicitly user-owned.
- Durable promotion authorization is accepted only from the exact manifest-pointed exploratory record and exact current scope/revision.
- The exploratory locator remains through bounded Definition recovery and is cleared only after the owning Definition result is durable.
- Historical root PROJECT exploratory/Research pointers without an exact migrated workstream are recovery/migration input only.
- Pre-execution Research keeps its locator through `complete`; reconciliation is applied before consumption/clear.
- Plan-review locator remains through pending/in-progress/GREEN/RED. The reviewer never clears it; Planning consumes the exact verdict and clears/repoints deterministically.

## Focused verification

GREEN static/scenario checks verified:

1. no active `PROJECT.md → Active exploratory scope` references remain in the migrated ChatGPT-only pre-execution lifecycle modules;
2. no active `PROJECT.md → Active research obligation` references remain there;
3. durable Brainstorming establishes/recoveries `routing.exploratory_scope`;
4. Brainstorming, Definition and Planning pre-execution Research use `routing.research_obligation`;
5. Research records still own Status/Origin/Return/reconciliation and the final-return crash-safe protocol;
6. Planning creates the exact review record and manifest `routing.plan_review` together;
7. independent Plan Review validates the manifest locator, does not clear it, and Planning owns GREEN/RED verdict consumption;
8. Router priority/recovery uses manifest Research/plan-review/exploratory locators and preserves the explicit user promotion gate;
9. invalid/mismatched locator state fails closed to Recovery;
10. no inspected ChatGPT-only file imports Codex-only lifecycle modules;
11. M02 OpenSpec is coherent and its three M02-T01 tasks are complete.

No repository test runner or CI result is claimed; this Card changes workflow contracts and was verified by exact branch readback plus focused static/scenario inspection.

## Scope/readback

The implementation delta from the durable M02-T01 start contains only the M02 OpenSpec and the contracted ChatGPT-only pre-execution lifecycle surfaces.

The workstream baseline predates current `main`. Among M02-T01 lifecycle files, current-target movement touches only `workflow/chatgpt_only/ROUTER.md` in the separate context-health anti-bounce block. M02-T01 does not intentionally alter that block; final target refresh/reconciliation remains mandatory before workstream integration.

## Deferred work

M02-T02 still owns Execution Prep/State/Recovery/Close/task-template removal of legacy/default as a normal active ChatGPT-only execution context and deterministic historical-live-state migration.

## Result

GREEN implementation verification. M02-T01 is ready to freeze an exact immutable subject for independent review.
