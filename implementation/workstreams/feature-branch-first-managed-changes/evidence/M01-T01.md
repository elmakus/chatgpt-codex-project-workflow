# M01-T01 implementation evidence

Date: 2026-09-20
Card: `M01-T01 — Define branch-first workstream routing state model`

## Implemented subject

- ChatGPT-only and Codex-only workstream templates accept neutral `kind: change`.
- Both templates expose the same locator-only pre-execution schema:
  - `routing.exploratory_scope`
  - `routing.research_obligation`
  - `routing.plan_review`
- Both policy-local `WORKSTREAMS.md` contracts define the same ownership boundary and fail-closed recovery semantics.
- Pointed artifacts retain lifecycle/status and subject/result ownership.
- Implementation/recovery Research remains selected-Task-Board-owned.
- Card/milestone execution/review state remains selected-Task-Board-owned.
- Manifest `review` remains workstream final-integration-review-only.
- No common lifecycle module or cross-policy import was introduced.
- OpenSpec design/spec now pins the exact routing keys and validation semantics.

## Verification

Exact branch readback was performed after all contract/schema writes.

Static checks: GREEN.

1. `kind: change` is present in both fixed-policy templates.
2. The `routing` block is structurally identical in both templates.
3. The routing block has exactly the three required nullable locator keys.
4. Both policy-local contracts state locator-only ownership.
5. Both policy-local contracts fail malformed/missing/mismatched/contradictory locators closed to Recovery.
6. Both contracts preserve Task Board ownership for implementation/recovery Research.
7. Both contracts preserve manifest final-integration review ownership.
8. Neither policy-local `WORKSTREAMS.md` imports the other policy namespace.
9. OpenSpec contains the exact schema and fail-closed validation contract.
10. The first two M01 OpenSpec tasks corresponding to this Card are marked complete.

No repository test runner exists for these prose/YAML contracts in the inspected repository root; no CI/test-suite execution claim is made beyond the exact readback/static checks above.

## Scope boundary preserved

This Card does not implement generic natural-language managed-change triggering/naming/collision behavior (M01-T02), root/bootstrap/default-state cutover (M01-T03), or the full ChatGPT-only/Codex-only lifecycle pointer migrations (M02/M03).
