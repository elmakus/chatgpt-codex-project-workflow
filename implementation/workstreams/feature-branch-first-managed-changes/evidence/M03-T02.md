# M03-T02 implementation evidence

Date: 2026-09-20
Card: `M03-T02 — Migrate Codex-only execution state and historical-default recovery`
Milestone: `M03`
Implementation content verified through: `2e5a779dfaa29ebaf6677fc6ffcd9e069da9e2c2`

## Authority

- `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` — M03
- `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-002..009, REQ-BF-011..014, REQ-BF-016..017 and Codex-only part of REQ-BF-015
- ADR-BF-001, ADR-BF-002, ADR-BF-003
- `openspec/changes/branch-first-m03-codex-lifecycle/`
- M01 GREEN; M02 GREEN migration lessons
- M03-T01 GREEN result/review

## Implemented contract

- `EXECUTION_PREP.md` now requires an exact validated branch-isolated workstream before managed execution mutation and permits only the manifest-selected `WORKSTREAM_TASK_BOARD_TEMPLATE.yaml` as active scaffolding. Root `implementation/TASK_BOARD.yaml` and the policy root template are historical recovery/migration surfaces only.
- `STATE.md` makes the manifest-selected branch-isolated Task Board the only active mutable Codex-only execution context; selecting or mutating root/default as active state is explicitly inconsistent.
- `RECOVERY.md` owns deterministic historical root/default migration-before-mutation. Migration outranks review, Research, batch recovery and execution on the historical board.
- Migration proves exact `integration_target`, exact creation/adoption base and independent-versus-stacked dependency topology before branch creation/adoption using the policy-local Intake classification rules. Ambiguous topology fails closed.
- Migration preserves Codex-specific continuation truth: semantic `implementation_owner_role`, append-only review attempts, implementation/recovery Research pointer, exact results/evidence/dependencies/blockers, and current/referenced M03 batch lineage including frozen base/member order/returned/integrated refs and post-batch review-drain lineage.
- Runtime worker/session/model/profile/invocation/worktree identity remains non-authoritative and is never migrated into durable Project Workflow state.
- The historical source root board remains read-only during migration. Partial migration retries the same deterministic identity/topology; branch readback must prove manifest ↔ Task Board binding, persisted topology and preservation of every live obligation before mutable ownership switches.
- `CLOSE.md` no longer offers active root/default finalization or handoff destinations; historical `project-handoffs/` and root navigation remain readable evidence only.
- Task Card/template surfaces no longer offer root/default as active state. The root Codex Task Board template is explicitly recovery/migration compatibility only.
- `REPOSITORY.md` recovery now uses selected-manifest pre-execution locators plus the selected manifest-bound execution Task Board; historical root Task Board/PROJECT routing pointers are Recovery migration input only.
- OpenSpec M03 now records the execution-state migration, topology-before-branch invariant, Codex review/batch lineage preservation and historical read-only boundary.
- Existing micro-fix, target-refresh, terminal target-side package/source-branch deletion, Codex Main sole-writer, exact-subject Tester and bounded batch/post-batch drain semantics remain unchanged.

## Focused exact-branch verification

Connector-backed assertion replay against exact branch contents is GREEN for 14/14 groups:

1. Execution Prep never scaffolds/selects/mutates root/default for active managed work.
2. State contract requires manifest-bound workstream execution state.
3. Historical migration outranks later roles and keeps source root state read-only.
4. Exact target/base/dependency topology is proven before branch creation/adoption.
5. Codex review ownership and M03 batch/post-batch lineage are preserved during migration.
6. Binding mismatch/historical active selection fail closed.
7. Codex Main, independent Tester and implementation Research ownership remain intact.
8. Serial-default, bounded-batch and post-batch review-drain contracts remain intact.
9. Micro-fix, target refresh and terminal recovery remain intact.
10. Historical artifacts remain readable but inactive.
11. Task/template surfaces do not offer root/default active state.
12. Runtime identity stays outside durable state.
13. Repository recovery uses selected manifest/workstream ownership.
14. Inspected Codex-only lifecycle files contain no `workflow/chatgpt_only/` lifecycle import.

A semantic sweep over the active M03-T02 Codex surface found no remaining active-state wording matching the historical/default/PROJECT ownership patterns removed by this Card.

## Runner status

A clean local checkout plus `python -m unittest discover -s tests -v` was attempted in the sandbox. The checkout could not start because DNS resolution for `github.com` failed. Therefore no Python/CI runner PASS is claimed. The focused verification above is the exact connector-backed assertion replay over the durable GitHub branch contents.

The repository's only visible GitHub workflow is the patch-tag automation, not a test CI workflow for this branch.

## Scope audit

Changed by this Card:
- Codex-only Execution Prep/State/Recovery/Close;
- Codex-only Card/root-board/workstream-board template and repository surfaces needed by the migration boundary;
- Router Recovery read set for Intake topology classification;
- M03 OpenSpec execution-state contract;
- focused Codex branch-first execution-state regression module.

Inspected but not behaviorally changed because already coherent:
- `MICRO_FIX.md`;
- `EXECUTION.md`, `REVIEW.md`, `RESEARCH.md`, `WORKSTREAMS.md` safety/ownership contracts.

Not changed:
- ChatGPT-only lifecycle;
- completed historical/default artifacts;
- bounded-batch scheduling semantics;
- final project/workstream integration/merge;
- M04 repository dogfood/general documentation closure.

## Implementation verdict

GREEN for M03-T02 implementation verification. Freeze the exact evidence-bearing commit as the RECOMMENDED independent-review subject. The Card remains non-terminal until a fresh independent review is GREEN.
