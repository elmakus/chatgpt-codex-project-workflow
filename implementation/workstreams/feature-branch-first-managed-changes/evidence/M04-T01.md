# M04-T01 implementation evidence

Date: 2026-09-20
Card: `M04-T01 — Reconcile repository dogfood, documentation and integrated branch-first regressions`
Milestone: `M04`
Implementation content verified through: `a9bf89e8d27c1547d3d5eb7453862d6ac831a92a`

## Authority

- `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` — M04
- `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-001..017
- ADR-BF-001, ADR-BF-002, ADR-BF-003
- M01 GREEN, M02 GREEN, M03 GREEN
- `implementation/workstreams/feature-branch-first-managed-changes/evidence/M04_JIT_INVENTORY_2026-09-20.md`

## Implemented closure

- `README.md` now presents branch-isolated workstreams as the active managed-change model for the migrated fixed policies. Historical root/default state is migration/recovery input only.
- README documents the natural-language transition from read-only exploration into managed work, keeps `#issue` / `#feature` as optional shortcuts and documents the proportional trivial-change/micro-fix path while retaining branch → PR → merge.
- Root `PROJECT.md` is now an integrated-project index: no active exploratory/Research locators and no active root/default Task Board pointer. Historical root Task Board/handoff entries are explicitly recovery/history navigation only.
- `CHANGELOG.md` records the newer branch-first fixed-policy migration without rewriting older historical entries that described the prior model.
- Added `tests/test_branch_first_integrated_closure_contract.py` to guard README/root PROJECT/template/bootstrap/router/root-default absence semantics.
- Existing `templates/PROJECT.md`, ChatGPT/Codex bootstrap prompts, `workflow/CONTEXT_ROUTING.md`, common authority and migrated fixed-policy Recovery/Execution Prep surfaces remain coherent.
- Mixed/non-migrated legacy routing remains policy-scoped and was not rewritten.

## Exact branch verification

Connector-backed replay against the exact branch contents is GREEN for 19/19 M04 closure assertion groups:

1. stale README active-root/default claims are absent;
2. historical root/default wording is recovery-only;
3. natural-language generic entry is documented;
4. `#issue` / `#feature` remain optional shortcuts;
5. trivial managed changes still use branch → PR → merge;
6. root `PROJECT.md` has no active exploratory/Research/root-Task-Board ownership;
7. root `PROJECT.md` keeps explicit historical/recovery navigation;
8. `templates/PROJECT.md` matches the integrated-project contract;
9. ChatGPT Project Instructions require workstream-before-first-write;
10. ChatGPT start bootstrap requires workstream before bootstrap/adoption writes;
11. Codex-only start bootstrap requires workstream-before-first-write and PR integration;
12. root policy router requires branch-first state for migrated fixed policies;
13. common authority carries only the policy-neutral branch-first invariant;
14. ChatGPT-only Execution Prep forbids new/continued root/default selection;
15. ChatGPT-only Recovery owns historical migration-before-mutation;
16. Codex-only Execution Prep forbids new/continued root/default selection/mutation;
17. Codex-only Recovery owns historical migration-before-mutation;
18. CHANGELOG records the fixed-policy branch-first migration;
19. the new focused regression module matches these closure invariants.

M02/M03 accepted evidence remains the authority for their deeper policy-local regression suites and cross-policy separation. M04 does not reopen those already-reviewed implementation subjects.

## Runner status

A clean local Git read was attempted from the sandbox with `git ls-remote`, but DNS resolution for `github.com` failed. Therefore no new Python/unittest or CI runner PASS is claimed for M04. The exact connector-backed assertion replay above is the verification actually obtained.

## Scope audit

Changed by this Card:
- `README.md`;
- root `PROJECT.md`;
- `CHANGELOG.md`;
- focused integrated closure regression test.

Validated but not behaviorally redesigned:
- root project template;
- ChatGPT/Codex bootstrap prompts;
- root policy routing/common authority;
- migrated fixed-policy Execution Prep/Recovery boundaries.

Deferred to Close:
- current-`main` target refresh/reconciliation;
- workstream final-integration review;
- PR creation/merge;
- target-side terminal package/result reconciliation.

## Implementation verdict

GREEN for M04-T01. Card-level independent review is `none` by contract. The distinct manifest-owned workstream final-integration review remains `RECOMMENDED` and must be frozen only after Close runs the current-target refresh gate.
