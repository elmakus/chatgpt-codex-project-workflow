# M01-T03 implementation evidence

Date: 2026-09-20
Card: `M01-T03 — Reconcile branch-first bootstrap and default-state boundary`
Branch: `feat/branch-first-managed-changes`

## Authority applied

- `planning/BRANCH_FIRST_MANAGED_CHANGES_MASTER_PLAN.md` — M01
- `requirements/BRANCH_FIRST_MANAGED_CHANGES.md` — REQ-BF-001..009, REQ-BF-011, REQ-BF-015..017
- ADR-BF-001..003
- `openspec/changes/branch-first-m01-state-entry/`
- M01-T01 and M01-T02 GREEN dependency state

## Implemented boundary

The M01 target contract now makes the integration target integrated project truth and makes branch isolation precede the first durable change-specific write for branch-first managed changes.

The root/default boundary is reconciled as follows:

- common authority states only the policy-neutral integration-target / branch-before-write / PR-merge invariant and leaves Intake, naming, migration, lifecycle, review and recovery mechanics policy-local;
- root policy routing no longer presents root `implementation/TASK_BOARD.yaml` as a normal fixed-policy mutable fallback;
- ChatGPT-only and Codex-only Router/Workstreams contracts classify historical root/default state as recovery/migration input and require migration to an exact branch-isolated workstream before further managed-change mutation;
- both policy-local repository contracts make bootstrap/adoption branch-before-write and make the workstream manifest the owner of active pre-execution routing locators in the target model;
- `templates/PROJECT.md` is integrated-project navigation only: active exploratory, pre-execution Research and plan-review locators are not root fields;
- ChatGPT/Codex start/bootstrap surfaces describe the branch-first boundary and no longer direct fixed-policy new work to a root default Task Board.

No shared cross-policy Intake/Workstreams/lifecycle module was introduced.

## Focused verification

Static/readback checks are GREEN for:

1. common authority contains integrated-target truth, branch-before-write, branch → pull request → merge, and explicit policy-local mechanics ownership;
2. root context routing treats historical fixed-policy root/default state as recovery/migration input only;
3. `templates/PROJECT.md` contains no `Active exploratory scope` or `Active research obligation` field and records historical/default board/handoff pointers as recovery/history navigation only;
4. root ChatGPT bootstrap, ChatGPT start/project-instructions bootstrap and Codex-only start surface all state the branch-first write boundary;
5. ChatGPT-only Router/Workstreams and Codex-only Router/Workstreams no longer describe legacy/default state as a valid normal mutable fallback;
6. both policy-local Repository contracts make new-project/adoption setup branch-before-write and keep root PROJECT integrated-only;
7. the inspected ChatGPT-only boundary files do not import Codex-only lifecycle modules and the inspected Codex-only boundary files do not import ChatGPT-only lifecycle modules;
8. OpenSpec M01 state-entry tasks are complete.

No CI/status result is claimed for this documentation/contract Card; verification is exact repository readback plus focused static/scenario checks.

## Deferred references intentionally not absorbed

M01 establishes the boundary/state contract; it does not claim end-to-end lifecycle rewiring before M02/M03.

M02 (ChatGPT-only lifecycle) still owns workstream-local replacement of legacy root-pointer/default-context references visible in:
- `workflow/chatgpt_only/ROUTER.md` promotion/pre-execution Research routing;
- `BRAINSTORMING.md`, `RESEARCH.md`, `PLANNING.md`, `PLAN_REVIEW.md`;
- default-context clauses in `STATE.md`, `EXECUTION_PREP.md`, `RECOVERY.md`, and `CLOSE.md`.

M03 (Codex-only lifecycle) still owns the analogous workstream-local lifecycle migration visible in:
- `workflow/codex_only/ROUTER.md`;
- `BRAINSTORMING.md`, `RESEARCH.md`, `DEFINITION.md`, `PLANNING.md`;
- remaining legacy-history/default-context clauses such as `CLOSE.md` and recovery compatibility wording.

M04 owns repository dogfood/documentation closure. The current project instance `PROJECT.md` still contains legacy active-pointer/root-board wording, and `README.md` still describes legacy/default continuation and older intake/root-pointer behavior. Those files were deliberately not normalized by M01-T03.

These residual references are therefore explicit downstream work, not evidence that M01-T03 silently expanded into M02/M03/M04.

## Integration-target movement note

The workstream was created from `main@808084a4c7989715f7ee4889a31bce28f778d597`. Current `main` has since moved; among M01-T03 boundary surfaces, the material concurrent overlap is `workflow/chatgpt_only/ROUTER.md`, where main-side changes are confined to the context-health trigger wording. M01-T03 does not intentionally modify that context-health block. Final target-refresh/reconciliation remains mandatory before workstream integration.

## Result

GREEN implementation verification. M01-T03 is ready to freeze an exact immutable review subject for independent review.
