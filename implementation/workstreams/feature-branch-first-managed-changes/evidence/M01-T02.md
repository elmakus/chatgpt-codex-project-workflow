# M01-T02 implementation evidence

Date: 2026-09-20
Card: `M01-T02 — Add generic natural-language managed-change entry`

## Implemented behavior

- Both fixed-policy routers treat clear natural-language authorization for a **new** managed repository change as an Intake trigger without requiring `#issue` or `#feature`.
- Exact continuation/handoff of an existing workstream/PR/manifest is not reinterpreted as a new generic intake.
- Read-only inspect/compare/analyze remains branch-free until the user authorizes repository/project mutation.
- Generic intake uses neutral `kind: change`, workstream ID `change-<slug>`, and branch `work/<slug>`.
- Existing exact durable identity is recovered rather than duplicated.
- Coherently different durable work uses the smallest available shared numeric suffix for ID and branch.
- A deterministic candidate branch with a missing/malformed/identity-inconsistent manifest fails closed to Recovery instead of being claimed, overwritten, or bypassed with an arbitrary suffix.
- Common Intake ordering now states explicitly that pre-creation steps are read-only and the workstream branch exists before the first change-specific durable Project Workflow artifact is written.
- `#issue` and `#feature` remain explicit shortcuts.
- Mechanics remain separately defined in `workflow/chatgpt_only/*` and `workflow/codex_only/*`; no cross-policy lifecycle import was introduced.
- OpenSpec task for generic natural-language entry / neutral naming / partial-creation recovery is marked complete.

## Verification

Exact branch readback was performed after the contract writes.

Focused static/scenario contract checks: **GREEN**.

1. Both routers contain the natural-language new-managed-change trigger and neutral `kind: change`.
2. Both routers/intake contracts explicitly preserve branch-free read-only inspect/compare/analyze behavior.
3. Both Intake contracts retain `#issue` / `#feature` shortcut semantics.
4. Both Intake and Workstreams contracts contain `change-<slug>` + `work/<slug>`.
5. Both Intake contracts preserve deterministic smallest-suffix collision handling for coherently different durable work.
6. Both policies recover exact valid workstream identity rather than duplicating it.
7. Both policies fail deterministic orphan/inconsistent candidate branches closed to Recovery.
8. Both Intake contracts require branch creation before the first change-specific durable Project Workflow write.
9. Generic Intake in both policies explicitly avoids reclassifying the workstream to issue/feature merely to reuse a lifecycle path.
10. Both policy-local Router bootstrap sequences remain contiguous after the new route was inserted.
11. No policy-local Router/Intake/Workstreams file imports the other fixed-policy namespace.
12. OpenSpec task 3 is complete and remains coherent with the implemented contracts.

Implementation scope from the pre-start execution base is limited to the Task Board start transition, the six contracted Router/Intake/Workstreams files, and the M01 OpenSpec task record.

## Scope boundary preserved

This Card does not perform the full downstream lifecycle-pointer migration owned by M02/M03, nor root/bootstrap/default-state cutover owned by M01-T03, nor documentation/dogfood closure owned by M04.
