# M01-T01 — Make Codex-only orchestration continuous

- Milestone: `M01`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/CODEX_ONLY_CONTINUOUS_ORCHESTRATION_MASTER_PLAN.md#M01 — Continuous Codex Main lifecycle`
- Requirements: `requirements/CODEX_ONLY_CONTINUOUS_ORCHESTRATION.md` COCO-R1…COCO-R7
- Accepted decisions:
  - `decisions/ADR_CODEX_ONLY_CONTINUOUS_MAIN_ORCHESTRATION.md`
  - `decisions/ADR_CODEX_ONLY_RUNTIME_BOUNDARY.md`
- Relevant OpenSpec: none
- Accepted dependency results: none

### Must preserve

- `chatgpt_only` Context Health behavior, file, and regression contract unchanged.
- The `codex_only` user-owned Brainstorming → Project Definition promotion boundary.
- Semantic Executor/Tester independent review, durable review evidence, Task Board/manifest ownership, Refresh/integration gates, and strict Recovery.
- Human/project stops only for unresolved authority, explicit user/deployment/live-write authorization, concrete unremediable runtime/access/input blockers, or end of approved scope.
- Project Workflow owns durable project lifecycle semantics; `codex_workflow` owns concrete worker/session/model/resume/replacement/runtime mechanics.

### Must not / rationale that must travel

- Do not change this repository's accepted execution policy.
- Do not duplicate or fork `codex_workflow` runtime orchestration.
- Do not weaken independent review, durable state/recovery, accepted requirements/decisions, or external-write authorization boundaries.
- Do not treat ordinary fresh-session recovery wording as a coordinator hygiene stop merely because it contains the word “fresh”.

## Dependencies

- none

## Outcome

The active `codex_only` Project Workflow continuously routes every deterministic authorized obligation across role and approved milestone boundaries without a Context Health/FRESH/coordinator-hygiene user stop, while all genuine human/project gates remain intact.

## Scope

### Included

- Remove coordinator Context Health/hygiene-stop routing from `workflow/codex_only/ROUTER.md`.
- Remove `workflow/codex_only/CONTEXT_HEALTH.md`.
- Remove/reword the coordinator refresh-boundary allowance in `workflow/codex_only/PLANNING.md`.
- Reconcile `workflow/codex/CODEX_ORCHESTRATION.md`, `workflow/codex_only/CLOSE.md`, and `workflow/codex_only/RECOVERY.md` only where needed for explicit continuous/recovery semantics.
- Add regression coverage for Router/Planning/Close/Recovery/orchestration and preservation of ChatGPT-only Context Health.
- Update user-facing README/changelog where needed to make the policy distinction discoverable.

### Excluded

- Any semantic change under `workflow/chatgpt_only/*`.
- Changes to `codex_workflow` worker/session mechanics.
- Changes to execution policy, accepted product requirements, or unrelated workflow cleanup.

## Acceptance

- No active `codex_only` workflow file routes to, depends on, schedules, or authorizes a coordinator Context Health/FRESH/hygiene stop.
- `workflow/codex_only/CONTEXT_HEALTH.md` is absent.
- `workflow/codex_only/PLANNING.md` cannot schedule a coordinating-context refresh/hygiene boundary.
- Router explicitly continues deterministic legal obligations across role, internal Tester, Close, and approved next-milestone boundaries without user continuation.
- Brainstorming → Definition promotion, semantic independent review, durable Recovery/state ownership, refresh/integration gates, and narrow true-stop categories remain intact.
- The Project Workflow / `codex_workflow` runtime boundary remains explicit.
- Existing `chatgpt_only` Context Health source and regression behavior remain unchanged.
- Targeted regression checks and the full repository Python test suite are GREEN.

## Required tests / checks

- Dedicated `codex_only` continuous-orchestration static contract test.
- Existing `tests/test_chatgpt_only_context_health_contract.py`.
- Relevant existing codex-only branch-first contract tests.
- Full repository `python -m unittest discover -s tests -p 'test_*.py'`.
- `git diff --check` and scope/readback inspection.

## External write/readback needs

Repository branch writes only; verify persisted GitHub branch content after implementation. No deployment/live-system mutation.

## Independent review

`RECOMMENDED` — this changes lifecycle stop semantics across several authoritative workflow contracts.
