# M02Q-T09 R01 independent discovery review — RF013 recovered-Execution contract

- Task: M02Q-T09, attempt R01 (discovery, card scope, epoch E01).
- Reviewer independence: fresh context; did not produce or repair the frozen Result or the product implementation; re-read durable repo state only.
- Recommended verdict: **GREEN** (subject meets the stable Card; no material defect found).

## Exact subject verified

- Consumer: `elmakus/chatgpt-codex-project-workflow`, branch `work/pwv21-policy-kernel-brainstorming`,
  tracked HEAD `757ba455507c0349b35ef94c5e51ea43bfe4b11f`, remote head identical, Board rev 142,
  `M02Q-T09` `in_progress`, `after-M02Q-T09` `waiting`, `reviews/M02Q-T09-R01.toml` `pending`.
- Frozen Result: `results/M02Q-T09.md@c5492325b5603da280b4acafb20eff5251e853df:ccc4206d4e1cbb244dd40ff88b4b66bfce6ac23d`
  (`Result status: success` present). Blob and commit re-resolved independently.
- Product: `elmakus/project_workflow_v2` branch `work/pwv21-policy-kernel`
  `67c9465687eda4c8ff866f7bc0d1f22505eb71c5`, parent `3e6632191cbcadbd6162c8a23217b2c2f4f394c0`,
  clean tree, remote head identical. Canonical main `4fb4bfb7d7b1481d6f347c182fc96a5a1135e045` untouched;
  canonical authority remains the installed-main plugin.
- All 7 changed blobs match the implementation claim; parent blobs for the fixture,
  `test_router.py`, `test_exact_locator.py` match. Only the 7 listed files changed
  (RF009/RF004 untouched; no candidate-plugin install, merge, or terminal-history rewrite).
- Consumer commits `c549232` (Result + implementation evidence) and `757ba45`
  (Board + review TOML) touch only T09 files. Terminal M01/M02/M02R remain `done`.
  Untracked consumer trajectory JSONs preserved and excluded.

## Falsification attempts (all remain unfalsified)

Independent probes (`/tmp/r01_falsify.py`, `/tmp/r01_parent_red.py`; product tree never dirtied):

- H023: `FAILED: acceptance test failed` with status success/failed/blocked/missing is never
  accepted success; none route `result_reconciliation`. Contradictory `success` + `FAILED:` prefix
  (exact, lowercase, padded) fails closed for both `review none` and `review required`.
  Invalid statuses (`GREEN`, `Success`, `successful`, `success!`, `none`, `green`) raise
  `invalid result status` and route Recovery. GREEN/success substrings without `success` status
  authorize nothing. H023 unfalsified.
- H024: minimal 2-line Card is rejected by `parse_task_card` and routes Recovery with a `task_card`
  reason both without result and with an accepted-success Result; missing-stable-field Card routes
  Recovery; valid Card without result routes `execution`. H024 unfalsified.
- Positives: valid Card + accepted success reconciles without replay (`review none`, reason carries
  `do not replay`) and freezes review (`review required`). Consumer frozen Result parses under the
  production parser as accepted success (`result_status == success`, summary has no FAILED prefix).
- RF007: dangling identity, mutated bytes, and backslash evidence-alias Results fail closed even with
  `success` status; `read_verified_card_result` + `normalize_locator_path` remain on the active path.
  No path-only or free-text success inference exists; the only summary check is the fail-closed
  `FAILED:` contradiction prefix (stripped, case-insensitive, prefix-only).

## Bounded FAILED-prefix gate assessment

Explored bypass shapes with `success` status: `ERROR:`, `FAIL:`, `RED:`, `FAILED` without colon,
`Test failed`, `failure`, mid-string `FAILED:` — all are accepted and reconcile. This is **not**
a material bypass: the Card makes structured status authoritative, gives the status schema bounded
latitude, and forbids substring/presence-only *success* inference; no accepted authority
(REQ-018..033/068/070/092, P7 RF013 row, Card acceptance) defines those wordings as normative
failure markers or requires free-text failure inference. Broadening the gate to arbitrary substrings
would itself violate the no-substring-inference rule. The bounded `FAILED:` contradiction gate matches
the exact probe shape, is documented in `workflow/EXECUTION.md`, and is disclosed in the frozen Result.

## RED-before / GREEN-after and regression proof

- Parent `3e66321`: no `CARD_RESULT_SUCCESS`/`is_accepted_success`/`FAILED_SUMMARY_PREFIX`;
  router active branch only reads the Card and parses it after result handling with no success gate.
  Behavioral RED reproduced in an isolated parent archive: minimal fixture routes `route/execution`
  while `parse_task_card` rejects it; 4-field `FAILED:` Result routes `route/result_reconciliation`.
- HEAD: focused `tests.test_recovered_execution_contract` 23/23 pass (7 predicate, 9 H023, 4 H024,
  3 RF007); full clean-tree `sh scripts/test.sh` passes 988/988 plus all 8 shell gates (exit 0,
  tree clean afterwards). Remote branch and exact-head CI `36220046880` (push) and `36220050581`
  (PR) are both `success` on `67c9465`.

## Residual risk and limits

- Failure semantics for non-normative free-text remain status-authoritative by design; a Worker that
  writes `success` with a misleading summary other than `FAILED:`-prefixed is a Worker-correctness
  issue, not a router defect under this Card.
- RF009, M02Q composition/Milestone Review, and M03 remain downstream; this review certifies only
  the RF013 seam. Evidence left uncommitted for Main; no product, Result, Board, TOML, or history edits made.
