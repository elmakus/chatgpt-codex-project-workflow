# M02 Milestone Review R01 evidence

Date: 2026-09-24
Workstream: `change-pwv21-policy-kernel-brainstorming`
Attempt: `M02-MILESTONE-R01`
Review subject: `elmakus/chatgpt-codex-project-workflow@9c6148044fa140319dc5fc38283ef5665904bc03:implementation/workstreams/change-pwv21-policy-kernel-brainstorming/results/M02-T01.md@d3cca7d34e8ea08e33fa7d2026d9ffbe84cee176`
Implementation subject named by the result: `elmakus/project_workflow_v2@af8610dda5b8efa9be5083bad1dac5b6ec596003`
Acceptance surface: `planning/PWV21_POLICY_KERNEL_MASTER_PLAN_P2.md`, M02 acceptance and PWV21-REQ-018…PWV21-REQ-033.

## Verdict

RED.

The exact M02 implementation is bounded to the accepted M02 surface and the raw GitHub Actions run #379 / ID 35942705073 is GREEN (13/13 dedicated M02 tests and 185/185 full discovered suite), but one acceptance defect remains in the governed mutation/readback contract.

## Blocking finding — M02-MR01-F01

PWV21-REQ-031 requires expected canonical mutation preconditions/postconditions followed by mandatory readback/validation. In `tools/obligation_contract.py`, `_verify_conditions()` currently validates a condition with ordinary Python equality:

`actual == condition["equals"]`

That is not type-exact for JSON/canonical state. In Python, `True == 1` and `False == 0`. Therefore an expected typed canonical postcondition such as integer revision `1` can incorrectly verify against boolean `true`, and the same defect applies to mutation preconditions.

This permits mandatory governed readback to return `verified` for a state that does not exactly satisfy the typed canonical mutation contract. The current M02 tests exercise integer mismatch but do not cover boolean/integer type confusion.

## Bounded correction required

Stay entirely inside M02:
- make mutation condition equality canonical/type-exact rather than Python-coercive;
- add negative regression coverage proving boolean/integer mismatches fail for both precondition and postcondition verification;
- rerun the dedicated M02 gate and full repository suite.

No M03 materialization or semantics are required.

## Other reviewed acceptance surface

Direct inspection covered the versioned Obligation/Result schemas, exact authority materialization, deterministic identity/freshness, stale-result reconciliation, telemetry exclusion, kernel seams, unknown-effect retry/readback behavior, the accepted M01→M02 14-file delta, and raw CI for the exact implementation subject. No additional blocking M02 finding was identified in this review.
