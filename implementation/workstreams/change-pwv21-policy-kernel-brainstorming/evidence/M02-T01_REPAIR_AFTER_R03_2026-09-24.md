# M02-T01 bounded correction after R03 — 2026-09-24

## Trigger

Independent review `M02-T01-R03` returned RED for a bounded M02 acceptance defect: the checked-in JSON Schemas were not exercised as real Draft 2020-12 contracts and diverged from the executable validator on exact-ref path safety and recursively prohibited telemetry keys.

## Bounded correction

Target repository: `elmakus/project_workflow_v2`  
Target branch: `work/pwv21-policy-kernel`  
Pre-correction subject: `593b1a350ff72e3fcfec6a1dde10e9b564fb827c`  
Repaired subject: `0b6fed7268747b772dc867aa6ae1037c6055ff61`

Correction stayed inside M02 and changed only M02 contract/testing support:

- aligned Obligation exact-ref path schema with executable relative/no-parent-path safety;
- aligned Result exact-ref path schema with the same safety rule;
- made nested telemetry rejection recursive in Obligation freshness inputs and mutation condition values;
- aligned nonblank semantic string constraints and blocked-result blocker requirement where the executable validator already enforced them;
- added a real Draft 2020-12 validation matrix using `jsonschema`, validating both golden fixtures and rejecting invalid breaking-version/path/nested-telemetry/blocked-result/blank-semantic cases;
- declared and installed the test-only JSON Schema dependency in CI;
- made the dedicated M02 contract gate fail clearly when that test dependency is absent.

No M03 artifact or behavior was added.

## Exact range readback

The repaired target branch read back at exact HEAD `0b6fed7268747b772dc867aa6ae1037c6055ff61`. The full accepted M01-base→repaired-M02 range is ahead by 31 commits with merge base exactly `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`. The range remains bounded to 14 files: the prior 12 M02 files plus `.github/workflows/test.yml` and `requirements-test.txt` required to execute the new schema validator in CI.

## Raw CI/readback evidence

GitHub Actions run #373 / ID `35940102673`, job `107445903522`:

- checked out exact repaired SHA `0b6fed7268747b772dc867aa6ae1037c6055ff61`;
- installed `jsonschema==4.26.0` from `requirements-test.txt`;
- ran `sh scripts/test.sh`;
- new `test_json_schemas_validate_golden_and_reject_invalid_matrix` passed;
- dedicated M02 contract suite: 12 tests, all GREEN;
- policy-kernel regression: 11 tests, all GREEN;
- router suite: 39 tests, all GREEN;
- execution/review/recovery gates GREEN;
- regression group: 80 tests, all GREEN;
- full discovered suite: 184 tests, all GREEN;
- final `M01 baseline checks: PASS`;
- workflow conclusion: `success`.

## Review consequence

The corrected implementation/result subject differs from the R03 subject. Because this context materially performed the bounded correction and will persist the repaired Result, it is ineligible to issue the independent verdict on that new subject. A new append-only pending attempt `M02-T01-R04` must be reviewed by a fresh independent context before M02 can return to DONE. The `after-M02-T01` trigger must remain `waiting`.
