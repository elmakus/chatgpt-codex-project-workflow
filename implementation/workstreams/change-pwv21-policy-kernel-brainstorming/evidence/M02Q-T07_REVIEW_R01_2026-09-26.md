# M02Q-T07 R01 independent Card review evidence — 2026-09-26

- Reviewer: fresh independent tester; did not implement/repair RF016. Discovery complete.
- Consumer: `30f90c14fd59d34bb0d331d75592e3fa455a4bc6`, Board rev 134, Card `cards/M02Q-T07.md`.
- Subject binding verified: `results/M02Q-T07.md@1e3200879b72bba86dda966f486a443013f94366:c8492c2a653fe599e271e0c51220f0fd8f642474` via `git ls-tree` + `hash-object`; attempt `reviews/M02Q-T07-R01.toml` pending, acceptance = Card path, independence basis recorded. Not edited.
- Product: `elmakus/project_workflow_v2@143d9dd2ecea7a8c4174fed60fa447cf2405086a`, single parent `95b7c838f4e0e39464252d20036b1dd003039d31`; `origin/work/pwv21-policy-kernel` reads back exact SHA; `origin/main=4fb4bfb7…`, impl not ancestor (no merge). 6-file diff only; `workflow/ROUTER.md` bytes unchanged (blob `3b3ac601…` both sides). All 6 changed blobs match implementation evidence. Consumer plugin stays `pw 0.1.0` (own repo), product `pw 0.2.1` — no candidate install.
- H028 RED-before (parent hook replay, independent `/tmp/m02qt07_review_probes.py`): two-marker-only, contradictory, truncated all ENABLED. GREEN-after (new hook): all three BLOCK with `failed integrity verification`; untouched package ENABLED with startup==resume==compaction parity (353 chars, ≤900).
- Fail-closed: missing/malformed manifest siblings (empty, null, bool/float version, uppercase/short/nonhex hex, oversized >4KiB, plus impl's 8), blob mismatch, missing/garbage/non-UTF8 router all BLOCK; router+manifest symlink escape BLOCKS `outside`; PLUGIN_ROOT mismatch BLOCKS; absent PLUGIN_ROOT ENABLES via script_root. JSON whitespace-only manifest variant ENABLES (correct).
- Thin/offline/no-policy: hook imports only hashlib/json/os/pathlib; no URLs, glob, subprocess, policy tokens; reads only `workflow/ROUTER.md` + `.codex-plugin/router-integrity.json`; wording is bootstrap-not-policy with no remote fetch. Manifest 103B, exact shape, no URLs/pin tokens, `git_blob_sha1` equals `git hash-object` and recomputed SHA-1.
- Focused suites: `test_session_router_integrity(12)+test_codex_delivery(9)+test_chatgpt_delivery(4)=25/25` OK; `sh scripts/test-plugin-probe.sh` PASS. `25/25` reconciles only with all three suites (evidence wording imprecise, not material). Full 949 suite not rerun (no concrete risk; small diff, focused green). CI exact-head push 36208658959 + PR 36208662530 both `success` on impl SHA (gh readback).
- Identity assessment: manifest mechanism genuinely satisfies Card-boundary exact release/Git identity for single-sided corruption (any router-only or manifest-only edit BLOCKS). Consistent co-edit of router+manifest ENABLES by design (probed) — package self-consistency, not signed/adversarial integrity. Card requires no signing; residual co-edit risk disclosed, not a Card failure. Manifest must be regenerated with each legal router update or fail-closed triggers (documented).
- Verdict recommendation: GREEN, no blocking findings.
