# M01-T01 bounded repair evidence after R01

Date: 2026-09-24
Card: `M01-T01`
Prior review: `R01` RED
Target repository: `elmakus/project_workflow_v2`
Target branch: `work/pwv21-policy-kernel`
Repaired implementation subject: `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`

## Correction

The repair stays within accepted M01 scope and addresses the exact R01 finding.

- `tools/router.py` now evaluates registered rules with `PolicyKernel.route()` and consumes the returned `MechanicalDecision.disposition`, `obligation`, and `owner_module` instead of duplicating those registered outcome fields in production routing branches.
- Recovery-disposition registered rules preserve the required Recovery-module read while still consuming the registry-owned outcome.
- `tests/test_policy_kernel.py` now verifies one production `kernel.route()` wiring per registered rule, checks that production wiring order follows registry precedence, and verifies that the production adapter consumes registered outcome fields.

No M02-M07 semantics were implemented.

## Verification

GitHub Actions workflow `test`, run #342 / ID `35934022918`, completed successfully for exact head SHA `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`.

The final branch head and changed artifacts were read back after the repair. The prior intermediate run #341 failed before the regression test update and is superseded by the exact final repair subject above.

Repair evidence: GREEN. A new independent review attempt is still required for the repaired exact result subject.
