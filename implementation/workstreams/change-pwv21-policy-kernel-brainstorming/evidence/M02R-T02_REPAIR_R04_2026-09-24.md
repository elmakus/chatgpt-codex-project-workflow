# M02R-T02 bounded repair evidence — R04

Date: 2026-09-24
Card: `M02R-T02`
Source review: `implementation/workstreams/change-pwv21-policy-kernel-brainstorming/evidence/M02R-T02_REVIEW_R04_2026-09-24.md`
Repaired implementation subject: `elmakus/project_workflow_v2@c4287d38541cccfa125c5dbe69bffdac3c26b051`
Repair range: `159aefa9d98af3d03268fe48a4afd64de7bc2267...c4287d38541cccfa125c5dbe69bffdac3c26b051`

## R04 F1 — exact accepted-redesign readback

The epoch-reset path remains restricted to the exact Task Card acceptance surface or one authority path accepted by the Card, and now additionally requires exact repository readback before an epoch may change.

- `tools/state_contract.py` accepts an epoch change only with an `exact_blob_reader`.
- The claimed `epoch_reset_subject.commit/path/blob` must resolve to the claimed blob.
- That same blob must be the accepted content at the new reviewed subject commit.
- The accepted content at that path must differ from the immediately preceding epoch's reviewed subject, so unchanged authority/acceptance content cannot manufacture a reset.
- `tools/router.py` supplies the readback from the selected project's own Git repository and fails closed when the commit/path/blob cannot be resolved as a blob.
- Focused state tests cover fabricated/nonexistent readback, valid exact accepted redesign, unchanged-content rejection, foreign repository, arbitrary authority path and accepted-authority path.
- Router coverage proves a syntactically valid fabricated exact Task Card subject routes Recovery instead of resetting the epoch.

Exact readback blobs at the repaired implementation head:
- `tools/state_contract.py@a870bb59d73e174cd29a842a50e65fe23ca4e9e5`
- `tools/router.py@5c9faa49485b26ccb3d2ad84b873e3496cbde42f`
- `tests/test_state_contract.py@d9e9a1aef3e3a337ae0ecc930ea06d8b638f46c1`
- `tests/test_router.py@6261e2ac9f4d1e37feb7aa09faa00f0d005b4b67`

## R04 F2 — exact failed classes for multi-class RED closure

RED closure-verification now separates classes being checked from classes that actually remained blocking.

- `failed_material_defect_class_ids` records the non-empty subset of `material_defect_class_ids` that failed a RED closure.
- Discovery and non-RED closure attempts cannot claim failed classes.
- A RED closure must record at least one failed class and every failed class must be inside the verified class set.
- `review_convergence_state` increments failed repair→closure rounds only for that failed subset while preserving the existing subject-transition, source-content, unchanged-content, commit-only and cyclic S1→S2→S1 rules.
- A focused multi-class regression proves a RED caused by `class-b` increments `class-b` only and leaves successfully verified `class-a` untouched.
- Template, state documentation and active V1-migration projection carry the new field without introducing runtime identity.

Exact readback blobs at the repaired implementation head:
- `tools/review_contract.py@c13c1c119ecf00a566d73b47b92ab0f66ef2276e`
- `tests/test_review_contract.py@91afa42b75725e609831872035ea3315d85f7359`

## Exact-head verification

Candidate branch `work/pwv21-policy-kernel` reads back exactly at `c4287d38541cccfa125c5dbe69bffdac3c26b051`.

GitHub Actions on that exact SHA:
- push run `36038350898`: completed / success
- pull-request run `36038358542`: completed / success

The R04 repair is bounded to M02R-T02 review/convergence semantics and regression coverage. It does not introduce observation/disposition semantics or BOOT-B/C/D scope.
