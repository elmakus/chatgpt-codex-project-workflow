# M02Q-T04 RF017 implementation evidence

- Scope and subject: `elmakus/project_workflow_v2` `work/pwv21-policy-kernel@8c3c751751e5d954f2aba2aa3b7b9b6d54f5fc30`, parent `b47e1bc88e0497d2828ece4b873b0440a86a2ad1`. Canonical `main` stayed at `4fb4bfb7d7b1481d6f347c182fc96a5a1135e045`. Only two RF017-local files changed: `tools/state_contract.py@7838a6662a9eac37afd924d5bf57e38d5dc40e75` and `tests/test_router.py@c8aab3d6b31edfddc1060517a873b98a73847cb6`; no other seam, historical record or fixture was rewritten.
- Correction: `read_toml` normalizes malformed durable TOML to `ValidationError` with path context. `read_project` normalizes malformed TOML front matter and invalid UTF-8 input likewise. Existing selector Recovery boundaries consume `ValidationError`, yielding `recovery/recovery_boundary` without parser exception escape. Valid records and parseable semantic-invalid records retain their separate routes.
- RED-before/GREEN-after: disposable probes on exact parent reproduced uncaught `tomllib.TOMLDecodeError` for malformed `WORKSTREAM.toml` and `TASK_BOARD.toml`; new maintained malformed-surface regressions errored without the correction. On the exact committed subject, five new RF017 tests passed: malformed Workstream/Board, PROJECT.md front matter, representative intake/research/tracker/planning/board-research/review/blocker siblings, and valid plus semantic-invalid controls. Malformed inputs returned Recovery with path context; valid control routed to execution, while parseable-invalid control recovered through semantic validation.
- Verification: affected router/state/recovery suites passed 112/112. Complete clean-tree `sh scripts/test.sh` passed 922/922 discovered tests and all shell gates with exit 0. `git diff --check` was clean before commit; after commit the worktree was clean and the remote candidate read back the exact implementation SHA. The two exact-head GitHub Actions runs [36144285617](https://github.com/elmakus/project_workflow_v2/actions/runs/36144285617) (push) and [36144290601](https://github.com/elmakus/project_workflow_v2/actions/runs/36144290601) (PR) both completed successfully on `8c3c751751e5d954f2aba2aa3b7b9b6d54f5fc30`.
- Limits: representative selector-owned TOML surfaces were exercised directly; remaining readers share the same normalized read boundary and were inspected by code path. Exact independent Card review is still required before terminal Card status. RF002 and later RF seams and the separate M02Q Milestone Review are not claimed complete here.
