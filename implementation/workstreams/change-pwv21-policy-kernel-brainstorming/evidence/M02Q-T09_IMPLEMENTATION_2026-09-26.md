# M02Q-T09 RF013 recovered-Execution contract implementation evidence

- Scope and subject: `elmakus/project_workflow_v2` `work/pwv21-policy-kernel@67c9465687eda4c8ff866f7bc0d1f22505eb71c5`, parent `3e6632191cbcadbd6162c8a23217b2c2f4f394c0`. Canonical main remained separate at `4fb4bfb7d7b1481d6f347c182fc96a5a1135e045`; canonical workflow authority remains the installed main plugin, not the candidate. Changed blobs: `tools/execution_contract.py@949a336dde6b7b6489fd44d0da7ec125933780a8`, `tools/router.py@f1d401a94f51b2d9787f30524d3d9d36ee5db920`, `workflow/EXECUTION.md@12650059d862b2b33e4e7784ada91f9507d82fd2`, `tests/test_recovered_execution_contract.py@1d55b1ed6e9ee66d61f14918fddf970e0b84d242` (new), `tests/test_router.py@7953e47e40032fec220889085002a83ef22eb55f`, `tests/test_exact_locator.py@ac6849f941a5a7100a8a56aa3f13163846d2e203`, `tests/fixtures/router/valid-project/implementation/workstreams/sample-workstream/cards/M01-T04.md@03c3af90b0ac93b9955604cd7f88ee8b6d6b7974`. The fixture Card changed from parent blob `dcda983d1cbcef527c7d4bc6f900133c13a7e3a5` (minimal 2-line, rejected by `parse_task_card`) to a full stable 9-field Card; `tests/test_router.py` changed from parent blob `1c0bd106eba50a1c50117b4e5a70533beac5c70e` and `tests/test_exact_locator.py` from `be092bec8ce63b76709794e9f3619439cbfdb9ae` by adding the required `Result status: success` line to accepted-success fixtures.
- Correction: Recovered Execution validates the full stable Card contract first: the router active branch reads the Card and calls `parse_task_card` before any result handling, in agreement with the stable Card parser, so an active Card that `parse_task_card` rejects fails closed to Recovery even when an accepted-success Result locator is present. Execution Results carry structured semantic status `success`, `failed`, or `blocked` with exact accepted-success predicate `result_status == success and not FAILED-prefix(summary)`; only accepted success can authorize result reconciliation, review freeze, or no-replay recovery. The free-text Tests/readback summary, evidence presence, and implementation subject never authorize by presence, non-emptiness, or substring, and no path-only or free-text success inference exists. The contradiction gate is bounded to the exact normative `FAILED:` Tests/readback summary prefix only (stripped, case-insensitive, prefix-only): a `success` status co-edited with that prefix is contradictory and fails closed for both reconciliation and review freeze, while any other free-text without the prefix is not failure-inferred and GREEN/success substrings authorize nothing. Legacy 4-field Results without structured status still parse with `result_status None` for history inspection but are never accepted success. Locator proof reuses the DONE/GREEN RF007 resolver: `read_verified_card_result` commit:blob identity plus evidence readback and `normalize_locator_path` confinement, so dangling, mutated-bytes, and backslash-evidence-alias Results fail closed even with `success` status. H023 FAILED-summary, `failed`/`blocked`/`missing`/`invalid`-status, GREEN-without-status, and contradictory-success fixtures fail closed; invalid status values (`GREEN`, `Success`, `successful`, `success!`, `none`, `green`) raise `invalid result status`. H024 minimal-card without result, minimal-card with success result, and missing-stable-field Cards fail closed with a `task_card` reason, while a valid stable Card without result routes `execution` and a valid Card with accepted success reconciles without replay (`review none`) or freezes review (`review required`). Accepted authority is REQ-018..033/068/070/092 with REQ-097..102/125 historical preservation.
- RED-before/GREEN-after: on the exact parent there is no `CARD_RESULT_SUCCESS`, `CARD_RESULT_STATUSES`, `FAILED_SUMMARY_PREFIX`, or `is_accepted_success` predicate (`git show` proves absence); `parse_card_result` accepts a `FAILED: acceptance test failed` summary as a valid non-empty 4-field Result, and the router active branch only reads the Card while parsing it solely inside the result branch after result handling, with no success gate. Behaviorally RED: the parent minimal 2-line fixture Card routes `route/execution M01-T04` while `parse_task_card` rejects the same bytes, and a valid Card with a 4-field `FAILED:` Result and review `none` routes `route/result_reconciliation`. After the fix the same H023 shapes route `recovery/recovery_boundary` with `not accepted success` (or `invalid result status`), accepted success with review `none` reconciles without replay and with review `required` freezes review, contradictory `success` plus `FAILED:` prefix fails closed for both obligations, H024 minimal/missing-field Cards fail closed to Recovery, and the valid-Card/accepted-success positive continues deterministically with RF007 locator proof intact.
- Verification: twenty-three new test methods in `tests/test_recovered_execution_contract.py` passed (7 accepted-success predicate, 9 H023 reconciliation/review-freeze, 4 H024 Card validation, 3 RF007-locator-with-success). Focused `tests.test_recovered_execution_contract` 23/23 and complete clean-tree `sh scripts/test.sh` passed 988/988 discovered tests and all 8 shell gates (plugin-probe, state-envelope, policy-kernel, obligation-contract, router, execution-contract, review-contract, recovery-contract) with exit 0 and a clean tracked tree afterwards. The remote product branch read back the exact implementation SHA. Exact-head GitHub Actions [36220046880](https://github.com/elmakus/project_workflow_v2/actions/runs/36220046880) (push) and [36220050581](https://github.com/elmakus/project_workflow_v2/actions/runs/36220050581) (PR) both completed successfully on `67c9465687eda4c8ff866f7bc0d1f22505eb71c5`. Consumer readback: branch `work/pwv21-policy-kernel-brainstorming` at `1e35dd408f030b243523fb8cd3419d2697b2e2d3` matching remote, Board revision 141 with `M02Q-T09` `in_progress` and `after-M02Q-T09` `waiting`, Card `M02Q-T09.md` acceptance matched, and the new consumer Result parses under the production parser as accepted success.
- Limits: RF009 Research provenance remains the separate downstream Card; the composed M02Q seam and fresh M02Q Milestone Review still require their own downstream evidence, and M03 stays unmaterialized until all 17 M02Q repair families and Milestone Review are terminal GREEN. Correctly terminal M01/M02/M02R history is unchanged. No candidate-plugin install and no main merge/rebase were performed. The contradiction gate covers only the normative `FAILED:` summary prefix, not arbitrary free-text failure inference. Board status, review attempts, and JIT state are unchanged; this Result is frozen as the immutable subject and fresh independent Card review is required before M02Q-T09 becomes DONE.
