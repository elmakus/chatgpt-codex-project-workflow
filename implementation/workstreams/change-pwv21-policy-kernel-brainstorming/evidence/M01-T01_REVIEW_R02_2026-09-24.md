# M01-T01 independent review evidence — R02

Date: 2026-09-24
Card: `M01-T01`
Attempt: `R02`
Exact result subject: `elmakus/chatgpt-codex-project-workflow@3e1ce1012db0a12cf3942e0a27b0a9a45873984f:implementation/workstreams/change-pwv21-policy-kernel-brainstorming/results/M01-T01.md@f077104c3918c06c62da97cfdb13d4c4d068f5d9`
Implementation subject under review: `elmakus/project_workflow_v2@a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`
Independence: this review context did not materially produce or repair the exact result subject or implementation subject.

## Authority and acceptance checked

- Stable Task Card `M01-T01`.
- Approved P2 M01 policy-kernel scope and acceptance.
- `PWV21-REQ-001` through `PWV21-REQ-010`.
- Accepted `ADR-PWV21-001`.

## Exact implementation review

The complete M01 change range from baseline `986affffb7ba816e260e48549bf56e198ed51c21` to repaired subject `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02` was checked across the registry, schema, policy kernel, production router integration, generated projection, policy-kernel tests, router regressions, and repository test entrypoint.

The R01 defect is corrected on the repaired subject:

- production registered routes call `PolicyKernel.route()`;
- `policy_result()` consumes the returned `MechanicalDecision.disposition`, `obligation`, and `owner_module`;
- all 12 registered rules have exactly one production `kernel.route()` wiring and source order follows registry precedence;
- outcome metadata is therefore no longer independently duplicated by those registered production branches.

The remaining M01 acceptance surface is present:

- the registry is versioned, deliberately small, and closed to arbitrary-expression vocabulary;
- every predicate is bound to declared canonical dotted input paths, and predicate evaluation receives only those extracted inputs;
- registry and implementation predicate vocabularies must match exactly;
- kernel evaluation is stateless/read-only and deterministic for identical inputs;
- the human-readable policy projection is generated from the registry and exact projection drift fails closed;
- registry-driven contract coverage is enumerated from registered rules rather than a separately maintained predicate list;
- M02-owned Obligation/Result/reconciliation seams are declared but deliberately reject premature use.

## Raw verification

GitHub Actions workflow `test`, run #342 / ID `35934022918`, was independently read back:

- head SHA: `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`;
- status: `completed`;
- conclusion: `success`;
- job `test`: `success`;
- step `Run repository checks`: `success`.

The checked `scripts/test.sh` executes the policy-kernel checks plus router, state, execution, review, recovery, close, delivery, migration, unittest discovery, Python compile, diff-check, and repository-cleanliness gates.

## Findings

No blocking acceptance defect found on the exact repaired subject.

Verdict: GREEN
