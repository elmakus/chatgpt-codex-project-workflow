# M02-T01 bounded correction after R04 — 2026-09-24

## Trigger

Fresh independent review `M02-T01-R04` returned RED for two bounded M02 contract defects in the exact implementation subject `0b6fed7268747b772dc867aa6ae1037c6055ff61`:

1. canonical JSON serialization permitted Python non-finite float tokens such as `NaN`/`Infinity`;
2. the Draft 2020-12 Obligation schema remained weaker than executable validation for case-variant telemetry keys and whitespace-only freshness prerequisite/constraint strings.

## Bounded correction

Target repository: `elmakus/project_workflow_v2`  
Target branch: `work/pwv21-policy-kernel`  
Pre-correction subject: `0b6fed7268747b772dc867aa6ae1037c6055ff61`  
Repaired subject: `902c067e24f4def69d872f509f476ba225a2c938`

Correction stayed entirely inside M02:

- `canonical_json()` now uses strict standard-JSON serialization with `allow_nan=False` and converts non-finite/non-serializable values into the typed `ExecutionEnvelopeError`;
- the Obligation schema now rejects the prohibited telemetry vocabulary case-insensitively at arbitrary nested JSON positions;
- freshness-material prerequisite/constraint strings now carry the same non-whitespace rule as the executable validator and duplicated top-level surface;
- the real Draft 2020-12 invalid matrix now covers case-variant telemetry plus blank freshness prerequisite/constraint entries;
- executable negative coverage now checks non-finite determining inputs and case-variant telemetry;
- the human-readable execution-contract document states the strict JSON transport rule.

No M03-M07 artifact or behavior was added.

## Exact range readback

The repaired target branch read back at exact HEAD `902c067e24f4def69d872f509f476ba225a2c938`.

Compared with accepted M01 base `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`:
- status: ahead;
- ahead by: 35 commits;
- behind by: 0;
- merge base: exactly the accepted M01 base;
- changed files: exactly 14, the same bounded M02 contract/schema/test/documentation/CI-support surface as before the R04 correction.

## Raw CI/readback evidence

GitHub Actions workflow `test`, run #377 / ID `35941549628`, exact `head_sha=902c067e24f4def69d872f509f476ba225a2c938`, event `push`:
- status: `completed`;
- conclusion: `success`;
- job `test` ID `107450294122`: `success`;
- `test_canonical_json_rejects_non_finite_determining_inputs`: GREEN;
- `test_json_schemas_validate_golden_and_reject_invalid_matrix`: GREEN;
- dedicated M02 typed-contract suite: 13 tests, all GREEN;
- policy-kernel regression: 11 tests, all GREEN;
- router suite: 39 tests, all GREEN;
- regression group: 80 tests, all GREEN;
- full discovered suite: 185 tests, all GREEN;
- final `M01 baseline checks: PASS`.

## Review consequence

This correction changes the implementation subject and therefore requires a new immutable Result subject and a new append-only independent review attempt. This context materially performed the correction and is not eligible to issue the independent verdict for the repaired subject. The next attempt must remain pending for a fresh independent reviewer. The `after-M02-T01` trigger remains `waiting`.
