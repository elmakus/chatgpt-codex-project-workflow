# M02R-T05 independent review evidence — R01

Date: 2026-09-25. Verdict: **RED**. The fresh independent review context did not produce or repair the exact result or product subject.

## Exact binding and verification

- Consumer freeze `elmakus/chatgpt-codex-project-workflow@9e52242882e5461490510d4cb1dcdb7991c84e83`, Task Board revision 59, pending R01. Reviewed result `results/M02R-T05.md@fb17228ae45e03d0ebc10f4b8ef4f18a75c23d38:ae1f1470cd64168dcedc8336087d57ef2078d0e6`; stable Card blob `dd24ffb64ecf86495e09ae8e41ebf3ab8b3315fb`. All Board/result/attempt bindings matched.
- Product `elmakus/project_workflow_v2@e39d8b32eff974a6962d8cde8368d1a962b2f934`, parent `f7353f440a0a37ccf2a8ee9e47856cd6c7b7f541`; all eight changed code/test/contract blobs matched the implementation evidence. Exact-head CI runs 36072967747 and 36072963930 both succeeded. Independent scratch checkout `sh scripts/test.sh` passed 326 discovered tests and M01 baseline.
- Twenty-three of twenty-nine independent probes passed, including whole-audit omission recovery, seven-dimension completeness, generic rebuttal rejection, micro-Card rejection, structural-boundary non-authority, historical M02 four-outcome mega-Card rejection when honestly declared as one, and DONE/blocked compatibility. No historical M02 Board/review artifact changed.

## Load-bearing finding F1 — split decision without split topology

`tools/card_sizing_contract.py` accepts a `sizing_audits` record with `decision = "split"` and two honestly declared separable outcomes when its `card_id` names the **only** materialized Card for that candidate scope. `validate_sizing_audits` checks that the id exists and is unique but does not bind the split outcomes to a second Card or a durable downstream JIT boundary. The real router then returns `execution` for that lone active Card. This lets a whole-scope mega-Card claim a split in its audit without actually splitting or supplying the only allowed atomicity/coupling rebuttal.

Reproducer on exact product head: construct a valid Board with one `in_progress` Card and one sizing audit bound to it; give the audit two outcomes with `independently_verifiable = true`, `independently_useful = true`, `falsifiable = true`, `substantial = true`, distinct semantic families, all seven dimensions, `decision = "split"`, empty rebuttal. `validate_board` passes and `select_route` yields `route/execution`. Independent probes `P11` and router probe `R2` are at `/tmp/t05r01_probes.py` and `/tmp/t05r01_router_probes.py` (scratch evidence). Expected: reject the materialized one-Card topology or require a durable, non-speculative mapping to actual separate Card/JIT boundaries before launch. This is within REQ-128/129 and the T05 acceptance that separable outcomes normally split; REQ-119..121 challenge is a later second-order safeguard, not a substitute for the primary decision rule.

## Other boundaries

- Self-declared outcome flags and free-text technical rationale need semantic Execution Prep judgment; the product evidence discloses this and later topology challenge will inspect risky cases. Independent probes confirmed this limitation but did not classify it as a separate T05 defect.
- REQ-119..121, REQ-130, BOOT-C/D remain outside the reviewed implementation. The F1 correction must not pull them into T05.

R01 remains terminal RED for this exact immutable subject. Correct the defect class, create a new result subject, and freeze a fresh independent attempt after clean exact-head verification.
