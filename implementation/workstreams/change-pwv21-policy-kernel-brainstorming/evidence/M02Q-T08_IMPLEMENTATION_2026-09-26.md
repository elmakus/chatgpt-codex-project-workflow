# M02Q-T08 RF012 Definition-authority key implementation evidence

- Scope and subject: `elmakus/project_workflow_v2` `work/pwv21-policy-kernel@3e6632191cbcadbd6162c8a23217b2c2f4f394c0`, parent `143d9dd2ecea7a8c4174fed60fa447cf2405086a`. Canonical main remained separate at `4fb4bfb7d7b1481d6f347c182fc96a5a1135e045`; canonical workflow authority remains the installed main plugin, not the candidate. Changed blobs: `tools/definition_authority.py@16c315e278a12b42be6f5c1ffb75947fa6a51e1c`, `tools/exact_locator.py@c888697089749473ce9cdff10b93fb6ee0afab96`, `tools/router.py@af0a8ac741f582bfd5fc12693e887abd2270aabf`, `tools/state_contract.py@8f8fb729987c7b9c877683edd1ac03353a47281b`, `tests/test_definition_authority.py@cfbbf067b6b9525d0f6b1ed26b75fe475cdcabdc`, `tests/test_router.py@1c0bd106eba50a1c50117b4e5a70533beac5c70e`, `tests/test_session_router_integrity.py@cbc10c3d931b498c95b537839183617095285e6f`, `workflow/PLANNING.md@7de24fae91a3b38fe426550de98c14dcd3b14ca0`, `workflow/PLAN_REVIEW.md@52c88372a5c419c28b2ade20b476355cc4132c4f`, `workflow/ROUTER.md@47245e036caff5255e7ccb00c0bcde6508e52757`, `.codex-plugin/router-integrity.json@90e44ab3c5d8c1a0a5d6605213ac3afa396d04f7`. `workflow/ROUTER.md` changed from parent blob `3b3ac6010d683b475ce8afb00ec87fe572af58f9` by one freshness row; the manifest was regenerated to bind the new blob.
- Correction: Planning and Plan Review bind one deterministic `definition_authority_key` covering the Definition revision plus exact requirements/decision Git blob identities: `rf012-v1:repository:<repo>|definition:<rev>|requirements:<path>@<blob>|decisions:<path>@<blob>,...` with decisions sorted by path. The key binds content blobs, never HEAD or per-file commits, so unrelated commits do not churn it; path-only or free-text authority is not proof. Planning binds the key for the current GREEN Definition revision; Plan Review binds the byte-identical string, both present or both absent with exact-shape validation. The router proves freshness via `verify_planning_authority_freshness` before any premium B/C, review, Execution Prep or Board dispatch, reusing the RF007 resolver (`resolve_blob_at_commit`, `read_confined_worktree_bytes`, `read_git_blob_bytes`, `git_blob_sha`). The stored key must equal both the freeze-time snapshot built from the `DEFINITION.toml` revision plus requirements/decisions paths at the frozen plan subject commit (`git show <commit>:<definition-path>`, `git rev-parse <commit>:<path>`, entry revision match) and the live string from current `DEFINITION.toml` plus `git hash-object` over worktree bytes. Keyless legacy P7 plans derive the same snapshot from the immutable frozen subject and compare it with live bytes; only a unique match continues. Stale, missing, mismatched or ambiguous bindings fail closed to Recovery. Accepted authority is REQ-020/028/077/082/092 with REQ-097..102/125 historical preservation.
- RED-before/GREEN-after: on the exact parent there is no `tools/definition_authority.py` freshness predicate (`git grep` proves absence; `validate_planning` takes no Definition and the router never compares `planning.entry_subject` to Definition revision/authority), so pre-M03 H021 R1 routes execution while the R2 Definition mutant with untouched Planning/entry_subject/premium/review routes identically and swapped requirements/decisions also execute. After the fix the R1 baseline with valid unchanged authority routes `execution M01-T04`; the R2 mutant, swapped requirements/decisions, and same-revision authority-file mutation fail closed to Recovery. Stale/missing/mismatched, path-only/free-text, and RF007 unsafe-path siblings fail closed. Co-edited-key mutants (R1 entry with co-edited R2 key; mutation with co-edited keys and unchanged subject) fail closed because the stored key must also equal the frozen snapshot. Keyless matching-snapshot continues while mutated-authority and revision-change keyless fixtures fail closed, and unrelated future commits do not churn the legacy key.
- Verification: sixteen new test methods in `tests/test_definition_authority.py` passed (key shape/determinism/binding, H021 R1/R2/swapped/mutation/stale-missing-mismatched/path-only/RF007-escape, four legacy-derivation cases, two co-edited-key mutants); `tests/test_router.py` fixture fixups bind frozen/approved fixtures to a real Git snapshot and `tests/test_session_router_integrity.py` tracks the new router blob. Focused `tests.test_definition_authority` 16/16 and complete clean-tree `sh scripts/test.sh` passed 965/965 discovered tests and all 8 shell gates with exit 0. The remote product branch read back the exact implementation SHA. Exact-head GitHub Actions [36217476986](https://github.com/elmakus/project_workflow_v2/actions/runs/36217476986) (push) and [36217478912](https://github.com/elmakus/project_workflow_v2/actions/runs/36217478912) (PR) both completed successfully.
- Limits: RF013 Execution contract remains the separate downstream Card; the composed M02Q seam and M02Q Milestone Review still require their own downstream evidence. Correctly terminal M01/M02/M02R history is unchanged. A new Definition revision or same-revision authority-file mutation requires a new planning cycle; updating the key without new plan content does not authorize the old subject. Fresh independent Card review is required before M02Q-T08 becomes DONE.
