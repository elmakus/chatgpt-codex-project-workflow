# M02-T01 bounded repair evidence after R01

Date: 2026-09-24
Card: `M02-T01`
Prior review: `R01` RED
Target repository: `elmakus/project_workflow_v2`
Target branch: `work/pwv21-policy-kernel`
Repaired implementation subject: `593b1a350ff72e3fcfec6a1dde10e9b564fb827c`

## Correction

The repair stays within accepted M02 scope and addresses the exact R01 findings.

- Result reconciliation now requires exact result-subject equality with the obligation subject before acceptance or stale-result resolution.
- Obligation validation now rejects drift between top-level prerequisites, constraints, completion and mutation conditions and their fingerprinted freshness material.
- `role` is included in deterministic `obligation_id` derivation, so role drift invalidates the envelope.
- Executable exact-ref repository validation now matches the declared `owner/name` schema.
- Regression tests cover subject-binding drift, role drift, duplicated determining-surface drift and schema/executable repository parity.
- The human-readable stale-result contract now matches the accepted/implemented `reuse`, `rebase`, and `reconcile` action set.

No M03-M07 semantics or accepted product/strategy authority changed.

## Verification

GitHub Actions workflow `test`, run #367 / ID `35938359469`, completed successfully for exact head SHA `593b1a350ff72e3fcfec6a1dde10e9b564fb827c`.

The exact repaired target branch head and five-file repair commit were read back after the write. The repository gate includes the dedicated M02 typed-contract suite and the complete existing baseline through `scripts/test.sh`.

Repair evidence: GREEN. A new independent review attempt is required for the repaired exact result subject.
