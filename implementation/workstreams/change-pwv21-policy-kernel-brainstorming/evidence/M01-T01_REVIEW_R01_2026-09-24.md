# M01-T01 independent review evidence — R01

Date: 2026-09-24
Card: `M01-T01`
Attempt: `R01`
Exact result subject: `elmakus/chatgpt-codex-project-workflow@41fb9e075668a785a441b450560c99686ce55d19:implementation/workstreams/change-pwv21-policy-kernel-brainstorming/results/M01-T01.md@c7c9597542c2564bb8b5b2d888c4f617cf1190ff`
Implementation subject under review: `elmakus/project_workflow_v2@17b9cda3f50f0873b96ddbc8fea4ffc4941f2b93`
Independence: this review context did not materially produce or repair the exact result subject or implementation subject.

## Verified evidence

- The exact implementation commit and changed M01 surface were read back.
- GitHub Actions workflow `test`, run #340 / ID `35933206958`, is completed with conclusion `success` for exact head SHA `17b9cda3f50f0873b96ddbc8fea4ffc4941f2b93`.
- Registry versioning, fixed predicate vocabulary, closed schema, generated projection, read-only kernel behavior, deferred M02 seams, and baseline regression execution are present.

## Blocking finding

RED — the registry is not yet the single mechanically authoritative representation for registered outcomes.

`policy/mechanical_policy.json` canonically stores each registered rule's `disposition`, `obligation`, and `owner_module`, and `PolicyKernel.route()` returns those fields in `MechanicalDecision`. However production `tools/router.py` calls only `kernel.matches(...)` and then independently hard-codes the same disposition/obligation/owner values in each routing branch.

Therefore an injected registry change to a registered outcome can be reflected in the checked projection and still leave production routing on the old hard-coded outcome. The existing drift tests cover vocabulary mismatch and registry/projection mismatch, but do not fail closed on this registry-versus-production-outcome disagreement.

This violates the M01 acceptance surface requiring one canonical representation and injected registry/implementation/contract disagreement to fail closed, and conflicts with PWV21-REQ-007 / PWV21-REQ-009.

## Required bounded correction

Keep the existing named predicates and router structure, but consume `MechanicalDecision` outcome fields from `PolicyKernel.route()` for every registered rule instead of duplicating those fields in `router.py`. Add regression coverage that proves registered outcome metadata is authoritative and that registry precedence/order drift against production wiring is detected.

Verdict: RED
