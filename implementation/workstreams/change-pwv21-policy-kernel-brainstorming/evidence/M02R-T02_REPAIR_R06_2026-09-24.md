# M02R-T02 bounded repair evidence — R06

Date: 2026-09-24
Card: `M02R-T02`
Source review: `implementation/workstreams/change-pwv21-policy-kernel-brainstorming/evidence/M02R-T02_REVIEW_R06_2026-09-24.md`
Repaired implementation subject: `elmakus/project_workflow_v2@b1934ede935ee0dc1a6365858c1390f8c62a9772`
Repair range: `bad28ef22924af16785acb094e1e91dd71d24439...b1934ede935ee0dc1a6365858c1390f8c62a9772`

## R06 F1 — Card review scope is bound to the review obligation owner

Convergence `review_scope` is no longer caller-controlled on the Card path.

- `tools/state_contract.py`: `validate_review` and `validate_review_history` accept an explicit `expected_review_scope` seam; when `expected_card_id` is set and no explicit scope is given, the expected scope defaults to `card`, so a Card-owned history claiming `milestone` or `final` fails closed.
- `tools/review_contract.py`: `review_convergence_state` accepts `expected_review_scope` and rejects any convergence-aware attempt whose `review_scope` does not match; the generic 5/4/3 ceiling matrix is unchanged.
- `tools/router.py`: all three Card-path convergence calls bind `expected_review_scope="card"`.
- `tests/test_state_contract.py`: `test_card_owned_history_requires_card_review_scope` (Card scope mismatch rejected) and `test_expected_review_scope_seam_binds_milestone_and_final` (reusable seam for later Milestone/Final integration).
- `tests/test_router.py`: `test_card_scope_mismatch_cannot_trigger_milestone_or_final_ceiling` (a Card cannot trigger the 4/3 breaker by changing only `review_scope`).
- `tests/test_review_contract.py`: `test_expected_review_scope_binds_convergence_owner` (owner binding plus preserved positive ceiling matrix).

Exact repaired readback blobs:
- `tools/state_contract.py@0d4ac75c1502f2728a5804414acd2978ab3e06c3`
- `tools/router.py@96fff1b46337a2a1db8a130c035e3f35b967ebf7`
- `tools/review_contract.py@be3880c5b60584bd6407bb327bda5809bf9a520a`
- `tests/test_state_contract.py@6cee60a79d4f6cb0af186f919361e19ecb8b2e4d`
- `tests/test_router.py@0dc08b75bc03f361cf53d8f32a19ccbbc2373f43`
- `tests/test_review_contract.py@581bac2a5808852b52d64dde7125ff76d92b32a8`

## Exact-head verification

Candidate branch `work/pwv21-policy-kernel` reads back exactly at `b1934ede935ee0dc1a6365858c1390f8c62a9772`, confirmed both locally (`git rev-parse HEAD` in `/tmp/pwv21-f1-product`) and on `origin` (`git ls-remote`). The repair is one commit touching 6 files (213 insertions, 6 deletions).

Clean product baseline `scripts/test.sh` was run locally on that exact head: full `unittest discover` reports 224 tests OK and the script completes with `M01 baseline checks: PASS`; the product checkout is clean afterward.

A separate local independent precommit F1 review of this repair is reported GREEN (executor and fresh-tester 224 tests OK). That precommit review is not a formal PWv2 R07 review: no R07 attempt is recorded here, no exact-head CI run is claimed, and no GREEN Card verdict is claimed.

This repair supersedes the runtime-access blocked state; blocker disposition is left to Main. The R06 repair is bounded to M02R-T02 scope-owner binding semantics and regression coverage. It does not introduce observation/disposition semantics or BOOT-B/C/D scope.
