# M02-T01 bounded correction after R05 — 2026-09-24

## Trigger

Fresh independent review `M02-T01-R05` returned RED for one bounded M02 contract defect in exact implementation subject `902c067e24f4def69d872f509f476ba225a2c938`:

- executable Obligation/Result validation accepted JSON boolean `true` as `schema_version = 1` because Python `True == 1`, while the Draft 2020-12 schemas correctly rejected that value.

This created a schema/runtime version-validation mismatch and violated the fail-closed typed-version acceptance surface.

## Bounded correction

Target repository: `elmakus/project_workflow_v2`  
Target branch: `work/pwv21-policy-kernel`  
Pre-correction subject: `902c067e24f4def69d872f509f476ba225a2c938`  
Repaired subject: `af8610dda5b8efa9be5083bad1dac5b6ec596003`

Correction stayed entirely inside M02:

- `validate_execution_obligation()` now rejects boolean schema versions before testing equality with supported version 1;
- `validate_execution_result()` uses the same fail-closed rule;
- executable version regression coverage now checks both unsupported numeric version `2` and boolean `true` for both envelope types;
- the real Draft 2020-12 invalid matrix now includes boolean-version cases for both Obligation and Result.

No authority-resolution, freshness, mutation, M01 semantics, or M03-M07 behavior was changed.

## Exact range readback

The repaired target branch read back at exact HEAD `af8610dda5b8efa9be5083bad1dac5b6ec596003`.

Compared with accepted M01 base `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02`:
- status: ahead;
- ahead by: 37 commits;
- behind by: 0;
- merge base: exactly the accepted M01 base;
- changed files: exactly the same 14 bounded M02 contract/schema/test/documentation/CI-support files.

## Raw CI/readback evidence

GitHub Actions workflow `test`, run #379 / ID `35942705073`, exact `head_sha=af8610dda5b8efa9be5083bad1dac5b6ec596003`, event `push`:
- status: `completed`;
- conclusion: `success`;
- job `test` ID `107453903114`: `success`;
- `test_versions_and_telemetry_vocabulary_fail_closed`: GREEN, including boolean-version executable rejection;
- `test_json_schemas_validate_golden_and_reject_invalid_matrix`: GREEN, including boolean-version schema rejection;
- dedicated M02 typed-contract suite: 13 tests, all GREEN;
- full discovered suite: 185 tests, all GREEN;
- final `M01 baseline checks: PASS`.

## Review consequence

This correction changes the implementation subject and therefore requires a new immutable Result subject and a new append-only independent review attempt. This context materially performed the correction and is not eligible to issue the independent verdict for the repaired subject. The `after-M02-T01` trigger remains `waiting`; M03 is not materialized.
