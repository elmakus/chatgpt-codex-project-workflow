# M02-T01-R12 — post-convergence fresh independent Card validation

Date: 2026-09-24
Verdict: GREEN

## Review boundary

This is the single authorized post-convergence fresh independent exhaustive full-scope Card validation for M02-T01.

Correctness was reconstructed from the stable M02-T01 Task Card, accepted P2 authority, PWV21-REQ-018 through PWV21-REQ-033, applicable accepted ADRs, the exact current implementation bytes, and raw current CI/readback evidence.

Prior Card-review findings, Milestone-review findings, repair narratives, convergence conclusions, and previous verdict rationales were not used as correctness evidence or as a checklist. Prior history was consulted only for lifecycle/exact-subject continuity and to establish that a separate fresh M02 Milestone re-review must follow a GREEN Card verdict.

## Exact subject and accepted authority

- Card acceptance: `implementation/workstreams/change-pwv21-policy-kernel-brainstorming/cards/M02-T01.md`
- Durable Card Result subject:
  - repository: `elmakus/chatgpt-codex-project-workflow`
  - commit: `eb17e9a9fd95195544426d7fc8dba5ae48f30cf0`
  - path: `implementation/workstreams/change-pwv21-policy-kernel-brainstorming/results/M02-T01.md`
  - blob: `46ce3ec63f7c0ae82fa5c0c13c90f6428d6e9800`
- Implementation subject named by that Result:
  - `elmakus/project_workflow_v2@e7a939e0a37f3cfcb7e39e04d5654b94101a5090`
- Accepted P2 subject:
  - consumer commit: `ca046952a4d249d9ed74af6408db71b0ded73a8f`
  - path: `planning/PWV21_POLICY_KERNEL_MASTER_PLAN_P2.md`
  - blob: `79ee0b6588be32767ba283b9f20a48a192999001`
- Accepted authority blobs verified against P2 pins:
  - `requirements/PWV21_POLICY_KERNEL.md` -> `21ee85e6a306ce106cb26e77b0a395a2e671c054`
  - `decisions/ADR_PWV21_POLICY_KERNEL.md` -> `0662b970d80e8b76df79e4dbd458d190ce6d4402`
  - `decisions/ADR_PWV21_ORCHESTRATION_CONTRACT.md` -> `051f8fc3f68773349a1fcad39fcf1c60d3157532`

The implementation branch `work/pwv21-policy-kernel` read back at exact HEAD `e7a939e0a37f3cfcb7e39e04d5654b94101a5090`.

## Raw current test/readback evidence

GitHub Actions run `35958508100` (run #403), job `107501825220`, event `push`, completed successfully for exact head SHA `e7a939e0a37f3cfcb7e39e04d5654b94101a5090`.

Raw job logs show:

- `tests.test_obligation_contract`: 15 tests, all GREEN;
- policy-kernel regression suite: 11 tests, all GREEN;
- state-contract suite: 28 tests, all GREEN;
- router suite: 39 tests, all GREEN;
- execution/review/recovery contract regressions GREEN;
- cumulative Python discovery: 187 tests, all GREEN;
- repository baseline check: `M01 baseline checks: PASS`.

The exact implementation delta from the accepted M01 implementation subject `a92c2c5f0eefd9d0bcd9fb75be4624b3719fff02` to the M02 implementation subject is a forward-only 61-commit range. The final tree contains the M02 schemas, executable obligation/result contract, policy-kernel integration, golden fixtures, dedicated tests, CI dependency wiring, and execution-contract documentation.

## Full M02 acceptance reconstruction

| Requirement | Independent validation |
| --- | --- |
| PWV21-REQ-018 | Versioned transport-neutral Obligation/Result JSON schemas exist, are closed, and are paired with executable validators/serializers and golden fixtures. Unsupported schema versions fail executable validation. |
| PWV21-REQ-019 | Obligation schema and compiler bind exact role, exact Git subject, resolved authority bundle, prerequisites, constraints, completion acceptance/tests/evidence, freshness, and mutation conditions. |
| PWV21-REQ-020 | PW-side authority resolution accepts only exact repository/40-hex commit/path/blob refs, reads the selected exact source, verifies Git blob identity and content digest, rejects duplicates, and materializes only the supplied bounded authority set. |
| PWV21-REQ-021 | Runtime transport cannot silently add/drop/reinterpret authority inside a valid obligation: the authority bundle is closed and must exactly match the freshness authority refs; content/blob/digest drift fails validation. Authority selection itself remains PW/Main-owned per ADR-PWV21-002. |
| PWV21-REQ-022 | Summary-only authority is structurally insufficient: each authority source requires exact locator, verified blob/content hash, and full UTF-8 content. |
| PWV21-REQ-023 | The obligation is content-derived/disposable; implementation stores no second project-state authority and contract documentation explicitly preserves repository/Git authority. |
| PWV21-REQ-024 | `obligation_id` is deterministic SHA-256 material over registered rule identity/fingerprint, role, exact subject, and freshness fingerprint; byte-identical determining inputs yield byte-identical obligations. |
| PWV21-REQ-025 | Freshness hashes only determining material. Scalar registered inputs are exact; collection inputs use bounded deterministic projections. K011 includes READY plus IN_PROGRESS/BLOCKED identities because active/blocked Cards preempt READY in the production router, while DONE-card metadata/order noise is excluded. K012 uses the canonical Card identity/status set because all terminal-card membership determines Close. Unrelated runtime/repository noise is excluded and regression-tested. |
| PWV21-REQ-026 | Result schema is closed to PW-relevant semantic fields: exact obligation/freshness/original subject binding, immutable result subject, changed-artifact refs, tests, evidence, readback, blocker, and semantic outcome. |
| PWV21-REQ-027 | Provider/model/worker/session/retry/worktree/Paseo/runtime/invocation/scheduler field-name families are recursively rejected from obligation material; result shape is closed; existing canonical-state prohibition remains GREEN in the baseline suite. |
| PWV21-REQ-028 | Acceptance validates exact result-to-obligation binding and recomputes current freshness. The kernel itself refreshes registered-rule fingerprint and minimal mechanical input projection; current semantic authority/completion/mutation material remains PW/Main-owned input, not runtime-supplied authority. Stale mismatch cannot return `accept`. |
| PWV21-REQ-029 | Stale results default to `reexecute`; `reuse`/`rebase`/`reconcile` require exact old/new fingerprint binding plus an explicit positive safety proof and non-empty basis. Semantic failed/blocked/non-GREEN results cannot be converted to success by a stale proof. |
| PWV21-REQ-030 | Breaking schema versions fail closed in both executable validation and JSON-schema tests; no partial application path exists. |
| PWV21-REQ-031 | Obligations carry canonical mutation pre/postconditions; preconditions and postconditions are checked by canonical JSON equality. Non-empty postconditions require observed canonical-state readback plus verified readback evidence before fresh acceptance. |
| PWV21-REQ-032 | The execution result contract forbids direct canonical workstream-state mutations in the obligation-subject repository, while Execution/Execution Contracts keep Main/coordinator as the sole canonical PW writer/finalizer. |
| PWV21-REQ-033 | Unknown external-effect completion is routed through exact-target readback/reconciliation; uncertain state fails closed without retry, verified no-effect permits retry, expected effect reconciles without replay, and blind duplicate effects are forbidden. |

## Material finding set

No material or blocking defect was independently discovered across the complete M02 acceptance surface.

The review did not terminate early on any local concern; schema, derivation/identity/freshness, authority resolution/bundle integrity, result acceptance/staleness, mutation/readback, direct-mutation boundary, unknown-side-effect retry behavior, documentation, dedicated tests, and baseline regressions were all evaluated before the verdict.

## Terminal decision

M02-T01-R12 is GREEN for the exact bound subject.

After this verdict, only deterministic M02-T01 Card finalization is authorized. A separate fresh M02 Milestone re-review must be frozen as a new pending boundary. This R12 context performed a constituent Card review and must not issue that Milestone verdict. The `after-M02-T01` JIT trigger remains unconsumed and M03 must not be materialized in this context.
