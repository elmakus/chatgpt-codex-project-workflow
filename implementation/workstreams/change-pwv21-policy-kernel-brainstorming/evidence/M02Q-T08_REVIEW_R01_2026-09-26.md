# M02Q-T08 R01 independent Card review — RF012 Definition-authority key

- Task ID: PWV21-M02Q-T08-REVIEW-R01
- Review attempt: R01 (discovery, pending TOML at
  `implementation/workstreams/change-pwv21-policy-kernel-brainstorming/reviews/M02Q-T08-R01.toml`)
- Reviewer: fresh independent session; did not produce or repair the Result or product implementation.
- Consumer HEAD at review: `8c41149eda3ff03fa1b6cfd76da20e61e47af5e7` (clean, Board rev 138, T08 in_progress).
- Frozen Result subject (verified): `results/M02Q-T08.md`
  at `a8f30ca4b7d78e3b370c46438cb597d21a40c987`, blob `9a766a9dcca9ac12f6b09af28ad95eb4fa29252d`
  (`git ls-tree` at that commit returns exactly this blob; `git cat-file -p` bytes match).
- Product subject (verified clean): `elmakus/project_workflow_v2@3e6632191cbcadbd6162c8a23217b2c2f4f394c0`,
  parent `143d9dd2ecea7a8c4174fed60fa447cf2405086a`, branch `work/pwv21-policy-kernel`.
  All 11 changed-file blobs at HEAD match the implementation evidence pins exactly
  (`definition_authority.py@16c315e…`, `exact_locator.py@c88869…`, `router.py@af0a8a…`,
  `state_contract.py@8f8fb7…`, `test_definition_authority.py@cfbbf0…`, `test_router.py@1c0bd1…`,
  `test_session_router_integrity.py@cbc10c…`, `PLANNING.md@7de24f…`, `PLAN_REVIEW.md@52c883…`,
  `ROUTER.md@47245e…`, `router-integrity.json@90e44a…`).
  Parent `workflow/ROUTER.md` blob `3b3ac601…` confirmed; manifest rebind is exact.
- Canonical authority respected: candidate branch is separate from main `4fb4bfb7d7b1481d6f347c182fc96a5a1135e045`
  (merge-base ancestor check); no candidate-plugin install claimed or observed.

## Acceptance assessment (Card M02Q-T08, H021)

1. H021 R1 routes execution: PASS. Delivered `test_r1_baseline_executes_with_valid_unchanged_authority`
   plus independent probe `r1/approved-executes` both route `(route, execution, M01-T04)` on valid
   unchanged authority. Observed, not fitted.
2. H021 R2 Definition mutant / swapped requirements-decisions fail closed: PASS.
   Delivered R2, swapped, and same-revision-mutation tests route to Recovery; independent probes
   (`ordering/stale-preempts-premium-B`, `coedited/*`) reproduce fail-closed with kind `stale`.
3. Stale/missing/mismatched key cannot execute; valid plan continues deterministically: PASS.
   Router calls `verify_planning_authority_freshness` after `validate_plan_review` and before
   `subject_key`/K005 premium B, K006/K007 review, editorial/K008 premium C, Execution Prep and Board
   dispatch (`tools/router.py` ~L685-697 vs ~L699+ and L820+). Stale preempts premium B proven by
   independent probe: frozen + B-due + stale key → Recovery; frozen + B-due + valid key → premium_B stop.
4. No path-only / free-text authority; key binds exact Git identities via RF007 resolver: PASS.
   Key grammar requires `rf012-v1:…` with 40-hex blobs, rooted paths, sorted unique decisions
   (`tools/definition_authority.py` L98-144); 9 independent shape negatives (unsorted, duplicate,
   short blob, traversal, empty, whitespace, wrong prefix, free-text, 3-segment) all raise.
   Only RF007 helpers are used for Git proof: `resolve_blob_at_commit`, `read_confined_worktree_bytes`,
   `read_git_blob_bytes`, `git_blob_sha`, `normalize_locator_path`.
5. Co-edited keys: PASS. Stored key must equal BOTH live string and freeze-time snapshot
   (L507-513 then L545-552); two delivered mutants plus independent direct-verify probe fail with `stale`.
6. Freeze-time snapshot: PASS. Snapshot derives `DEFINITION.toml` + authority blobs at the immutable
   plan subject commit (`git rev-parse <commit>:<path>` semantics), requires GREEN snapshot state,
   workstream match, and entry_revision match (L316-375).
7. Legacy keyless P7: PASS. Keyed path untouched; keyless derives snapshot and requires byte-equality
   with live (L555-602); ambiguity fails closed. Delivered 4 legacy tests (match-continues,
   mutated-fails, revision-change-fails, unrelated-commits-don't-churn) all pass; independent probes
   confirm no-git and entry-revision-mismatch fail closed.
8. Terminal M01/M02/M02R unchanged: PASS. Board shows M01-T01/M02-T01 done, all 13 M02R done,
   M02Q-T01..T07 done, only T08 in_progress. Product diff touches no history, no migration.
9. RF013 / RF004 boundary: PASS. Diff names only the 11 RF012 files; `-G "RF013|execution_contract|
   ExecutionContract"` and `-G "RF004"` over the diff are empty; `tools/execution_contract.py`
   and `workflow/EXECUTION.md` untouched.
10. Dependencies: PASS. T07 R01 GREEN and T02 R01 GREEN; result pins
    `M02Q-T07.md@1e32008…:c8492c…` and `M02Q-T02.md@c085e14…:181e850…` resolve exactly.

## RED-before / GREEN-after

- RED-before confirmed at parent `143d9dd`: `git grep definition_authority` finds only the unrelated
  `recovery_contract` classification string; `validate_planning(data, workstream_id)` takes no Definition;
  parent router references `planning.entry_subject` only as a display subject, never compared to Definition
  authority. The H021 absence claim is literally true.
- GREEN-after: 16/16 focused tests observed (`python3 -m unittest tests.test_definition_authority`,
  0.216s, OK); full clean-tree `sh scripts/test.sh` observed 965/965 + all 8 shell gates, exit 0,
  ending `M01 baseline checks: PASS` with tree still clean.

## External readback

- Remote product branch `origin/work/pwv21-policy-kernel` resolves to exactly `3e6632191c…` (observed
  after fetch; local HEAD identical; both worktrees clean).
- Exact-head CI: `gh run view 36217476986` → push, headSha `3e6632191c…`, completed success;
  `gh run view 36217478912` → pull_request, same headSha, completed success. Both observed.

## Falsification attempts (reviewer-owned, /tmp only)

`/tmp/rf012_falsify.py` — 18 independent probes (shape ×9, determinism ×2, ordering ×2, R1 positive,
mismatch, keyless no-git, keyless entry-mismatch, co-edited direct-verify): 18/18 pass.
One intermediate probe bug (wrong fixture repository string) itself demonstrated repository-mismatch
fail-closed, then was corrected and re-run green. No counterexample found; no assertion was weakened.

## Limitations / residual risk

- CI conclusions read via `gh` API (authenticated as review environment); run logs not re-audited line by line.
- Legacy P7 compatibility is proven against synthetic frozen subjects, not by replaying the real consumer
  P7 blob `37aac21d…` through the new router; the derivation path is commit-agnostic by construction
  (`git rev-parse <commit>:<path>`), so residual risk is low.
- Composed M02Q seam and Milestone Review remain downstream by design (RF013 next).

## Recommendation

No material defect found. All acceptance clauses are independently evidenced.
Recommended verdict for R01: **GREEN**.
