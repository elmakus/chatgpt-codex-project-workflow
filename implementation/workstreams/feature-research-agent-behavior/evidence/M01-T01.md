# M01-T01 implementation evidence — Research agent behavior

Date: `2026-09-21`
Card: `M01-T01`
Tested implementation head: `067d32c07f669df99c2acf3b4d1e0d3eb47613aa`

## Implemented outcome

- Added one policy-neutral proportional prior-art contract to `workflow/common/RESEARCH.md`.
- Added explicit shared-behavior application to both `workflow/chatgpt_only/RESEARCH.md` and `workflow/codex_only/RESEARCH.md` without changing their durable continuation/reconciliation semantics.
- Preserved Codex Investigator runtime realization under `codex_workflow`.
- Corrected `templates/RESEARCH.md` so ChatGPT-only pre-execution Research points to selected workstream manifest `routing.research_obligation`, and added evidentiary-weight structure to the source table.
- Added JIT OpenSpec `openspec/changes/research-agent-behavior/` and focused contract regression coverage.
- README required no change: its existing migrated-policy Research/pointer description already matches selected-manifest pre-execution ownership.

## Behavior verified

- relevant external prior art is required when reasonably likely to materially help;
- purely local/private questions can use the smaller local evidence path;
- official/upstream, issue/discussion, comparable implementation and community evidence classes are represented;
- stronger/primary evidence takes precedence on conflict while practical community evidence remains usable;
- conflicts, unavailable required sources and uncertainty are surfaced rather than fabricated/normalized;
- searching stops once sufficient source-grounded evidence answers the exact question and alternatives;
- Research remains evidence rather than accepted authority and existing Return-target/reconciliation ownership remains intact;
- ChatGPT-only and Codex-only expose equivalent evidence behavior;
- no crawler, search index/database, new Research phase/state owner or Codex runtime mapping was introduced.

## Verification

- Focused + relevant regressions: `python3 -m unittest tests.test_research_agent_behavior_contract tests.test_chatgpt_only_branch_first_execution_state_contract tests.test_codex_only_branch_first_preexecution_contract` → `28/28 GREEN`.
- Full repository suite: `python3 -m unittest discover -s tests -p 'test_*.py'` → `97/97 GREEN` on tested implementation head.
- `git diff --check 13e27863cb11ec9e223ee2af191f79fe6b8d559d..HEAD` → GREEN.
- Conflict-marker scan → GREEN.
- Refresh check: workstream base `13e27863...` and current `main` were identical before review freeze; no target drift required reconciliation.

## Result

Implementation acceptance is GREEN from the author side. The Card remains non-terminal because its `RECOMMENDED` independent review must judge the exact frozen subject before Definition of Done.
