# M01-T01 Implementation Evidence — Workstream state model and routing foundation

Card: `M01-T01`
Milestone: `M01`
Implementation subject: `0f24c9053a4e41346b0e14a9d043421057a99767`
Workflow-main baseline checked: `03035876f3283d33e8a10ff43265f5be21a27a06`

## Result

Implemented the M01 state/routing foundation for branch-isolated ChatGPT-only workstreams while preserving the legacy/default Task Board path.

The subject adds:
- `workflow/chatgpt_only/WORKSTREAMS.md`;
- a branch-isolated workstream manifest template;
- a workstream-local Task Board template;
- selected-workstream state resolution in the ChatGPT-only router/state/repository/recovery/preparation/research/Card contracts;
- policy-neutral authority/bootstrap wording that allows a policy-selected canonical Task Board while retaining `implementation/TASK_BOARD.yaml` as the default fallback.

The workstream manifest is explicitly routing/workstream-lifecycle metadata. Card/milestone execution, implementation-review, result and implementation/recovery Research state remain canonical only in the selected Task Board. The manifest review block is reserved for a distinct workstream-level final-integration review and must not mirror Card/milestone review state.

## Acceptance verification

GREEN:
- legacy/default fallback remains explicit: no selected branch-isolated workstream means `implementation/TASK_BOARD.yaml` remains authoritative;
- selected branch-isolated recovery resolves exact branch + manifest before loading its exact Task Board;
- seriality is scoped to the selected Task Board, so two independent workstreams may each own one `in_progress` Card while two such Cards in one selected board remain invalid;
- no mutable repository-global workstream registry is introduced;
- current workflow `main` remained at the recorded baseline during implementation;
- no positive mixed/Codex/legacy parallel-lane execution mechanism was added.

## Checks

- `git diff --check fcce41f9a5828c8cac526e1c615a97a2c5494910..0f24c9053a4e41346b0e14a9d043421057a99767` — PASS.
- required new workstream contract/template files exist — PASS.
- static ownership assertions for manifest versus selected Task Board — PASS.
- legacy/default fallback assertions — PASS.
- no-global-registry assertion — PASS.
- selected-board seriality assertion — PASS.
- grep audit found no remaining active ChatGPT-only/policy-neutral assertion of repository-global one-Card seriality in the M01-targeted surfaces — PASS.
- policy-neutral authority wording retains `implementation/TASK_BOARD.yaml` as the default for routes that do not define another canonical board — PASS.
- manual structural checks confirmed required manifest and workstream Task Board keys — PASS.

YAML parser note:
- Python `PyYAML` and Ruby were not installed in the available runtime, so no parser-based YAML validation is claimed.
- The YAML changes are template-only and were checked structurally; no runtime YAML consumer exists in this repository's current test infrastructure.

## External state

No material external runtime/deployment mutation was required.

## Review boundary

Independent review: `RECOMMENDED`.

Review must inspect the exact implementation subject above against the M01-T01 authority slice and this evidence. The implementing chat must not issue the independent verdict.
