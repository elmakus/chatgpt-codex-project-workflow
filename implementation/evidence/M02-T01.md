# M02-T01 Implementation Evidence — Explicit #issue / #feature intake routing

Card: `M02-T01`
Milestone: `M02`
Implementation base: `02945ab80110e743566a1dbf9e6309368a2f9c05`
Implementation subject: `85c268bc3931902de265c2dba3d749732d2ceec4`
Prior rejected subject: `1a8b2c358abc18d69d045c0b73d4a74c27c9baae` — RED in `implementation/evidence/M02-T01-review-01.md`
Workflow-main baseline checked: `03035876f3283d33e8a10ff43265f5be21a27a06`

## Result

Implemented the M02 ChatGPT-only intake layer for explicit `#issue` / `#feature` directives.

The frozen subject:
- adds `workflow/chatgpt_only/INTAKE.md`;
- gives explicit intake directives router precedence before unrelated prior/default workstream execution state;
- makes active pre-execution intake recoverable from exact branch + manifest + intake record even when no Task Board exists yet;
- defines issue diagnosis/base/dependency/path classification;
- defines feature workstream creation and handoff into canonical Brainstorming while preserving explicit Definition promotion authority;
- defines stable workstream ID/branch naming plus deterministic collision handling;
- extends workstream manifest routing metadata with an intake lifecycle/pointer without duplicating Card/milestone state;
- preserves M01 manifest ↔ Task Board binding and default-board fallback.

## Acceptance verification

GREEN:
- explicit `#issue` / `#feature` router precedence is evaluated before unrelated Task Board review/execution selection;
- ordinary prompts without an operator directive fall through to the existing branch/default state-resolution path;
- an active intake is recoverable before Task Board binding, including `task_board: null` discovery state;
- issue intake defaults to the normal integration target and selects an unmerged parent only for concrete parent-only dependency;
- mere symptom exposure, likely file overlap or convenience is explicitly insufficient for stacked classification;
- issue intake records micro-fix eligibility using accepted R6 criteria while leaving actual micro-fix execution/review mechanics to M03;
- feature intake explicitly keeps Definition promotion pending and states that `#feature` is not promotion authorization;
- same exact branch/workstream recovery reuses the existing intake rather than creating a second lane;
- workstream IDs/branches use stable kind-specific naming with smallest-available numeric collision suffixes;
- manifest intake metadata remains routing/workstream-lifecycle state and does not duplicate Task Board Card/milestone state;
- M02 now owns new-workstream parent/base selection; M04 still owns worktrees, refresh/rebase/retarget and final integration;
- no mixed/Codex/legacy implementation surface changed in the M02 implementation range.

## Checks

- exact M02 implementation range `02945ab8...1a8b2c35` changes only:
  - `workflow/chatgpt_only/INTAKE.md`;
  - `workflow/chatgpt_only/ROUTER.md`;
  - `workflow/chatgpt_only/WORKSTREAMS.md`;
  - `workflow/chatgpt_only/WORKSTREAM_TEMPLATE.yaml`.
- current workflow `main` remains the recorded baseline and the M02 subject is ahead with no behind drift.
- static precedence assertion: explicit marker check appears before canonical Task Board resolution — PASS.
- static no-marker fall-through assertion — PASS.
- static active-intake-before-Task-Board assertion — PASS.
- scenario A logic trace (active feature A + independent issue B) — PASS.
- scenario B logic trace (genuine parent-only dependency creates stacked B) — PASS.
- scenario D logic trace (`#feature` discovery without Definition authorization) — PASS.
- scenario F logic trace (same workstream is recovered, not duplicated) — PASS.
- collision-suffix logic trace — PASS.
- ownership check: intake metadata does not mirror Card/milestone state — PASS.
- trailing-whitespace scan over the four M02 behavior files — PASS.
- exact `WORKSTREAM_TEMPLATE.yaml` parses successfully as YAML in the available validation runtime and exposes the expected `intake.state` / `intake.record` mapping — PASS.

No claim is made for full M03 micro-fix execution, M04 worktree/integration refresh or M05 handoff/E2E closure; those remain later approved milestones.

## Bounded correction after independent review RED

Independent review attempt 1 found one ordering ambiguity in `INTAKE.md`: the common flow selected a base/created the branch before the issue-specific diagnostic sequence that was supposed to justify independent-versus-stacked classification.

Corrected subject `85c268bc3931902de265c2dba3d749732d2ceec4`:
- recovers an exact existing workstream before any new-workstream sequence is replayed;
- requires genuinely new intake to complete route-specific pre-creation discovery/diagnosis before common base selection;
- makes `#issue` establish/reproduce the problem when practical and gather dependency evidence before base selection or branch creation;
- makes `#feature` perform duplicate/dependency discovery before base selection;
- removes duplicate route-specific create/select steps that conflicted with the common numbered flow;
- leaves all other M02 semantics unchanged.

Correction self-validation:
- current workflow `main` remains `03035876f3283d33e8a10ff43265f5be21a27a06`;
- the correction commit changes only `workflow/chatgpt_only/INTAKE.md`;
- static ordering check confirms route-specific pre-creation discovery/diagnosis precedes common base selection;
- issue and feature sections explicitly bind their pre-creation steps to before Common intake flow step 4;
- neither route-specific section contains a second create-workstream step;
- trailing-whitespace check on corrected `INTAKE.md` passed.

This correcting chat does not issue the independent verdict for the corrected subject.

## External state

No material external runtime/deployment mutation was required.

## Review boundary

Independent review: `RECOMMENDED`.

Review inspected exact corrected subject `85c268bc3931902de265c2dba3d749732d2ceec4` against `implementation/cards/M02-T01.md`, the M02 Master Plan section, applicable requirements/ADR, accepted M01 dependency result and this evidence. Independent re-review attempt 2 is GREEN in `implementation/evidence/M02-T01-review-02.md`.
