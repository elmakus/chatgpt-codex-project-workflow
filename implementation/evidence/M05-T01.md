# M05-T01 Implementation Evidence

Card: M05-T01
Result: GREEN corrected implementation/coherence validation; independent review attempt 01 was RED; fresh independent re-review of the corrected subject is pending.

## Subject

- Execution-start baseline: `b14dbf24c03ec2f0329f1ea1afaf3cd6ba87c901`
- Original review-01 subject: `69e29e68fdd18f745aa2396b5220b5ac5d34c766`
- Corrected implementation subject for re-review: `13975e34df485fe10d01730613e042e9dab952c7`
- Branch: `feat/chatgpt-only-multi-workstream-intake`
- Workflow/main integration target at freeze: `03035876f3283d33e8a10ff43265f5be21a27a06`
- Current `main` remained identical to the accepted baseline; the frozen subject was 0 commits behind `main`.

Independent review attempt 01 judged the original subject RED; durable evidence is `implementation/evidence/M05-T01-review-01.md`. The fresh re-review must judge the full corrected feature-branch implementation represented by `13975e34df485fe10d01730613e042e9dab952c7`, not only the corrective delta.

## Implemented

- `workflow/common/USER_STOP.md`
  - Card/milestone independent-review handoff now points to the exact selected Task Board: legacy/default board or manifest-selected workstream board.
  - Added a separate locator-only branch-isolated workstream final-integration review handoff whose durable pointer is the selected `WORKSTREAM.yaml`, matching manifest-owned review state.
- `prompts/CHATGPT_FRESH_SESSION.md`
  - documents selected workstream Task Board versus manifest pointer ownership without duplicating the canonical USER_STOP template.
- `prompts/CHATGPT_START.md`
  - resolves branch-isolated manifest state before mutable execution state;
  - recognizes selected Task Board Card/milestone review and manifest-owned final-integration review;
  - retains legacy/default Task Board fallback.
- `README.md`
  - scopes seriality to one `in_progress`/READY Card per selected Task Board;
  - documents branch-isolated manifest/Task Board ownership, `#issue` / `#feature`, micro-fix, local worktrees, stacked dependencies and target refresh;
  - updates review, progressive-disclosure and fresh-handoff descriptions for selected workstream state.
- `templates/PROJECT.md`
  - adds an optional non-live workstream-root convention;
  - keeps branch-isolated mutable state in manifest-selected owners rather than turning PROJECT into a registry/state mirror.
- `CHANGELOG.md`
  - records the complete branch-isolated workstream/intake behavior.
- `docs/audits/CHATGPT_ONLY_MULTI_WORKSTREAM_INTAKE_SCOPE.md`
  - persists the final REQUIRED independent architecture/coherence review scope and ten-scenario E2E matrix.
- Project root `PROJECT.md`
  - reconciled the latest cumulative handoff pointer from stale M01 to the accepted M04 handoff.

No change was made to root `CHATGPT.md`: its canonical-source wording was already policy-generic and explicitly warns not to assume every review lifecycle lives in the default Task Board.

No M05 change was made to `WORKSTREAM_TEMPLATE.yaml` or `WORKSTREAM_TASK_BOARD_TEMPLATE.yaml`: inspection confirmed the existing accepted templates already contain stable workstream identity, branch/base/target, parent dependency, manifest-owned final-review metadata, Task Board binding identity and one-in-progress-per-board seriality.

## Static / coherence validation

GREEN:

- exact M05 implementation delta after execution start changes only:
  - `CHANGELOG.md`
  - `PROJECT.md`
  - `README.md`
  - `docs/audits/CHATGPT_ONLY_MULTI_WORKSTREAM_INTAKE_SCOPE.md`
  - `prompts/CHATGPT_FRESH_SESSION.md`
  - `prompts/CHATGPT_START.md`
  - `templates/PROJECT.md`
  - `workflow/common/USER_STOP.md`
- no conflict markers or trailing whitespace remain in the exact M05 changed files;
- active ChatGPT-only state contracts inspected across Router/State/Repository/Intake/Micro-fix/Execution Prep/Execution/Review/Recovery/Close/Workstreams/templates consistently scope seriality to the selected Task Board or explicitly mark the legacy/default board;
- no active changed ChatGPT-only/common execution contract imports mixed-policy capability routing, Codex orchestration or legacy parallel-lane execution semantics;
- README references to legacy/Codex namespaces are explanatory policy-boundary documentation, not active ChatGPT-only execution imports;
- no mutable global workstream registry/scheduler was introduced;
- no suitable existing regression harness was identified in the inspected standard project paths, so this evidence makes no automated-regression claim and uses the required durable static/coherence + logical E2E checks instead;
- GitHub reported no combined status checks for the frozen subject, so no CI-execution claim is made.

## Required logical E2E matrix

### 1 — legacy single-workstream project: GREEN

`WORKSTREAMS.md` preserves default fallback to `implementation/TASK_BOARD.yaml` when no branch-isolated workstream is selected. Existing state is not migrated merely because multi-workstream support exists. The default Task Board retains one-in-progress-Card seriality.

### 2 — active feature A + independent #issue B: GREEN

Explicit `#issue` has Intake precedence over unrelated active state. Issue diagnosis/discovery establishes whether the problem needs parent-only state before base selection. An independent bug uses the normal target/base, receives a distinct branch/manifest/state, and may execute/review/integrate without mutating or waiting on A. A later refreshes against the moved target.

### 3 — parent-dependent #issue B stacked on A: GREEN

Intake records parent workstream/branch plus exact `parent_dependency` and creation base. `WORKSTREAMS.md` forbids direct child → final target while required parent-only content remains absent and defines both legal paths: child folded into parent, or parent integrated first followed by child reconciliation onto the current target.

### 4 — #feature lifecycle: GREEN

Feature Intake creates/recover discovery state and explicitly keeps promotion authorization pending unless the user separately authorizes the exact brainstorming scope/revision. The `#feature` marker is not promotion authority. Definition GREEN then returns deterministically to Planning.

### 5 — micro-fix: GREEN after review-01 correction

Issue Intake requires all R6 qualification criteria before `path: micro_fix`. The completed Intake record is a recoverable pre-Task-Board anchor; Execution Prep creates one bounded fix Card plus selected workstream Task Board.

Corrected deterministic closure trace:

`micro-fix Intake → Execution Prep → one bounded Card → implementation/evidence → fresh Card review → Execution terminalizes Card only → router → Close → current-target integration refresh → exact stronger-review coverage reuse OR manifest final-review pending → fresh manifest review when needed → router → Close refresh-preservation check → target re-read → integration → micro-fix workstream/result reconciliation`.

No Master Plan or milestone entry is synthesized. Manifest final-integration review is never first reused/frozen before target refresh. GREEN manifest review returns to Close rather than looping back into Review.

### 6 — two local workstreams: GREEN

`REPOSITORY.md` + `WORKSTREAMS.md` require separate worktrees/equivalent mutable checkouts for concurrently executing local workstreams. Branch names alone are insufficient. Remote-only GitHub operations remain exempt. Each selected Task Board still allows at most one in-progress Card.

### 7 — same-workstream second chat: GREEN

Exact branch/manifest identity selects the same workstream; active Intake/Task Board/review/Research state is recovered instead of creating a second lane. Binding mismatch routes to Recovery rather than fallback/guessing.

### 8 — target moves after GREEN: GREEN

Integration refresh compares with the current target and reruns affected compatibility verification. Changed target SHA/ancestry alone does not invalidate review when the exact covered workstream content/behavior and acceptance surface remain unchanged and compatibility verification is GREEN. Material covered behavior/acceptance change freezes a new exact subject and requires fresh independent review.

### 9 — RED review in B: GREEN

RED remains in the exact review owner for B. Corrective execution/Research uses B's selected Task Board and does not inspect/mutate unrelated A as a correction lane. A corrected reviewable subject receives a new pending boundary and cannot be self-reviewed by the correcting chat.

### 10 — fresh handoff: GREEN

USER_STOP now distinguishes selected Task Board pointers for Card/milestone review from selected manifest pointers for workstream final-integration review. Prompts stay branch-aware and locator-only; workstream state/checklists/SHAs/remediation branches remain durable rather than serialized. After the named role completes, router-owned continuation resumes until a real stop.

## Integration / PR readback

At frozen-subject verification:

- PR #28 was open, draft and reported mergeable;
- base: `main@03035876f3283d33e8a10ff43265f5be21a27a06`;
- head: exact frozen subject `69e29e68fdd18f745aa2396b5220b5ac5d34c766`;
- final integration is intentionally not performed before independent review.

## Review boundary

M05-T01 requires **REQUIRED** fresh independent architecture/coherence review.

Canonical scope: `docs/audits/CHATGPT_ONLY_MULTI_WORKSTREAM_INTAKE_SCOPE.md`.

Freeze `review_subject` to `69e29e68fdd18f745aa2396b5220b5ac5d34c766`, keep the Card and M05 non-terminal, and stop for a fresh independent reviewer. Review evidence/state after the frozen subject is bookkeeping and must not be treated as behavioral drift.


## Independent review 01 RED remediation

Review-01 finding: the original qualified micro-fix path had no legal no-milestone transition into final workstream integration. `MICRO_FIX.md` deliberately omitted milestones while `CLOSE.md` and the router still assumed a milestone contract. Review also exposed an ordering inconsistency: Execution could reconcile/reuse the manifest final-review gate before the integration refresh contract had run.

Bounded L1/L2 correction inside accepted authority changed only the active micro-fix close/integration contracts:

- `EXECUTION.md`: terminal GREEN-reviewed micro-fix Card now returns to the router without touching the manifest final-review gate.
- `ROUTER.md`: terminal unfinished micro-fix routes to Close when higher-priority Task Board/manifest review obligations are absent; Close read-set accepts bounded micro-fix authority without a milestone.
- `RECOVERY.md`: the same terminal state recovers into Close rather than a removed Execution reconciliation path.
- `MICRO_FIX.md`: exact stronger-review reuse is legal only after current-target refresh; no synthetic milestone is required.
- `CLOSE.md`: explicit qualified-micro-fix entry, refresh-before-review ordering, exact null/GREEN/pending review handling, target re-read and no-milestone finalization.
- `STATE.md`: terminal micro-fix state now points to Close for refresh/review/integration ordering.

Correction validation:
- corrected behavioral subject: `13975e34df485fe10d01730613e042e9dab952c7`;
- current workflow `main` is still exactly `03035876f3283d33e8a10ff43265f5be21a27a06`, so no authority drift needs reconciliation;
- PR #28 remains open, draft and reported mergeable against that same `main`;
- GitHub reports no combined status checks for the corrected subject, so no CI-execution claim is made;
- correction files have zero conflict markers, zero trailing-whitespace lines, and no stale references to the removed “Micro-fix final-review reconciliation” route;
- required logical E2E scenario 5 now has a complete no-milestone path through refresh, final-integration review and integration;
- scenarios 1–4 and 6–10 retain their accepted routing/state semantics; the correction does not alter their state owners, intake/base selection, feature promotion, worktree/stacked rules, RED isolation or fresh-handoff ownership.

## Corrected re-review boundary

The corrected implementation was authored by the chat that performed bounded remediation after review-01 RED. That chat is therefore not independent for the corrected subject.

Freeze the next REQUIRED review attempt on exact corrected subject `13975e34df485fe10d01730613e042e9dab952c7`. Keep M05-T01 and M05 non-terminal until a fresh independent reviewer records GREEN.
