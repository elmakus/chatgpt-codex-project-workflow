# M03-T01 Implementation Evidence — Micro-fix, execution, review and recovery semantics

Card: `M03-T01`
Milestone: `M03`
Implementation base: `7831c88a903a6d1d1743d280600ca993b60f4c55`
Implementation subject: `fe447d4e0322b8b4f1ef5a5f1fd46b703877a75b`
Workflow-main baseline checked: `03035876f3283d33e8a10ff43265f5be21a27a06`

## Result

Implemented the M03 ChatGPT-only direct micro-fix contract and branch-local execution/review/recovery semantics.

The frozen subject:
- adds `workflow/chatgpt_only/MICRO_FIX.md` as the canonical qualified micro-fix contract;
- allows a qualified issue to proceed from completed Intake directly into Execution Prep without a full Master Plan/milestone decomposition;
- materializes micro-fix state as one bounded fix Card on the manifest-selected Task Board with `plan_revision/current_milestone: micro-fix` and empty `milestones`;
- makes Execution Prep and Execution consume only the selected workstream state, while keeping the legacy/default Task Board path valid;
- keeps implementation/recovery Research pointers on the selected canonical Task Board, including Research opened from a workstream-level RED review;
- distinguishes Card/milestone review state in the selected Task Board from workstream final-integration review state in `WORKSTREAM.yaml`;
- adds `review.covered_by` so an already-independent exact GREEN verdict may satisfy the distinct workstream gate only when it covers the identical immutable integrated subject and the whole workstream acceptance surface;
- makes behavioral issue/feature workstreams carry at least a RECOMMENDED final-integration review requirement unless existing risk authority requires REQUIRED;
- keeps RED correction and same-branch recovery attached to the exact selected workstream instead of creating/consulting another mutable lane;
- preserves M04 ownership of worktree isolation, stacked reconciliation and target refresh, and M05 ownership of final UX/migration/E2E closure.

## JIT / OpenSpec classification

`openspec_change: null` is intentional.

M03 changes the repository's canonical workflow/state contracts directly in Markdown/YAML. The persistent state and reconciliation rules are fully specified in those active contracts plus the workstream templates; a second OpenSpec layer would duplicate the same authority rather than reduce ambiguity. This is the required JIT OpenSpec classification, not an omitted classification.

## Acceptance verification

GREEN:

- **R6 qualification:** all five accepted micro-fix criteria are explicit; any false/materially uncertain criterion returns to the smallest normal route instead of forcing direct execution.
- **Scenario C / direct fix:** a qualifying issue can use completed Intake + one bounded fix Card + selected workstream Task Board, with no synthetic Master Plan/milestone.
- **Scenario C / verification + fresh review:** the direct fix contract requires measurable acceptance/regression checks, durable implementation evidence, exact immutable subject freeze and at least RECOMMENDED independent review for behavioral/code/runtime-configuration changes.
- **Negative micro-fix trace:** failed/unknown R6 qualification routes to Research / Project Definition / Strategic Planning / normal Execution Prep as justified.
- **Pre-Task-Board recovery:** completed Intake records exact `path: micro_fix` + `next_route: execution_prep:micro_fix`; Router recovers that state before Task Board binding exists and Execution Prep owns materialization.
- **Branch-local execution:** Execution resolves/validates the selected manifest ↔ Task Board pair and explicitly reads only that selected canonical Task Board.
- **Scenario F / same-branch collision:** a fresh chat on the same branch/workstream recovers the one existing obligation; one selected Task Board still permits only one `in_progress` Card.
- **Branch-local Research:** implementation/recovery Research uses only the selected Task Board pointer; a final-integration RED review cannot place its Research continuation in the default board or another workstream.
- **Review ownership:** Card/milestone review remains Task-Board-owned; workstream final-integration review is manifest-owned and must not mirror the Card lifecycle.
- **Exact review coverage:** final-gate coverage reuse requires the identical immutable integrated subject, no behavioral drift after that subject, and the same authority/acceptance surface for the whole workstream.
- **RED correction:** final-review RED correction/Research remains confined to the affected selected workstream and creates a new exact pending subject after behavioral correction when coverage is not already proven.
- **Behavioral final-review requirement:** Execution Prep ensures intake-created behavioral issue/feature workstreams have manifest review requirement at least RECOMMENDED (REQUIRED under stronger risk authority); null state is explicitly not integration approval.
- **Legacy/default regression:** `implementation/TASK_BOARD.yaml` remains the fallback/canonical board when no branch-isolated workstream is selected; no migration is forced.
- **M04/M05 boundary:** M03 establishes the review/recovery contracts but explicitly does not implement worktree isolation, stacked integration, target refresh/rebase/retarget, or final UX/migration/E2E closure.
- **Foreign-policy boundary:** the changed M03 active contracts contain no imports of `workflow/chatgpt/`, `workflow/codex/` or `workflow/legacy/`.

## Static checks

- exact M03 behavior range `7831c88a...fe447d4e` changes 15 active `workflow/chatgpt_only/*` files;
- semantic validation matrix: **16/16 PASS**;
- trailing-whitespace scan across all 15 changed M03 behavior files: **PASS**;
- conflict-marker scan across all 15 changed M03 behavior files: **PASS**;
- workstream manifest template shape: `review.covered_by` present under the distinct workstream review block — **PASS**;
- workstream Task Board template identity binding (`workstream_id` + exact branch) — **PASS**;
- workstream Task Board micro-fix discriminator (`plan_revision/current_milestone: micro-fix`) and empty milestone container shape — **PASS**;
- current `main` is still `03035876f3283d33e8a10ff43265f5be21a27a06`; project branch is ahead and **0 behind**, so no target drift affected this M03 implementation subject — **PASS**.

A native local `git diff --check` and YAML parser were not available in this execution runtime; the exact changed-file trailing-whitespace/conflict-marker scan and structural template assertions above are claimed instead. No YAML-parser claim is made.

## Changed behavior files

- `workflow/chatgpt_only/MICRO_FIX.md`
- `workflow/chatgpt_only/EXECUTION_PREP.md`
- `workflow/chatgpt_only/EXECUTION.md`
- `workflow/chatgpt_only/REVIEW.md`
- `workflow/chatgpt_only/STATE.md`
- `workflow/chatgpt_only/RECOVERY.md`
- `workflow/chatgpt_only/RESEARCH.md`
- `workflow/chatgpt_only/ROUTER.md`
- `workflow/chatgpt_only/WORKSTREAMS.md`
- `workflow/chatgpt_only/INTAKE.md`
- `workflow/chatgpt_only/REPOSITORY.md`
- `workflow/chatgpt_only/TASK_CARDS.md`
- `workflow/chatgpt_only/TASK_CARD_TEMPLATE.md`
- `workflow/chatgpt_only/WORKSTREAM_TASK_BOARD_TEMPLATE.yaml`
- `workflow/chatgpt_only/WORKSTREAM_TEMPLATE.yaml`

## External state

No material external runtime/deployment mutation was required. Implementation was repository-only remote GitHub mutation, so the accepted R8 local-worktree requirement is not triggered by this execution mode.

## Review boundary

Independent review: `RECOMMENDED`.

Review must inspect exact subject `fe447d4e0322b8b4f1ef5a5f1fd46b703877a75b` against `implementation/cards/M03-T01.md`, the M03 Master Plan section, applicable requirements/ADR, accepted M01/M02 dependency results and this evidence. The implementing chat must not issue the independent verdict.
