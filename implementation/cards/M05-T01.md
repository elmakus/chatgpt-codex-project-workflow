# M05-T01 — UX, templates, migration and final regression closure

- Milestone: `M05`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the canonical Task Board selected by the ChatGPT-only state resolver.

## Authority slice

- Master Plan / milestone contract: `planning/CHATGPT_ONLY_MULTI_WORKSTREAM_MASTER_PLAN.md#M05--ux-templates-migration-and-regression-coverage`
- Requirements: `requirements/CHATGPT_ONLY_MULTI_WORKSTREAM_INTAKE.md` — R1–R15 plus acceptance scenarios A–F
- Accepted decisions: `decisions/ADR_CHATGPT_ONLY_BRANCH_ISOLATED_WORKSTREAMS.md`
- Relevant OpenSpec: `none` — this Card closes workflow routing/state/documentation contracts directly; adding a second behavior-spec layer would duplicate accepted authority.
- Accepted dependency results:
  - `project-handoffs/M01_HANDOFF.md`
  - `project-handoffs/M02_HANDOFF.md`
  - `project-handoffs/M03_HANDOFF.md`
  - `project-handoffs/M04_HANDOFF.md`
  - `implementation/evidence/M04-acceptance.md`

### Must preserve

- branch-isolated workstreams are the concurrency unit; seriality remains one `in_progress` Card per selected Task Board;
- legacy/default `implementation/TASK_BOARD.yaml` remains valid without migration;
- active workstream/manifest resolution precedes branch-isolated Task Board state interpretation;
- `#issue` / `#feature` are intake entry directives, not permanent session-scope boundaries;
- `#feature` preserves the user-owned Brainstorming → Project Definition promotion gate;
- micro-fix keeps bounded scope/evidence/review without requiring a synthetic Master Plan;
- Card/milestone review state remains Task-Board-owned; workstream final-integration review remains manifest-owned;
- behavioral issue/feature integration retains at least a RECOMMENDED fresh independent review gate unless exact stronger independent coverage proves the identical immutable integrated subject and whole acceptance surface;
- local concurrent mutation requires isolated worktrees/equivalent checkouts; remote-only GitHub operations remain exempt;
- stacked parent/dependency provenance and M04 target-refresh/review-validity semantics remain intact;
- fresh-session prompts stay locator-only and return to router-owned continuation after the named entry role;
- no mutable global workstream registry/scheduler and no mixed/Codex/legacy parallel-lane semantics enter active `chatgpt_only`.

## Dependencies

- M01–M04 are GREEN and closed.
- Current workflow `main` must be rechecked at Refresh Gate and before the final review boundary.

## Outcome

Operators can discover, start, recover, review and continue legacy/default or branch-isolated ChatGPT-only workstreams from normal fresh chats without knowing internal state layout, while active documentation/templates consistently describe workstream-local seriality, canonical state ownership and locator-only handoffs.

## Scope

### Included

- update `workflow/common/USER_STOP.md` with branch-isolated workstream-aware fresh handoff shapes, including manifest-owned final-integration review, while keeping prompts locator-only;
- keep `prompts/CHATGPT_FRESH_SESSION.md` as a thin pointer to the canonical USER_STOP owner and update only where needed for workstream pointer semantics;
- update `prompts/CHATGPT_START.md` so ChatGPT-only recovery explicitly resolves a selected workstream/manifest before branch-isolated mutable state and recognizes the manifest-owned final-integration review lifecycle;
- update `README.md` where current wording still assumes repository-global single-card state or a single mandatory default Task Board;
- update `templates/PROJECT.md` with an optional non-live workstream-root convention and selected-state wording while keeping PROJECT free of live workstream/Card/review state;
- verify existing `WORKSTREAM_TEMPLATE.yaml` and `WORKSTREAM_TASK_BOARD_TEMPLATE.yaml` cover the accepted manifest/Task Board contracts; amend only if M05 coherence requires it rather than duplicating templates;
- update `CHANGELOG.md` for the complete multi-workstream/intake capability;
- create durable `docs/audits/CHATGPT_ONLY_MULTI_WORKSTREAM_INTAKE_SCOPE.md` defining the final independent architecture/coherence review objectives and required logical E2E matrix;
- perform implementation-author static/coherence regression checks across active ChatGPT-only/common entry, docs, prompts and templates; fix only contradictions inside accepted authority;
- execute and persist the required logical E2E matrix 1–10 from the approved M05 plan;
- verify current `main` drift and final PR/integration assumptions before freezing the final exact review subject.

### Excluded

- merging PR #28 or otherwise integrating this feature branch into `main` before the required independent architecture/coherence review is GREEN;
- changes to mixed, Codex or legacy policy namespaces;
- a global workstream registry, scheduler or lock service;
- forced repository-wide migration of existing single-workstream projects;
- unrelated documentation cleanup or redesign;
- new test infrastructure solely to simulate coverage when no suitable existing harness exists.

## Acceptance

- active ChatGPT-only/common docs no longer make a repository-global one-Card claim where workstream-local seriality is intended;
- active docs distinguish the selected canonical branch-isolated Task Board from the legacy/default `implementation/TASK_BOARD.yaml`;
- final-integration review handoff for a branch-isolated workstream can use the exact manifest as its smallest durable start pointer without copying workstream state into the prompt;
- `PROJECT.md` template may name a workstream-root convention but does not become a live registry or state mirror;
- existing workstream manifest/Task Board templates remain sufficient and internally coherent with R1–R15;
- all ten approved M05 logical E2E scenarios trace deterministically through the active contracts;
- single-workstream compatibility, issue intake, feature intake, micro-fix, same-workstream recovery, worktrees, stacked integration, target refresh, RED isolation and fresh-handoff continuation are all covered by durable audit/evidence;
- no active changed contract imports Codex/mixed/legacy execution semantics;
- a durable final architecture/coherence review scope exists before the fresh-review boundary;
- the exact final implementation subject is frozen for a fresh independent REQUIRED architecture/coherence review before PR integration.

## Required tests / checks

- compare current workflow `main` with the accepted branch baseline and classify any material drift before implementation/freeze;
- coherence scan for repository-global seriality/state-ownership wording across active ChatGPT-only/common entry docs, README, prompts and PROJECT template;
- handoff trace: legacy Task Board review uses Task Board pointer; branch-isolated Card/milestone review uses selected workstream Task Board; manifest final-integration review uses selected manifest; all prompts remain locator-only;
- template trace: `WORKSTREAM_TEMPLATE.yaml` + `WORKSTREAM_TASK_BOARD_TEMPLATE.yaml` preserve identity binding, parent dependency, final-integration review ownership and one-in-progress-per-board semantics;
- required logical E2E matrix 1–10 from the M05 Master Plan section;
- policy-boundary scan for accidental `workflow/legacy/`, `workflow/codex/`, old `workflow/chatgpt/` execution imports in the changed active ChatGPT-only/common files;
- conflict-marker/trailing-whitespace inspection for the exact M05 implementation subject;
- inspect PR #28/current-target state and ensure final review/integration assumptions are explicit and current;
- when no suitable existing automated regression harness exists, record the exact static/coherence checks performed rather than claiming nonexistent automation.

## External write/readback needs

none beyond ordinary durable GitHub project-state writes on the implementation branch. PR merge/integration is explicitly outside this Card until independent review is GREEN.

## Independent review

`REQUIRED` — this is the final implementation Card for a change that rewires ChatGPT-only routing/state ownership, concurrent workstream recovery and integration/review semantics. The fresh reviewer must judge the exact frozen **final feature-branch implementation subject** against the durable architecture/coherence audit scope, full accepted authority and required logical E2E matrix before PR integration.

## Contract overrides

none
