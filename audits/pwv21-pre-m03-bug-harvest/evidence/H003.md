# H003 Reproduction and Technical Triage

## Exact subjects

- Original audited PWv2 commit: `elmakus/project_workflow_v2@4fb4bfb7d7b1481d6f347c182fc96a5a1135e045`
- Batch current-candidate snapshot: `elmakus/project_workflow_v2@31764d82911de523e36d2de5c5d98ce7c3cedb18`

## Claim under test

The generic top-level Research dispatch can return before a higher-precedence durable human/premium boundary is evaluated.

## Expected canonical semantics

The audited production router declares `PRIORITY_FOUNDATION` in this order: `explicit_human_or_premium_boundary`, independent review/RED correction, `research_return`, result reconciliation, current Card, next legal stage. Its exact-subject production test asserts that ordering. `workflow/DEFINITION.md` says a GREEN Definition with premium A due is a real human-facing boundary before material Strategic Planning, and `workflow/USER_STOP.md` independently lists premium A/B/C as real-stop sources.

Research is factual and non-authoritative. `workflow/RESEARCH.md` gives it one exact origin/return owner and says it never selects a different target by itself. Nothing in the canonical state contract says an active Research record cancels or satisfies an already-due premium boundary.

## Minimal reproduction

Use the audited router fixture and add two validator-legal records under the same selected workstream.

1. Promoted Brainstorming bound to `scope-plan@1` (the same shape used by the production router tests).
2. GREEN Definition bound to that exact promoted subject, with:
   - `completeness_audit = "green"`
   - `premium_a = "due"`
   - valid authority locator(s).
3. Active Research:
   - `workstream_id = "sample-workstream"`
   - `origin_role = "definition"`
   - `origin_subject = "scope-plan@1"`
   - `return_target = "definition"`
   - `return_reconciliation = "pending"`
   - `return_result = ""`
   - all four source classes present with `pending` status, matching the canonical active-Research fixture.

The relevant validators independently accept these shapes. `validate_research()` accepts `active`, `origin_role = definition`, and `return_target = definition`; `validate_definition()` accepts GREEN with `premium_a` in `{due,satisfied}`. There is no cross-record validator forbidding active Research while the Definition-owned premium-A gate is due.

## Observed original-subject behavior

At `4fb4bfb7d7b1481d6f347c182fc96a5a1135e045`, production `select_route()` reads and validates the workstream Research locator immediately after the manifest. If Research is active, lines 208-213 return `route / research` immediately.

The Definition record is not read before that return. The later Definition branch that returns `stop / premium_A` for GREEN Definition with A due is therefore unreachable for this state.

This contradicts the router's own declared priority foundation, where an explicit human/premium boundary precedes Research return/continuation.

## Technical verdict

CONFIRMED_MATERIAL

## Why

This is one minimal validator-legal vector, independent of H002: the higher-precedence owner is Definition's premium-A human boundary, not Brainstorming's explicit-user-stop flag. Canonical contracts say that premium A is a real stop and the production router explicitly declares premium boundaries above Research in its precedence foundation. Yet the generic Research branch executes first and returns without even loading the due Definition state.

The defect is therefore not merely a documentation-order ambiguity; the selector's control-flow order contradicts its own canonical precedence tuple and permits a lower-priority factual obligation to outrun a durable human authority gate.

## Current candidate check

- candidate SHA: `31764d82911de523e36d2de5c5d98ce7c3cedb18`
- status: `persists`

The frozen candidate retains the same ordering. `select_route()` reads/validates Research at lines 329-331 and returns active Research at lines 332-337. Only later does it read Definition and invoke the PWv2.1 mechanical-policy decision for premium A (K002) at lines 463-470. The candidate validators still independently admit active Definition-owned Research and GREEN Definition with premium A due.

The policy-kernel introduction therefore does not repair this vector because the generic Research early return happens before the kernel's premium-A route can execute.

## Likely defect boundary

Primary defect boundary: top-level router precedence between generic Research dispatch and durable human/premium boundaries.

Secondary surface: cross-record state coherence permits the two obligations to coexist, so the selector must either honor declared precedence or reject an impossible combination. No product repair is performed here.

## Regression shape

Build one selected-workstream fixture containing exact-bound promoted Brainstorming, GREEN Definition with `premium_a = "due"`, and active Definition-origin Research. Require production `select_route()` to fail closed or return `stop / premium_A` before any `route / research`. The test should also assert that the stop loads `workflow/USER_STOP.md`.

The regression must exercise production validators and selector, not a copied precedence model.

## Limitations

Dynamic execution of the exact checkout was not available in this worker. Remote Desktop Commander reported monthly usage paused, and the Codex-native command bridge rejected the available call because no valid outer-harness turn token was exposed. The technical conclusion is based on immutable exact-subject source inspection, the production validator code, the production selector's deterministic early return, and exact-subject production tests that assert the precedence tuple and legal component shapes.

No product code, canonical workflow state, PR, merge, or active PWv2.1 workstream was modified.
